# 更新日志

**这个文件是 GitHub Release 发布说明的唯一来源。**

打 tag 之前必须先写好对应版本的那一节：CI 的 release job 会用
`python desktop/release_notes.py <tag>` 抽取该节作为 Release 正文，
抽不到、正文为空、或正文里还留着 `__VERSION__` 占位符就直接失败，
不会把附件传上去。`python desktop/make_release.py` 在打包前也会先拦一次。

为什么要有这道关卡：beta0.7 的发布说明只有一行自动生成的 compare 链接。
早期版本直接提交到 main，GitHub 自动生成的发布说明没有完整记录实际变化，
所以用户可读的说明统一在这里维护。现在 CI 把这里的内容放在前面，
再让 GitHub 追加自动生成的 compare 链接。

## 格式约定

- 每节以 `## <版本号>` 开头，版本号与 `internal/version/VERSION` 完全一致（不带 `v` 前缀）。
  抽取时只认完全相等的标题，所以 `beta0.7` 不会命中 `beta0.7.1`。
- 尚未发布的改动写在 `## 未发布` 一节；发版时把标题改成真实版本号，再补充验收边界。
- 下载清单由脚本按版本号自动生成，不在正文重复维护。
- 说明用户可见变化和未完成验证，不用内部任务编号代替说明。

## beta0.9.3

**本地候选版，尚未公开发布；当前公开下载仍为 beta0.9.2。** 本轮把像素校园、桌面伙伴、待办与专注整理成更连贯的日常体验，保留现有本机存档。

- **新的首页与湖畔庭院**：首页可直接选择伙伴、写下小事、开始专注或去收获。回访时收紧欢迎展示，突出正在做的事。农田加入原创湖畔场景，点选六块真实田地后集中选择种子、种植、浇水、收获或开田；伙伴小屋重排照料、成长、每日目标、图鉴与收藏，提高部分提示字号。
- **学习和校园服务分区**：学习工具分为专注、课表、成绩；校园服务可直达空间、公告、常用入口与学院琴房，减少翻过无关表单。记住主动选择的公告来源及本科/研究生层次。未录入成绩时显示空状态，不再让 0.00 看起来像真实绩点。
- **待办可以整理和回看**：新增编辑、计划日期、完成时间、已完成分类、归档和恢复。旧待办继续保留，没有记录过的日期不凭空补写；只想写一件小事时仍可直接添加。
- **专注和真实记录连在一起**：保留 5/25/45 分钟快捷项，增加 1–120 分钟自定时长，可选择关联一件待办。完成并领取后记录当时的任务文字和时长，近七日回顾显示实际专注分钟与完成的小事；最多保留最近 365 条专注历史。取消不算完成，结束计时不会自动勾完待办，也不监控学习行为。
- **专注与成长按投入时间计算**：每完成并领取 1 分钟专注获得 1 荔枝币和 1 成长，一次 45 分钟与九次 5 分钟的直接奖励相同。专注纪念章按累计 75 分钟判断，旧版已经领取的纪念章保留。
- **少刷点击，多一点陪伴**：每只伙伴每天前 3 次有效摸头各增加 2 成长与 3 亲密度，后续仍可互动和改善心情；每天前 5 件首次完成的小事各增加 2 成长。反复勾选同一件事不会重复领奖，删除重建也受当日上限约束。
- **长周期作物也值得种**：保留一分钟成熟的新手萝卜，调整四种作物的产量、售价与收获成长，使更长等待带来更好的单次收成。浇水缩短剩余时间，不再额外刷成长；装饰和开田价格没有为拖慢进度上涨。庭院解锁永久保留，切到低等级伙伴不会重新锁住已解锁的作物。
- **七次来访的小故事**：不同日子回来可翻开「荔园拾光」七个章节，收下校园纪念物，放进回忆册与小屋。无需连续签到，晚回来不会清零。荔宝、小白和阿青补齐普通、开心、低落和睡眠四种状态，栗栗沿用四种状态；四位伙伴增加共 56 条有各自个性的摸头、喂食、陪玩、醒睡、专注完成和收获台词。
- **安装版的安静陪伴设置**：专注完成时可通知，即使主窗口隐藏也能提醒；支持关闭通知与勿扰，完整退出后不发通知。宠物显示/隐藏与置顶偏好可保留；Windows 自启由用户主动开启，启动时不弹主窗口，不默认登记自启。
- **跨日和草稿修复**：日期变化及窗口恢复可见时刷新每日目标、补给、待办默认日期和周回顾，避免昨天的状态一直留在屏幕上。照料、专注操作与待办筛选保留旁边未提交的草稿；修复确认框按 Esc 沿用上次选择、待办编辑字段冲突、公告选择保存冲突、慢查询占住保存操作和研究生查询错误状态显示的问题；补回常用服务联系电话入口，版本信息不再等待较慢的校园状态查询。
- **本地导出庭院分享图**：先预览，再主动保存带像素校园背景的 1200×800 PNG，展示当前伙伴、累计专注、收获与田地数量，不含学号、课表或成绩；图片在本机生成，不自动上传。
- **手动检查更新与更方便的反馈**：设置中可检查正式版/测试版渠道，查看对应更新说明并打开下载；不会自动下载或安装。反馈前可预览并复制版本与环境信息，不含账号、密码、Cookie 或学校个人数据，不自动发送。
- **存档和交付细节**：新增记录兼容旧存档，保留伙伴成长、作物和已领取奖励；扩大存档容量并统一导入与保存限制。补齐安装包项目、第三方与字体许可材料，按不同登录方式说明凭据的保存位置和有效期。奖励、成长、学习记录、桌面偏好、更新与反馈检查加入发布检查清单。
- **学校连接错误更准确**：场地读取不再把所有连接失败都解释为“需要校园网”，区分访问失败与学校实际要求。学校原页面仍用于本人登录、预约确认和结果查看，程序不代提交预约。

**候选版验收边界**：本地安装包与便携包已构建，25 个相关检查脚本及 Go 测试、静态检查通过。真实浏览器完成了专注与周回顾、待办编辑归档、种植浇水收获、偏好恢复、版本查询及 PNG 预览保存；同版首页、农田、伙伴小屋与学习页截图已核对，420px 窄窗农田无横向溢出。便携 ZIP 和安装版构建目录的资源、版本与许可材料核对通过；尚未完成本候选包实际安装升级、原生系统通知、自启和资源占用验收，未将构建目录检查或旧版结果当作新安装包通过。全套美术统一、真实同学试用、长期成长平衡、物理多显示器与完整宣传录屏仍有待办。

**学校业务边界**：本次网络环境已确认在校园网内，但本机部分学校查询仍访问失败；研究生官方页可达、会话过期，等待本人重新登录，暂无本科权限测试账号。真实成绩、本科课表、研究生非空排课、安装版会话接回、预约完整流程、学院琴房权限及校园认证仍有待验收范围，不因入口或错误提示修复而宣称通过。校内后端、Docker、云同步和自动安装更新继续延期；安装包仍未签名。

## beta0.9.2

- **四位桌面伙伴**：保留荔宝和栗栗，加入原创像素白鹭「小白」与小龟「阿青」。伙伴小屋可点击形象卡片选择，桌宠菜单增加「切换伙伴」；两处选择同步，每位伙伴分别保存名字和成长。旧存档追加缺少的基础伙伴，保留已有伙伴及当前选择。
- **切换伙伴时清理改名草稿**：显式切换及照料遇到存档冲突后换到另一伙伴，都不会把上一位的改名草稿带过来。
- **成绩分页**：按学校返回的总数继续读取后续页面；缺少总数时保留未确认完整的提示，中途失败或分页不一致时报错，不把部分结果当作完整成绩。查询受整体时限约束，取消后停止后续请求。
- **学校会话切换**：重新读取学校登录时，即使窗口里已经没有有效 Cookie，也清除对应业务的旧账号会话，避免退出后仍显示上一个账号的数据。学校返回的权限不足、连接失败等原因按实际提示显示。
- **升级与备份检查**：发布流程增加从已发布 beta0.9.1 安装包升级的检查，核对庭院、学习记录、加密账号配置和桌宠设置。实际窗口检查覆盖存档下载、取消恢复及确认恢复，使用独立的合成数据。

**验收边界**：升级检查必须在干净 Windows 发布环境通过后才发布。真实学校成绩、本科课表、研究生非空课表、安装版会话接回、预约完整流程、学院琴房权限和两类校园网认证仍有待验收项；分页和会话回归不替代学校真实数据验收。校内后端、Docker 部署和自动更新本轮不实施。安装包仍未签名。

## beta0.9.1

- **宠物点击菜单**：左键或右键点击桌面伙伴，查看饱腹、心情和精力，直接摸头、喂食、陪玩或切换睡眠。照料操作沿用庭院规则并保存到同一份本机存档；菜单也可打开庭院、农田、学习工具或主窗口，以及隐藏宠物、退出应用。
- **随手调整大小与位置**：鼠标停在宠物上滚动，每次增减 10 个百分点，范围 40%–200%；菜单可放大、缩小或恢复默认，原有设置滑杆与托盘预设保留。按住宠物拖动位置，大小和位置在重启后恢复。
- **像素校园界面打磨**：保留像素深大与木质导航，缩短顶部场景和欢迎区，改用浅纸色、绿色主按钮与更清晰的字号层次。首页移除重复网络徽标，新增伙伴场景卡片，统一表单、服务入口、窄屏和暮色样式。
- **学校登录修复**：放行实际使用的官方 WebVPN 认证主机，保留精确域名和 HTTPS 限制；更新研究生课表登录提示，学校窗口连接失败显示脱敏错误代码。
- **发布检查**：新增桌面宠物照料命令的回归检查，保留安装版、便携版、macOS 凭据及其他现有发布检查。两种 README 和便携包快速开始说明同步更新。

**发布检查**：本地真实 Electron 与 Go 程序已验证点击菜单、隐藏主窗后的照料与库存保存、页面跳转、双轴拖动、大小和位置恢复；已检查实际菜单显示。自动发布仍须通过 Windows 安装、重启、重装与卸载，以及现有便携版和 macOS 检查。

**学校业务边界**：研究生备用登录与当前课表已用真实账号读取，但当前样本没有已排课程；本科权限、真实成绩、非空课表和安装版会话接回仍未验收。官方浏览器已选时段并打开预约确认表单，未提交；原生学校窗口详情加载和连接稳定性仍有问题，学院琴房连接也未恢复。本版不代表完整预约或学校账号业务已通过。在线成绩仍只读第一页，安装版自启未实现，安装包未签名。

## beta0.9.0

- **常驻桌面宠物与托盘**：关闭主窗口后可从宠物或托盘重新打开；支持隐藏/显示、三档尺寸和 40%–200% 连续缩放，大小保留到下次启动。伙伴有心情、睡眠与点击动作，系统减少动态效果设置生效。
- **修复宠物交互与打包**：补齐安装包遗漏的设置模块；修复关窗后无法重开、滑杆未绑定、一次动作不恢复、缩放保存失败被吞掉及自定义大小误勾选。小尺寸文字设有可读下限；显示器变化会重新定位。
- **应用内学校页面**：安装版可在独立窗口打开教务、成绩和社区预约原页面；保留学校验证码与二次验证，由用户在原页预约并查看结果。登录仅本次运行，不向庭院页面暴露会话，也不修改学校页面的安全策略。
- **学校登录接回查询**：选择本科/研究生的课表或成绩，在学校窗口登录后，返回应用读取本次登录。必须收到相应业务数据才显示可用，修复了原账号登录分支把 HTTP 200 登录页面当成成功的问题。普通安装版界面不再要求复制 Cookie；便携版保留折叠的备用入口。
- **校历 OCR**：优先使用简体中文识别器，修复英文用户语言导致校历被识别成乱码的问题（#9，感谢 rcwalter24 的报告与补丁思路）。失败保留缓存并显示实际原因。
- **学院琴房只读接入**：登录、分页琴房列表和我的预约；密码用后清空、切换失败清掉旧登录，不提供预约、取消或开门接口。
- **发布检查与协作**：安装包检查覆盖宠物/托盘、主窗重开、设置与菜单缩放、重启保存；main 合并必须通过 test。同步两种 README，并增加问题反馈及 PR 模板。

**验收边界**：真实本科/研究生课表、成绩、学校预约登录全流程和学院琴房权限仍待本轮账号验收；当前测试环境的教务、认证与 WebVPN 连接失败，不将模拟响应视为真实通过。程序不会代提交预约。多显示器只完成几何和显示事件处理检查，本机只有一块显示器；英文 Windows 的完整 OCR 场景也仍需对应设备。在线成绩只读第一页；安装版自启未实现；安装包未签名。

## beta0.8.0

- **新增「场地与琴房规则速查」**。学校场地接口其实一直把规则数据发给我们，此前全部丢掉了：类型名（共享琴房 3 / 网络面试间 6 / 心灵小屋 2 / 共享会议室 4）、单日上限、可提前天数、每日开放时段、爽约黑名单天数，以及一份学校统一使用须知。现在按「类型 + 社区」分组展示，一次请求拿全（列表里内嵌的 type 与单独查询键集相同，不新增任何接口目标），不需要登录。
  - 使用须知是学校返回的 HTML，已在服务端剥成纯文本，原始 HTML 不进浏览器，前端照旧转义。
  - 学校四类场地的须知是同一份文本，所以只显示一次，不按场地重复。
  - **没有做图片**：学校只给裸文件名，拼不出可访问 URL，这轮不热链，改为引导去学校页面。
  - 办理预约仍在学校官方页面完成，本页只读。

- **修掉「场地与琴房规则速查」可能让整个「校园服务」页静默停止渲染的缺陷**。学校返回的每日开放
  时段是一个 64 位整数掩码，而 JavaScript 的位运算只有 32 位，所以只能另算位数；原实现在遇到
  非整数、带千分位的字符串或超出安全整数范围的值时会抛异常，而它又在页面渲染的关键路径上被
  无条件求值——**结果是整个「校园服务」页静默不更新，并且不向用户报任何错**。现在这类值一律
  显示为「—」，绝不抛。同时给「校园服务」的每张卡片和整个页面各加了一层兜底：坏掉一块只降级
  这一块，整页坏掉也换成一页可读的提示，不再留空白。
- **场地规则不再张冠李戴**：原来按类型名称分组，两类场地重名时会被并成一组、并用其中一类的
  规则去描述另一类；现在按类型编号分组。
- **同一份场地数据不再被两个卡片各请求一遍**，读取失败时也不会把失败结果缓存住。
- **学校统一使用须知的分段修好了**：学校用 `div` / `h3` / 表格排版时，整段须知原来会被挤成
  一行；HTML 注释文字也不再混进正文。
- **修好「用命令行版开机自启」**（F27）。这条路上登记给系统的参数是 `szunet login --auto`，而
  `szunet login` 从来没有 `--auto` 这个参数——参数解析遇到不认识的开关会直接退出，**所以开过
  这个开关的用户，开机时程序什么都没做，而且不会有任何报错**。现在改为登记 `login`（登录本来
  就是非交互的，只用已保存的凭据），同时保留了 `--auto` 兼容开关，**已经开过这个开关的用户不必
  手动改注册表**。桌面端（`szudesktop`）的开机自启不受影响，一直是好的。

- **新增 Windows 安装版**：Electron 提供独立窗口，安装包内含 Go 引擎；旧版便携 EXE 和各平台命令行版继续提供。保留荔枝庭院的像素校园界面和原有本机存档。
- **离线启动**：窗口就绪只检查本机服务，网络与学校门户查询在后台进行，避免校外或断网时误报启动超时。
- **首次打开卡住**：明确设置前端模块的 JavaScript 响应类型，避免 Windows 文件关联把脚本误判为普通文本，导致安装版或便携版停在“正在打开庭院”。
- **修复启动与退出**：已有 Go 引擎可以复用，不再把正常复用误判为启动失败；引擎文件缺失、健康检查超时和分块启动日志都会得到正确处理。关闭安装版只清理本次启动的引擎，复用的后台服务保留。
- **升级 Electron 至 44.4.5**，限制窗口导航和权限，增加内容安全策略；引擎意外停止时提供重新打开/退出提示。
- **学校查询不再卡住整页**：读取公告、场地、校历时仍可切换页面；加载状态留在对应卡片，过期查询结果不会覆盖新条件。
- **明确安装版自启范围**：本版不新建开机自启项，可关闭旧项；便携版与命令行的自启保留。常驻宠物窗、托盘与安装版后台自启留到后续版本。
- **发布与安装检查**：发布只收取指定构建产物，安装包和便携 ZIP 均提供 SHA256；CI 增加安装、再次启动、同版本覆盖保留存档、已有引擎共存及卸载检查。

安装包仍未签名。学校真实成绩与课表的完整账号验收仍未完成；预约继续在学校原页面办理。本次桌面安装包更新不代表这些业务已通过验收。

## beta0.7.3

**只有 macOS 用户会感觉到变化**：修复了 beta0.7.1 / beta0.7.2 上可能存不了凭据的缺陷。
Windows 与 Linux 的行为与 beta0.7.2 相同。

- **修好 macOS 上可能存不了凭据的缺陷**（F26，P0）。真实的 `security -w` **可能**要求输入两遍
  （密码 + 确认），而 beta0.7.1 与 beta0.7.2 只喂了一行，在那些机器上会得到
  `passwords don't match`（退出码 44）、条目根本建不起来，于是 `szunet config set`
  与桌面端保存凭据/会话全部失败。现在改为喂「密码 + 确认」两行，只问一遍的机器上
  多出的那行是没人读的剩余输入，两种情况都成立（真机 macOS 26.6.2 上验证过 4 次）。
  失败时程序一直是明确报错的，**没有泄漏密码、也没有写成明文文件**，但功能确实不可用。
- 这个缺陷是 CI 新加的 macOS 探针在真机上抓到的。该探针此前设了 `continue-on-error`，
  会把失败显示成成功；现已去掉，并让发布 job 依赖它——macOS 凭据这条路坏掉时发不了版。
  探针自身也修了两个 bug：漏跑界面资源同步（`go vet` 会因 `go:embed all:assets` 失败），
  以及建了临时钥匙串却用不带 `-k` 的 `add` / `find`（写进默认钥匙串、只在临时的里找，
  必然报 could not be found）。
- 两个测试替身（可移植版与 unix 真 exec 版）现在如实模拟 `security` 的两次提问，
  所以这条性质在本地与 Linux CI 上也守得住，不必依赖 macOS。

### 需要说明的边界

- `security` 问不问第二遍**并不稳定**：同一镜像的后续 4 次运行都只问一遍。
  所以「会要求确认」那种状态下的两行喂法尚未被直接观测到；探针每次运行都会记录所处状态，
  采到就会自动闭合。
- 真机 macOS 上完整跑一次 `szunet config set` → `config get` 往返仍未做。
- 本科与研究生的成绩、课表、教务登录仍**未做真实账号验收**，学校个人业务请以官方系统为准。
- 本版仍无数字签名；校园认证仍待在真实教学区 / 宿舍网络现场验收。

## beta0.7.2

安全与诚实性修复为主；桌面端界面只有课表卡片一处变化。

### 相对 beta0.7.1 的变化

- **命令行的 `szunet status` 现在没有保存账号也会查在线状态**（F22）。此前写的是「有账号才查」，
  而学校的门户查询本来就按出口 IP 回答、跟账号无关；结果同一台机器同一时刻，
  桌面端显示「已在线」、命令行却显示「账号状态：没查到」，很容易被误判成掉线。
  JSON 输出新增 `online_known`；没有账号且确实离线时会补一句登录提示。
- **研究生「教务登录」与「我的课表」拆成两张卡**（U16）。登录卡只放学号、密码、验证码和清除登录；
  课表卡放研究生课表读取与展示，以及本科入口和说明。没登录时读取按钮置灰并说明前置步骤。
- **删掉了预约的服务端写操作端点**（F23）。beta0.6.1 当时只撤下了界面入口，服务端的
  `/api/booking/{session,history,prepare,commit}` 还留在程序里，其中 commit 会真的向学校提交预约，
  而这条路径从未用真实账号验收过。现在四个端点全部删除，预约模块只保留只读的场地与空位查询，
  代码层面不再存在向学校提交预约的通路。**界面上看不出变化**——本来就已经是跳学校原页办理。
- **凭据不再有明文兜底**（F24）。Linux 上缺 Secret Service、或密钥环写入失败时，
  此前会把校园网密码写成权限 600 的明文 JSON 并报告「保存成功」；Windows 上拿不到用户目录时
  也会写一个相对路径的明文文件。现在这两种情况都明确报错、不落任何明文文件，
  与业务 Cookie 的处理标准一致；「忘掉账号」仍会清掉老版本可能留下的明文文件。
  **影响**：没有密钥环的 Linux 机器上无法再保存密码，需要每次登录手动输入，
  或安装 gnome-keyring / libsecret（提供 `secret-tool` 命令）。
- **实验 VPN 模块的来源声明改成如实描述**（F11）。此前包注释写着「代码为自行编写，
  未复制任何厂商或第三方的源代码」，而来源与授权其实没有核实过，现已删除这句保证。
  该模块不在任何发布件里（只有 `-tags campusvpn` 才会编译），**本次改动不影响用户拿到的程序**。
- **CI 增加 macOS 测试 job**（F25）。此前 darwin 只被交叉编译、从不执行，
  而 `szunet-darwin-*` 是随发布分发的。新 job 在 macOS 上跑静态检查与凭据测试，
  并用一次性钥匙串直接验证「`security -w` 放末尾时会从标准输入读密码」这个假设。
  该 job 不阻断发布。

### 需要说明的边界

- macOS 保存凭据这条路**仍未在真机验收**。如果上面那个假设不成立，`szunet config set`
  会明确报错而不是静默泄漏密码，但也就意味着存不了凭据。
- 本科与研究生的成绩、课表、教务登录**仍未做真实账号验收**，学校个人业务请以官方系统为准。
- 预约一律在学校官方页面办理，应用内不再提供任何提交入口。
- 校园认证仍需在真实教学区 / 宿舍网络完成现场验收；本版仍无数字签名。

### 仓库与文档

- 新增 `SECURITY.md`（漏洞报告渠道、三平台凭据现状、已知未解决项）与
  `CONTRIBUTING.md`（环境、界面资源规矩、提交前必跑清单、红线、发布流程）。
- `docs/STATUS.md` 新增第 36 节（全项目严重问题复核，逐条带文件行号）与第 37 节（修复与验证证据），
  更正了六处过期的「未发布」标记，并修掉问题总表里一个会让 F21 / F22 两行在 GitHub 上渲染断裂的空行。
- 两份 README 修正了与实际代码不符的表述（预约端点、各平台凭据存放方式），
  补上缺失的 `szunet vpn` 子命令、完整的 10 个前端回归清单，以及一个指向 `github.com/releases` 的坏链。
- **发布说明改为以本文件为唯一来源**。此前 CI 只有 `generate_release_notes: true`，
  而本项目直接提交 main、没有 PR，GitHub 只能生成一行 compare 链接——beta0.7 就是这样发出去的。
  现在 release job 会抽取本文件里对应版本的那一节作为正文，抽不到就不上传任何附件；
  `make_release.py` 在打包前也会先拦一次。

## beta0.7.1

修复版：只含一个安全修复和一处发布件质量问题，桌面端功能与 beta0.7 相同。

### 相对 beta0.7 的变化

- **macOS 保存凭据不再经命令行参数暴露**。原先执行 `security add-generic-password ... -w <密码>`，
  密码会出现在子进程的命令行参数里，同一台机器上的其他进程用 `ps` 就能看到。现在改为：
  `-w` 放末尾触发提示输入、子进程脱离控制终端、密码只从标准输入喂入；
  写入前先用一次性条目自检这条路可用，写入后读回校验。
- **`.sha256` 校验文件不再带 CRLF**。此前在 Windows 上打包时行尾被写成 `\r\n`，
  Linux / macOS 用户执行 `sha256sum -c` 会因行尾的 `\r` 直接报错。

### 需要说明的边界

macOS 上 `security` 的提示输入是否确实读取标准输入（`readpassphrase` 在打不开 `/dev/tty` 时的
退回行为）**尚未在真机验证**。如果不成立，`szunet config set` 会明确报错，而不是退回把密码写进
命令行——这是有意的取舍：宁可失败并让人知道，也不要偷偷暴露。Windows 端不受影响。

本科与研究生成绩、课表的真实账号验收，以及应用内预约仍未完成；学校个人业务请以官方系统为准。

## beta0.7

### 相对 beta0.6.1 的用户可见变化

- 设置页可以开关开机自启，并显示注册表里的真实状态；读不到状态时明说「状态未知」，
  不再显示成「未开启」
- 首次打开会给一张四段短引导（这是什么、数据只在本机、怎么退出、先做什么），可跳过、不会每次都弹
- 成绩、课表、在线读取统一标注「接入测试 · 未经真实验收」；已真实验证的官方校历保持成功色，不混用
- 校园背景压暗并做整图量化，Windows 成品从 11.6MB 降到 9.6MB

### 仓库层面

- 版本号收敛到单一来源 `internal/version/VERSION`，页面顶栏、关于页与打包脚本都从它取
- 删除未核实的旧游戏素材与第三方字体、25 个一次性脚本；VPN 协议笔记移出公开仓库

### 仍未完成

本科 / 研究生真实账号验收、应用内预约衔接与提交、余额与流量数据层、实验 VPN 的 F06 / F07 / F08。

> 这一版发布时 GitHub Release 的正文只有自动生成的一行 compare 链接，
> 以上内容事后按 `docs/STATUS.md` 第 31.4 节补记。

## beta0.6.1

- 学院 / 部门公告筛选：28 个学院与学部入口，其中 17 个栏目可直接读取；其余提供官网入口
- 预约改为只读空位预览 + 学校原页办理入口，界面上撤下预约 Cookie 输入、F12 教程
  和未经真实验收的本地提交表单
- 本次发布不代表课表 / 成绩真实账号验收或应用内预约已完成

> 服务端的预约写端点当时没有一并撤下，已在「未发布」一节中删除（F23）。
