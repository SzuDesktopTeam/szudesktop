# 安全政策

本项目会接触**校园网账号密码**和**学校业务系统的登录 Cookie**，所以安全问题请按下面的方式处理。

## 报告漏洞

**请不要开公开 issue 描述可利用细节。**

- 优先尝试仓库的 [私有漏洞报告入口](https://github.com/SzuDesktopTeam/szudesktop/security/advisories/new)。这需要维护者启用 GitHub Private vulnerability reporting；入口不可用时不要把漏洞细节改发到公开页面。
- 若入口不可用，可在 [Issues](https://github.com/SzuDesktopTeam/szudesktop/issues/new) 仅申请「需要私密安全报告渠道」，等待维护者提供渠道。组织主页没有私信功能，请不要写「已私信」或在公开 issue 中附上凭据、个人数据与可利用细节。

请在报告里写清：受影响的版本或提交、复现步骤、影响范围（本机 / 校园网 / 学校系统）、
以及你是否已经公开过这个细节。

## 本项目对凭据的处理（现状，如实）

登录方式不同，保存方式也不同；不能把「官方窗口登录」与「手动保存 Cookie」混为一谈：

| 入口 | 当前保存范围 | 清除方式 |
|---|---|---|
| 校园网认证 | 未勾选记住时仅本次请求使用；勾选后仅在认证成功时写入系统安全存储 | 设置中删除已保存凭据 |
| 安装版学校官方窗口 | 独立的内存浏览器会话；用户主动读取的教务 Cookie 仅进入本次 Go 进程内存，不进入庭院存档 | 「清除本次学校登录」或完整退出应用 |
| 便携版备用教务登录 | 登录请求使用密码，返回 Cookie 只留在进程内存 | 清除本次登录或完整退出应用 |
| 高级备用入口：手动粘贴并保存 ehall Cookie | 用户点保存后写入系统安全存储，和校园网密码分开 | 清除已保存登录状态；不会注销系统浏览器中的学校会话 |
| 学院琴房备用登录 | 请求使用账号密码，返回 token 仅留在本次进程内存 | 清除琴房登录或完整退出应用 |

**安装版关掉主窗口后会继续常驻，因此不等于清除本次登录。** 完整退出请使用托盘、宠物菜单或设置中的「退出应用」。系统浏览器中的登录状态由浏览器和学校负责，应用退出不会代为注销。

系统安全存储：Windows 使用 DPAPI；macOS 使用系统钥匙串（`security`），写入前有一次性自检；Linux 使用 Secret Service（`secret-tool`）。不可用或写入失败时明确报错，不退回明文文件。当前图形安装版面向 Windows；其他平台的实现主要供命令行版使用。

其他约定：

- 本地服务只监听 `127.0.0.1`，非回环地址直接拒绝启动；所有 `/api/*` 校验 Host、`Sec-Fetch-Site` 与同源 `Origin`。
- 本机没有预约写接口。预约、选课、付款、签到由用户在学校官方系统完成；安装版可以在隔离的学校窗口中与官方页面交互。查到空闲时段不代表预约已提交。
- 公告和校历读取公开页面；场地、成绩、课表及琴房查询按接口要求使用校园网或用户自己的学校会话。所有业务目标固定，不做任意网址代理。真实学校业务的验收边界以 README 和 STATUS 为准。
- 庭院存档导出不含账号密码、Cookie 或琴房 token，但包含用户在应用中保存的课程、任务、专注记录和伙伴名称等个人内容；导出文件不是匿名诊断文件。
- 「复制环境信息」仅复制版本、系统类型和界面模式，复制前界面显示相同内容，不读取或发送学校凭据及存档。向 GitHub 提交反馈由用户自己操作。
- 版本检查只在用户点击时，通过本机服务读取本项目的 GitHub 官方公开发布接口；不附带学校 Cookie、账号或成绩。不自动下载、安装或执行远程代码。发布服务会收到普通网络请求（包括来源 IP）。
- 没有 telemetry、没有埋点。

## 已知未解决的安全相关项

这些都记在 [docs/STATUS.md](docs/STATUS.md)，不要当成已修：

- **F06 / F07 / F08**：实验 VPN 模块存在假成功、缺全链路超时、跳过证书验证的问题。
  该模块**不在任何发布件里**（只有 `-tags campusvpn` 才编译），但问题本身没修。
- **F11**：实验 VPN 模块的来源与授权尚未核实。
- **F21 / F25 / F26**：macOS 保存凭据改成标准输入喂入后，CI 的 macOS 探针在真机上遇到过
  `security -w` 要求输入两遍（密码 + 确认）的情况，而 beta0.7.1 与 beta0.7.2 只喂一行，
  导致**在那些机器上 macOS 命令行版存不了凭据**（F26）。失败是明确的、不泄漏也不落明文，
  但功能不可用。该行为不稳定（同一镜像 macOS 26.6.2 的后续 4 次运行都只问一遍），
  现已改为喂两行、两种情况都成立，**修复自 beta0.7.3 起发布**；探针不再忽略失败，并已成为发布前置条件。
- 宿舍区 Dr.COM 认证走**明文 HTTP**（`http://172.30.255.42`），这是学校协议的现状、不是本项目的选择。
- 学院琴房业务同样使用学校现有的内网 HTTP 接口，其 token 位于业务请求参数中；这条路径不能描述为端到端 HTTPS。它只在用户发起查询时使用，账号密码不落盘，具体内网地址不写入公开指引。

## 不算漏洞的设计决定

以下是有意为之，报告前请先确认：

- 不使用代码签名证书，因此杀毒软件可能误报（README 已如实说明）。
- 不做自动重连、不做后台周期性认证。
- 读不到状态时报「状态未知」而不是「未开启」——宁可说不知道，也不猜。
- 探测网络时（判区、协议指纹）会跳过证书校验，但这些请求**不携带任何凭据**；
  真正带密码的登录请求保留证书校验。
