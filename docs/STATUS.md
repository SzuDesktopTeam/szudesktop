# szuDesktop 项目总清单与当前状态

> **唯一维护文档**：全部问题、功能计划、设计要求、执行顺序与验收记录统一在本文维护。
> 更新：2026-09-27。合并原 STATUS、任务清单、界面重设计、本地任务副本及代码/视觉/产品审查。
> 原独立清单和设计方案已移除；本地原始调研只作为冻结的历史证据，不再维护另一套任务。
> 本文是公开安全版本，不包含真实账号、内网拓扑或未公开接口细节；旧调研中的数量、规则、可达性和许可结论须在实际接入时重核。

## 1. 当前状态

- 2026-09-27 产品审查后的 **beta0.9.3 本地候选版已构建，尚未公开发布**。补齐页面分区、待办编辑归档、专注记录与周回顾、跨日刷新、常驻偏好与提醒、七次回访故事、分享图、版本检查和反馈；调整成长规则，完成选田后集中操作与四伙伴四状态。关键浏览器路径、同版截图、便携包及安装版构建目录的一致性与许可检查已通过；实际安装升级、系统通知、自启、学校业务和真实同学试用仍待验收。UX01–UX26 状态见第 54.9 节，证据与候选文件校验值见第 55 节。
- 新版首页与首次使用引导已在本地源码完成，尚未发布：以文山湖场景、首页伙伴互动和 5 分钟专注组织第一印象；中英文 README 已换宣传结构和真实源码截图。具体改动与检查见第 52 节，公开安装包仍是 beta0.9.2。
- 庭院继续打磨：新增原创湖畔农田场景，重排伙伴小屋、每日目标、纪念章与作物图鉴，奖励反馈读取实际存档变化。源码构建与本机浏览器验收完成，详情见第 53 节；尚未进入公开安装包。
- 已发布 **[beta0.9.2](https://github.com/SzuDesktopTeam/szudesktop/releases/tag/beta0.9.2)**，PR #17 已合并（`b85ef24`），10 个发行附件齐全；安装包与便携 ZIP 实际下载校验一致。跨版本安装、备份恢复与交付证据见第 51 节，beta0.9.1 历史证据保留在第 49 节。
- beta0.9.2 交付荔宝、栗栗、小白（白鹭）、阿青（小龟）四位伙伴及庭院卡片/桌宠菜单同步切换，保留像素校园、菜单照料、滚轮缩放、拖动和位置恢复；旧存档补齐基础伙伴并保留已有成长。
- 成绩按学校总数逐页读取，失败、取消或超时不返回假完整结果；修复接回登录失败仍保留旧账号及学校具体错误被覆盖的问题。真实学校业务尚未因此验收通过。1.0 范围及后端延期决定见第 50.2 节。
- beta0.9.0 整合校历 OCR、桌面宠物与尺寸/动作、学院琴房和学校登录分支，新增安装版学校原页窗口与内存会话接回。PR #10、#11、#12 的代码已由 #13 统一交付，三个旧 PR 已关闭。
- 安装版关主窗口后宠物与托盘继续运行；从宠物菜单「打开主窗口」或托盘重开，完整退出使用宠物菜单、托盘或设置。便携版保持所有窗口关闭约 10 秒退出。
- 真实账号复测已有部分通过：研究生学校原页登录、应用备用登录与当前课表读取成功，能区分无排课和非空已选课程。安装版 WebVPN 认证跳转缺陷已随 beta0.9.1 修复，真实窗口完成二次验证并打开场地列表。本科、成绩、安装版会话接回、完整预约及琴房权限仍未验收通过；见第 47 节。
- 总范围维护在**第 32 节**，1.0 执行与完成状态见**第 50.2 节**；历史阶段记录不代表当前状态。README 中英两版随每次更新同步。
- 保留像素校园、木框与纸张的视觉风格；庭院与学习存档在本机，校园卡号默认隐藏。实验 VPN 不随公开发行版构建，校内共享后端尚未部署。

## 2. 分类方法

| 维度 | 标记 | 含义 |
| --- | --- | --- |
| 重要程度 | P0 | 发布阻断：凭据/安全、未认证报成功、代码许可来源；相关功能对外发布前解决 |
| 重要程度 | P1 | 当前核心必修：正常操作、真实状态、内容可见、数据可靠；功能扩展前完成 |
| 重要程度 | P2 | 近期体验/功能：不阻断基础使用，但提高效率与价值 |
| 重要程度 | P3 | 长期或低频功能，先记录范围 |
| 难度 | S 容易 | 单点或少量文件、依赖明确、可本地回归 |
| 难度 | M 中等 | 多处联动、页面结构或数据层改造，需要跨页验证 |
| 难度 | L 较难 | 协议/平台/认证链路或外部证据依赖，需要专项验证 |

按 **P0/P1 × S → P0/P1 × M/L → P2 × S/M → 其他** 执行。难度不是工期承诺。
“已完成”须有验收证据；“部分完成”不关闭剩余范围；真实验证进展以第 14 节为准：已在学校官方 WebVPN 完成登录并查看场地时段；不等于校园网双协议认证已现场验收。

## 3. 问题与优化总表

### 3.1 功能、安全与工程

| ID | 问题 | 重要 | 难度 | 状态 | 处理与验收 |
| --- | --- | --- | --- | --- | --- |
| F01 | refreshStatus 引用不存在的 opt-zone，阻断后续刷新 | P1 | S | 已完成（本地验证） | 删除失效引用；使用真实 HTML id 执行状态刷新，验证网络/宠物/开关后续更新 |
| F02 | 外网可达但未认证仍报成功，导致保存未验证密码 | P0 | S | 已完成（本地验证） | 自动认证按协议证据判区；无指纹明确失败，不触发页面保存分支 |
| F03 | 联网后区域为 online，桌面及 CLI 自动注销无法选协议 | P1 | S | 已完成（本地验证） | 共享认证区域选择；模拟教学区与宿舍网关，验证实际登录/注销请求 |
| F04 | 本地 API 缺来源校验、写操作方法限制，跨站可触发操作或覆盖凭据 | P0 | S | 已完成（本地验证） | 所有 API 校验回环 Host、Origin/Fetch Metadata、方法；POST 只接受 JSON；拒绝请求不得写入凭据 |
| F05 | VPN 会话 TWFID 被写入日志并经状态接口暴露 | P0 | S | 已完成（本地验证） | 删除会话值日志，只保留流程提示；不代表 F08 全部解决 |
| F06 | VPN 只凭 SOCKS 监听就报连接，RX/TX 失败不传播 | P0 | L | 待做 | 收发隧道成功才报已连接；重试耗尽转失败并清理；已有隔离复现证据 |
| F07 | VPN 登录/握手缺完整超时及取消，可能长时间忙碌 | P1 | M | 待做 | HTTP/TLS/隧道全链路 deadline/context；取消必须停止请求、释放资源 |
| F08 | VPN 跳过证书验证、协议探针可能明文携带会话，错误内容需脱敏 | P0 | L | 待做 | 明确安全连接方案与兼容边界；证书错误可控处理；实际隧道测试；全链路日志审查 |
| F09 | 随机 localhost 端口使 localStorage 存档随源变化，看似丢失 | P1 | M | 已完成（现有发布范围） | 固定本机 JSON、原子替换、跨进程锁、冲突同步与跨端口恢复已验证；无老版本用户，不做迁移助手 |
| F10 | 冒烟按 exe 名杀进程，可能关闭用户另一客户端 | P1 | S | 已完成（本地验证） | 只结束并回收测试自己的子进程 PID |
| F11 | VPN 来源与“独立实现”宣传冲突；MIT 与非商业措辞不一致 | P0 | L | 部分完成 / **仍待来源核验**（保证语句已撤，见第 37.3 节） | 「默认包不编译实验 VPN」已核实为真：`internal/vpn` 只被 `//go:build campusvpn` 的 `desktop/internal/ui/vpn.go` 引用，CI 与 `build-windows.py` 都不传 `-tags`。原先公开写在 `internal/vpn/vpn.go` 包注释里的「代码为自行编写，未复制任何厂商或第三方的源代码」**已删除**，改为如实说明来源与授权尚未核实、不作独立编写保证，并写明 F06/F07/F08 未修。**但来源本身仍未核实**：历史实验源码授权没有确认，F11 不能关闭，相关宣传仍受限 |
| F12 | 外网连通、区域、账号认证状态混用；固定显示已连 SZU_WLAN | P1 | S | 部分完成 | 去掉固定已连接；online 显示区域未确认，账号未确认不写不在线；真实区域/会话查询仍待完善 |
| F13 | VPN 把系统原有代理显示成本应用已打开 | P1 | S | 已完成（本地验证） | 区分 enabled 与 managed；显示其他代理；切换按钮按应用管理状态表达 |
| F14 | 旧记录“在线位置”误填凭据存放方式 | P1 | S | 待复核 | 当前源码已写存放位置，旧错位未重现；与 F12 实机复核后关闭 |
| F15 | 时长/流量/套餐/余额及历史数据层未接入 | P1 | M | 待做 | D3；区分本地测量与官方账单，不能取得的数据继续待接入 |
| F16 | 测试偏接口/字符串，遗漏前端异常、裁切与 VPN 假成功 | P1 | M | 部分完成 | 新增庭院规则、存档并发/损坏保护与真实 exe 冒烟；桌面/窄窗浏览器交互检查通过；实验 VPN 故障闭环仍待做 |
| F17 | 临时凭据与已保存状态不够清楚；HTTP 失败退成演示模式 | P2 | M | 已完成（本地验证） | 默认空白账号、显式查看、留空使用保存凭据；拒绝混合新账号与旧密码；请求失败不退成假演示状态 |
| F18 | 校外 VPN、宿舍 Dr.COM、教学区及教工区缺真实环境验收 | P1 | L | 待外部验证 | 分场景实测；模拟协议服务不能替代真实网络验收 |
| F19 | CI 依赖升级及 Node 20 弃用告警待核对 | P2 | S | 已完成（PR CI 验证） | 查各 action 实际最新标签后升级：checkout v7.0.1 / setup-go v7.0.0 / setup-python v7.0.0 / upload-artifact v7.0.1 / download-artifact v8.0.1 / action-gh-release v3.0.3；不能只凭旧警告盲改 |
| F20 | 出口已被占用时报成成功、能上网时反而跳过在线查询 | P1 | S | 已完成（本地验证） | 2026-09-20 真实网络复现：深澜 `ip_already_online` 是 IP 级短路，账号密码与 `ac_id` 都未被校验，不能算成功、更不能缓存 `ac_id`；能上外网时也必须在统计在线状态，否则用户只看到"没查到"而误判掉线 |
| F21 | macOS 保存凭据把密码 JSON 当命令行参数传给 `security -w`，本机其他用户或进程 `ps` 可见 | P0 | M | 已修复（待 macOS 真机验收，见第 33 节） | 改为 `-w` 放末尾触发提示输入 + 子进程脱离控制终端 + 密码经标准输入喂入；写前用一次性条目自检、写后读回校验，自检不过就明确报错，不退回 argv 写法 |
| F22 | CLI `status` 在没有保存账号时跳过在线查询，只报「没查到」 | P1 | S | 已完成（真实网络验证，见第 35 节） | 原先写的是 `if credErr == nil { 查 }`，而门户查询按出口 IP 回答、与账号无关；同一时刻桌面端报「已在线」、CLI 报「没查到」。去掉短路，与桌面端共用同一查询入口，并补 `online_known` 与登录提示 |
| F23 | 预约的**服务端写操作端点仍在发布包里活着**，而 README 与本文都写着「已撤下本地提交表单」「不代提交」 | P0 | S | 已完成（真机成品验证，见第 37.1 节） | 删掉 `/api/booking/{session,history,prepare,commit}` 四个端点及其专属代码（`bookingInput`/`bookingPending`/`bookingRecord`/`bookingHistory`/`validateBooking`/`history()`），`request()` 收敛成只发 GET、不再接受任何写载荷，`bookingSecure` 与 Cookie 字段一并移除。只读的 rooms / availability 保留。验收：新增 404 断言测试（先看它红）、前端回归、74 项冒烟、以及**真实成品**上 12 种方法组合全部 404 且 rooms 仍返回真实场地 |
| F24 | Linux 上校园网密码可能**明文落盘且静默报成功**，文档只承诺 DPAPI/钥匙串 | P1 | S | 已完成（本机测试 + Linux 编译验证，见第 37.2 节） | 范围比复核时判断的更大：Windows 在拿不到用户目录时也会写明文相对路径。做法是删掉 `fileStore`，新增 `unavailableStore` 与 `ErrStorageUnavailable`——没有系统密钥环就明确报错、绝不落明文，与会话存储同一标准；`Delete()` 仍会清掉老版本可能留下的明文文件。两份 README 与排查指南已补实情 |
| F25 | macOS 凭据路径**没有任何自动化覆盖**：CI 只在 ubuntu 跑测试，darwin 仅交叉编译不执行 | P1 | M | 已完成（第 37.4 / 39.3 / 40 节） | `test-macos` job 已加入且成为 release 前置条件，含对真实 `security` 命令的 stdin 探针。首跑即抓出 F26；现已稳定 |
| F27 | 命令行版开机自启登记的是 `szunet login --auto`，而 `login` 没有 `--auto`；flag 集为 `ExitOnError`，未知开关直接 `os.Exit(2)`——**开过这个开关的用户开机时静默失效，无任何报错** | P1 | S | 已修复（外部 PR #5，见第 42.3 节） | 登记参数改为 `login`（本身非交互），抽成 `CLILoginArgs()`；给 `login` 保留 `--auto` 兼容开关，让已写进注册表的旧启动项不必手工改。新增 `autostart_args_windows_test.go` 用 `ContinueOnError` 起同配置 flag 集解析登记参数，把这类不匹配拦在测试阶段 |
| F28 | `booking.mjs` 的 `openHours()` 用 `BigInt(m||0)`，学校返回带千分位字符串 / 小数 / 超安全整数时抛异常；它在 `services()` 里被无条件求值并整段赋给 `#main`，**异常即整页静默白板、无任何提示** | P1 | S | 已修复（外部 PR #5，见第 42.2 节） | `openHours` 与 `ruleNum` 一律不抛，不可信输入降级成「—」；`ruleNum` 用 `isSafeInteger` 同时挡小数 / `Infinity` / `NaN` / 超安全整数；0 也显示「—」（服务端会把越界值收敛成 0，分不清真 0 与自清零）；分组键从 `name` 改 `typeId`；`services()` 每张卡片各自 try/catch，`render()` 由 `pageHTML()` 兜底 |
| F26 | macOS 上 `security -w` **可能要求输入两遍**（密码 + 确认），我们只喂一行时就得到 `passwords don't match`、退出码 44，条目根本建不起来——那种机器上**凭据完全存不进去** | P0 | S | 已修复（CI 真机探针 4 次验证，见第 39.4 节）；beta0.7.1 与 beta0.7.2 **都带着这个缺陷发出去了** | 该行为不稳定：同一镜像 macOS 26.6.2 的后续 4 次运行都记录为「只问一遍」，所以只影响会要求确认的机器。F21 改成 stdin 喂入时假设只问一遍；`secretInput` 现在喂「密码 + 确认」两行，只问一遍时多出的那行是没人读的剩余输入（已真机验证）。两个测试替身都改成如实模拟两次提问，因此本地与 Linux CI 也守得住；CI 探针去掉 `continue-on-error` 并进入 release 的 `needs` |
| F29 | CLI `status --json` 把「没探过」的门户状态当成「探不到」发布：`cmdStatus` 直接消费 `portal.Detect()`，而它在「能上外网」时会提前返回，`DormPortalOK` / `TeachPortalOK` 只是零值 `false`，JSON 里也没有 `probed` 键 | P2 | S | 已修复（本机实测复现 + 2 项新回归；未发布） | 抽出纯函数 `statusReport()`：**始终发 `probed`**，只在 `det.Probed` 为真时才发 `dorm_portal_ok` / `teaching_portal`——缺键＝没测量，`false`＝测了但不通，两者不能混为一谈。2026-09-23 实测：同一台机器、同一时刻，改前 `status --json` 报 `dorm_portal_ok:false` / `teaching_portal:false` 且无 `probed` 键，而同机 `detect --json` 这两个键报 `true`（同一件事给出相反结论）；改后 `status` 发 `probed:false` 且不再发那两个键，`detect` 输出不变。新增 `cmd/szunet/status_report_test.go` 两项回归：改前第一项红、加门控后两项绿 |

| F27 | 「用命令行版开机自启」登记的 `szunet login --auto` 里，`--auto` 这个参数**从来不存在**；`login` 的 flag 集是 `ExitOnError`，遇到未知参数会直接 `os.Exit(2)`，于是开机时程序什么都没做就退出了——一个没有任何报错的静默失效开关 | P1 | S | 已修复（本机分析 + 新增回归；真机开机流程未验收，见第 41 节） | 注册参数改为 `login`（`login` 本来就是非交互的：按「命令行参数 > 环境变量 > 已保存凭据」取账号，失败只体现在退出码上），并抽成 `cliLoginArgs` 常量，避免三处各写各的；另外给 `login` 补上 `--auto` 兼容开关，让**已经写进用户注册表**的旧启动项不必改注册表就能恢复。新增 `cmd/szunet` 的 Windows 测试：用 `ContinueOnError` 起一个与 `login` 同配置的 flag 集去解析登记的参数，把「注册了 `login` 不认的参数」拦在测试阶段 |

### 3.2 排版、外观与交互

本轮审查基线：1366×768 顶部约 258px，概览主体约 269px；网络任务卡正文需约 344px，实际只有 77px。1440×900 仍有字体/标签/层级问题。
旧走查“页面超出视口 130–662px”属于此前布局；当前重点是隐藏溢出的静默裁切，锁住 body 并不等于完成一屏。

| ID | 问题 | 重要 | 难度 | 状态 | 处理与验收 |
| --- | --- | --- | --- | --- | --- |
| U01 | 顶部场景与底部组件挤压内容，卡片和登录按钮被裁切 | P1 | M | 已完成（本地验证） | 取消锁高与隐藏溢出，正文自然滚动；导航滚动时保留；1366×768 / 1440×900 / 390×844 检查 |
| U02 | 隐藏所有滚动条，溢出不可发现；小窗回退与一屏策略矛盾 | P1 | M | 已完成（本地验证） | 可见浏览器滚动条；窄窗实测 scrollY 722 + viewport 844 = document 1566，完整到达页脚 |
| U03 | 登录/VPN 标签及常态说明奶油色落在浅底上，难以辨认 | P1 | S | 已完成（本地验证） | 标签深墨色、14px，说明深色；检查两页实际渲染 |
| U04 | 中文过小、中英比例失衡，标题/正文/数字缺层级 | P1 | M | 已完成（本地验证） | 恢复标题/菜单/按钮的 Fusion Pixel 字体；长说明/输入/正文使用清晰系统中文，正文 14px、辅助 12–13px；不再使用全页 12px 细字 |
| U05 | 切页内容宽度跳变；导航、内容、对话框对齐线不统一 | P1 | M | 已完成（本地验证） | 主导航与 1160px 内容框共用对齐线；内页两栏、小窗一栏 |
| U06 | 纹理/厚框/阴影/扫描线叠加，主操作和状态不突出 | P2 | M | 已完成（本地验证） | 恢复暖棕木牌、奶油纸张、橙金按钮与木框；纹理限于装饰，移除全屏扫描线；顶部缩减占位，内容继续自然滚动 |
| U07 | 导航混合功能、表现形式和游戏概念，入口多层重复 | P1 | M | 已完成（本地验证） | 统一今日/校园网/校园服务/荔枝庭院/学习工具/设置；庭院四分区避免相同入口重复 |
| U08 | VPN 普通填写项与端口等高级设置同级 | P1 | M | 默认版不适用 | 初始包使用官方 WebVPN；实验连接设置暂不向普通用户展示 |
| U09 | 素材像素颗粒、缩放、边框、阴影和图标尺寸不一致 | P2 | M | 部分完成 | 图标尺寸与木框规范统一；旧游戏光栅素材不打包；恢复项目自绘花草，原 emoji 装饰已替换为像素花草/CSS 灯笼 |
| U10 | 常驻动效过多，装饰颜色稀释成功/失败语义 | P2 | S | 已完成（本地验证） | 只保留伙伴轻微动画；遵循 reduced-motion 并可在设置关闭 |
| U11 | 假余额、课表、闭馆通知及旧日志误导行动 | P1 | S | 已完成（本地验证） | 余额待接入；移除编造课表/通知/日志，明确未接入 |
| U12 | 收获条、固定水果数与空工具槽占空间，演示标注不就近 | P2 | S | 已完成（本地验证） | 移除占空间的底部水果条、空槽和虚假库存；庭院资源来自真实本机存档 |
| U13 | 一键登录实为跳页；记住被写成开机自动登录；名片说明不符 | P1 | S | 已完成（本地验证） | 改前往登录、认证成功保存与编辑个人资料 |
| U14 | 仿窗口按钮与实际最小化/最大化能力不符 | P2 | M | 已完成（本地验证） | 删除仿系统标题栏及伪最小化/最大化按钮；保留浏览器原生标题栏，设置内完整退出 |
| U15 | 缺首次使用引导与统一空/错误/未接入状态 | P2 | M | 已完成（初版引导，见第 29 节） | 默认欢迎与首块萝卜、空待办、无账号、加载失败、请求错误、备份确认均有说明；首次启动另有四段短引导，可跳过且不重复弹 |
| U16 | 研究生「教务登录」与「我的课表」挤在一张卡里，本科课表还被嵌在研究生登录卡内 | P2 | S | 已完成（真机界面核验） | 拆成两张卡：登录卡只放学号/密码/验证码/清除登录，课表卡放研究生读取与展示 + 本科入口与说明；未登录时读取按钮置灰并给出前置说明（`#school-timetable-hint`），`paint()` 同步该提示。新增 `check-school.mjs` 1 项回归（两张卡各自只含自己的关注点） |

设计建议：保留校园意象、荔枝角色、温暖配色与局部像素插画；重做骨架/字体/层级；收起全屏扫描线、多重厚边框、空槽与重复标识。建议尚未全量实施。
参考官方截图：[Clash Verge Rev](https://github.com/clash-verge-rev/clash-verge-rev) 的固定导航和层级；[福大助手](https://github.com/west2-online/fzuhelper-app) 的服务网格与局部校园图像。移动端布局不能直接照搬。
旧设计参考 NES.css、98.css、system.css、XP.css 保留为组件/窗口结构参考，不叠加另一套 CSS，不沿用未经复核的 star/许可数字。

## 4. 原功能任务（完整保留 A–E 编号）

A 静态/本地，B 拟接公开数据，C 用户授权业务，D 基础能力，E 长期规划。
C 类须验证各系统会话；D1/D2 不是“一次完成全部解锁”。预约/付款/选课/签到最终操作由用户在官方系统完成。

| ID | 功能 | 重要 | 难度 | 状态 | 范围 |
| --- | --- | --- | --- | --- | --- |
| A1 | 自习地图 | P2 | M | 待做 | 自习点地图：开放时间、座位、插座、噪音；历史地点与时段发布前核对 |
| A2 | 校历 | P2 | S | 部分完成 | 学习工具支持手动设第 1 周起始日并计算教学周；学校正式假期/考试校历尚未接入 |
| A3 | 常用电话簿 | P2 | S | 部分完成 | 已收录图书馆三个咨询号码与邮箱（来源：图书馆官网公开页脚，可点击拨号）；教务部/研究生院/办事大厅/WebVPN 只给官方入口。**未查到官方公开号码的部门一律不写数字**，宁可少一条也不放编造的号；校验规则已进回归测试 |
| A4 | GPA / 绩点计算器 | P2 | S | 部分完成 | 本科/研究生成绩表批量导入、预览去重、学期筛选与持久化；本科等级按官方规则换算，在线自动同步未做 |
| A5 | 学分进度 | P2 | M | 待做 | 对着培养方案看还差什么 |
| A6 | 智算中心入口聚合 | P3 | S | 部分完成 | 校园服务已列 AI 云官方控制台入口；文档/数据集等完整聚合仍待核对 |
| A7 | 智算上手引导 | P3 | S | 待做 | 官方申请指南是空的，我们自己写一份大白话版 |
| A8 | 校园系统地图 | P2 | M | 部分完成 | 9 个可搜索官方入口按常用/教与学/资讯分组；20+ 系统的完整导航仍待补齐 |
| A9 | 图书馆座位规则速查 | P2 | S | 部分完成 | 社区场地的爽约黑名单规则已随「场地与琴房规则速查」上线（1 天内不可再约，取自学校 `blacklistValidDuration`）；**图书馆**自己的暂离时限与黑名单规则仍未核实来源 |
| A10 | 缴费路径指引 | P2 | S | 待做 | 网费 / 电费 / 学杂费分别在哪交 |
| A11 | 算力资源估算器 | P3 | M | 待做 | "训这个模型要几卡几小时" |
| B1 | 场地浏览 | P2 | M | 部分完成 | 社区场地 15 个已可按类型 + 社区分组浏览（含设备说明、校区、社区、停用标记）；**不含照片**——学校只给裸文件名，`coverImage` / `images` 拼不出可访问 URL（试过 6 个常见前缀全 404），要做得先逆推其 SPA |
| B2 | 琴房浏览 | P2 | M | 部分完成 | 共享琴房 3 间已含在上述分组里，设备说明（钢琴/座椅/风扇）来自学校 `description`；同样无照片 |
| B3 | 场地类型与规则 | P2 | M | 部分完成 | 4 类场地（共享琴房 3 / 网络面试间 6 / 心灵小屋 2 / 共享会议室 4）的类型名、单日上限、可提前天数、每日开放时段（掩码折算小时）、爽约黑名单天数**均已从学校接口取出并显示**。B1 原述那批活动场地（凯风餐厅前空地等）属另一套系统，未接 |
| B4 | 琴房规则 | P2 | M | 部分完成 | 学校四类场地的使用须知是**同一份文本**（返回内容 sha 相同），已按「学校统一使用须知」显示一次，不按场地重复。`roles` 返回 null、`guidelines` 为空，「谁有资格预约」目前拿不到 |
| B5 | 审批流程 | P2 | M | 待做 | 7 条 |
| B6 | 事务中心公告 | P2 | M | 待做 | 接入前确认数据来源和使用范围 |
| B7 | 校区列表 | P2 | M | 待做 | 粤海 / 丽湖 |
| B8 | 校园笔记 / 社区 | P3 | M | 待做 | 接入前确认数据来源和使用范围 |
| B9 | 失物招领 | P2 | M | 待做 | 接入前确认数据来源和使用范围 |
| B10 | 校园地图地点 | P2 | M | 待做 | 接入前确认数据来源和使用范围 |
| B11 | 抓公开页：公告聚合 | P2 | M | 部分完成 | 当前源码新增按发布院系选择：28 个院系入口，17 个学院栏目可直接读取，加上原有教务部和研究生院；保留官网入口、日期和缓存提示。已随 beta0.6.1 发布，公文通等需登录公告仍未接入，见第 27 节 |
| B12 | 智算中心通知订阅 | P2 | M | 待做 | 接入前确认数据来源和使用范围 |
| B13 | 讲座日历 | P2 | M | 待做 | 接入前确认数据来源和使用范围 |
| B14 | 校园网质量历史曲线 | P2 | M | 待做 | 把每次 `detect` 结果存本地，画时间线 |
| C1 | 课表 | P2 | L | 待做 | 事务服务中心 或 办事大厅「我的课表」 |
| C2 | 成绩查询 | P1 | L | 部分完成 | 两类官方成绩表可本地导入；个人成绩在线读取仍待稳定认证与接口验证 |
| C3 | 绩点 | P1 | M | 本地计算完成 | 官方绩点优先，本科等级按已核实规则换算；研究生不推测换算，支持是否计入与学期筛选 |
| C4 | 培养方案 | P3 | L | 待做 | 办事大厅 |
| C5 | 考试安排 | P2 | L | 待做 | 办事大厅 |
| C6 | 网上评教 | P3 | L | 待做 | 办事大厅 |
| C7 | 本科选课 | P3 | L | 待做 | 本科生选课系统（独立站点） |
| C8 | 场地预约申请 | P3 | L | 待做 | 事务服务中心 |
| C9 | 琴房预约 | P3 | L | 待做 | 事务服务中心 |
| C10 | 静音仓预约 | P3 | L | 待做 | 事务服务中心 |
| C11 | 研讨间预约 | P3 | L | 待做 | 图书馆研讨间预约系统 |
| C12 | 体育场馆预约 | P3 | L | 待做 | 办事大厅 或 内部网 |
| C13 | 摊位预约 | P3 | L | 待做 | 事务服务中心 |
| C14 | 导师预约 | P3 | L | 待做 | 事务服务中心（「荔园辅学」） |
| C15 | 假期留宿申请 | P3 | L | 待做 | 事务服务中心 |
| C16 | 海报申请 | P3 | L | 待做 | 事务服务中心 |
| C17 | 宿舍电费查询 | P2 | L | 待做 | 事务服务中心 |
| C18 | 教师信息查询 | P3 | L | 待做 | 事务服务中心 |
| C19 | 知识库 | P3 | L | 待做 | 事务服务中心 |
| C20 | 一卡通余额 / 流水 | P2 | L | 待做 | 校园卡电子服务平台 |
| C21 | 图书馆借阅 / 到期 | P2 | L | 待做 | 图书馆主页 |
| C22 | 勤工助学 | P3 | L | 待做 | 事务服务中心 或 学工系统 |
| C23 | 奖学金申请 | P3 | L | 待做 | 学工系统 |
| C24 | 邮箱未读提醒 | P2 | M | 待做 | 候选方案为官方 IMAP；服务器地址、客户端权限、授权方式和未读同步尚未实测，不能称只需授权码 |
| C25 | 校园网费续费 | P2 | S | 待做 | 内部网 → 校园网络 → DR.COM 自助服务 |
| C26 | 电费充值 | P2 | S | 待做 | 微信小程序「深圳大学校园码」⚠️ 只能做指引 |
| C27 | 图书馆阅览座位 | P1 | L | 官方规则已核实 | 与社区静音舱分开；微信服务号绑定卡号、入馆扫码后按规则选座，不伪造现场签到 |
| C28 | 社区静音舱 / 学习场地预约 | P0 | L | 官方网页流程已验证，应用内接口待做 | 用户当前最高优先级；已登录查看场地与半小时时段，未提交预约；本机提醒/日历导出完成 |
| D1 | CAS 会话打通 | P2 | L | 待做 | 统一身份认证会话与过期检测；各系统逐一验证，不保证所有 C 类共享会话。**与 D2 / R04 合称「套壳登录」，完成度 0%，见 §32.2 首条** |
| D2 | 认证态 WebView | P2 | L | 待做 | 用户授权后复用登录态打开官方页；核对跨域、Cookie 和嵌入限制。**已随 §15.1 降级为「多一种获取方式」；与 D1 / R04 合称「套壳登录」，完成度 0%，见 §32.2 首条** |
| D3 | 本机数据层 | P1 | M | 部分完成 | 庭院/个人资料/待办/绩点本机稳定数据层已实现；学校业务缓存/账单/提醒基线仍未接入 |
| D4 | WebVPN 自动切换 | P2 | L | 待做 | 校外时自动走 VPN 访问校内系统；校内系统在校外可用 |
| D5 | 白名单转发 | P1 | M | 待做 | 新目标必须进白名单；安全底线 |
| E1 | Android / iOS 端 | P3 | L | 待做 | 界面可复用部分；VPN、系统代理、凭据存储及应用分发仍需平台适配 |
| E2 | 响应式网页 | P3 | L | 待做 | 手机浏览器直接能用（比出 App 快） |
| E3 | 今日看板 | P2 | M | 待做 | 打开就看到：下节课倒计时 / 卡余额 / 待办 / 校园网状态 |
| E4 | 主动提醒 | P3 | L | 待做 | 卡余额低、邮件来了、借书到期、考试临近 → 飞书推送 |
| E5 | 大文件内网分发 | P3 | L | 待做 | 数据集 / 模型权重在校内网分发 |

### 4.1 原设计和发布收尾补充项

| ID | 任务 | 重要 | 难度 | 状态 | 范围/依赖 |
| --- | --- | --- | --- | --- | --- |
| X01 | 首次使用引导 | P2 | M | 已完成（初版，见第 29 节） | U15 统一验收 |
| X02 | 桌面开机自启 | P2 | M | 已完成（见第 29 节） | 注册表逻辑抽到 `internal/autostart`，命令行与桌面共用；设置页显示真实状态并可开关 |
| X03 | 深色模式 | P3 | M | 部分完成 | 暖阳/暮色背景模式可选；暮色不是全套深色阅读主题 |
| X04 | 窗口真实控制 | P2 | M | 已完成（移除伪能力） | 沿用系统标题栏控制窗口；提供本地服务退出按钮，不自制虚假窗口按钮 |
| X05 | 构建自动注入版本号 | P2 | S | 已完成（见第 29 节） | 唯一来源 `internal/version/VERSION`；两个 main.go 经 go:embed 读取，页面从 `/api/status` 取，构建与打包脚本读同一文件。README / 发布说明的版本文字仍在发版时手工更新 |
| X06 | macOS / Linux 桌面端 | P3 | L | 待做 | Windows 优先，区分现有 CLI 支持 |
| X07 | 天气/穿衣建议 | P3 | S | 待做 | 现有为演示；真实数据来源和更新时间 |
| X08 | LMS 作业/实验报告截止 | P3 | L | 待做 | 独立会话及数据源；汇入 E4 |
| X09 | 选课冲突/名额提醒/绩点预估 | P3 | L | 待做 | C7 扩展，不自动抢课或代提交 |
| X10 | 成绩单/学籍证明申请入口 | P3 | M | 待做 | 用户在官方页面提交 |
| X11 | 文献访问/下载助手 | P3 | L | 待做 | 用户已有权限，WebVPN/图书馆入口，不绕过限制 |
| X12 | 宿舍/教室报修 | P3 | M | 待做 | 先入口指引，再只读状态 |
| X13 | 就业/实习资讯 | P3 | M | 待做 | 公开来源与更新时效 |
| X14 | 报销/差旅入口 | P3 | M | 待做 | 官方页面直达 |
| X15 | 番茄钟/专注模式 | P3 | S | 已完成（本地验证） | 5/25/45 分钟、跨刷新计时、取消确认、结束后一次性奖励与累计记录 |
| X16 | 多设备同步 | P3 | L | 待做 | F09/D3 后；隐私、账号、同步冲突单独设计 |
| X17 | 荔宝养成完善 | P3 | M | 已完成（初版范围） | 伙伴照料/成长/休息/命名/装饰/目标/图鉴，与种植和专注闭环；完整规则见第 8 节 |
| X18 | 空闲教室查询 | P2 | M | 待做 | 与 A1 结合，区分地图资料与实时空闲 |
| X19 | 智算申请直达 | P3 | M | 待做 | A6/A7 后续，用户自行申请 |
| X20 | 成绩/邮件/借阅/考试/缴费提醒 | P2 | L | 待做 | E4 统一汇总，用户选择通知渠道，不默认向第三方发送 |

### 4.2 历史完成记录

- beta0.3 起：双协议认证、接入点发现与缓存、诊断；VPN 有实现，仍需 F06/F08/F11/F18。
- beta0.4：按时段问候、名片关联欢迎语、默认“新同学”；标题与关于页版本接通。
- 概览/校园网/设置已有二级菜单；移除了三栏内滚/翻页器，但一屏验收尚未完成。
- 高级接入点选项默认折叠，相关错误才展开；名片套餐/余额/时长/流量已使用待接入。
- 工具栏认证入口可跳登录页，占位设置说明做过诚实化；关闭按钮尝试关窗，最小化/最大化仍为提示。
- 已有 Windows 构建、资源同步、冒烟、截图/几何走查工具；后续补 F16 覆盖。

## 5. 统一执行顺序与设计验收（更新到 beta0.5）

1. **本地初始版**：滚动/隐私/标题栏/CMD、稳定存档、伙伴农田、学习工具、打包与回归已落地；第 10 节记录边界。
2. **对外认证验收**：教学区与宿舍真实认证、注销、路由器场景；不能以模拟网关代替现场验证。数字签名与用户反馈渠道后续评估。
3. **校内后端第一期**：服务目录、公共公告、官方校历、版本清单，优先 B 类只读公共信息；见第 9 节。
4. **学校个人业务**：明确接口和用户授权后再接课表、成绩、借阅/卡余额；不集中托管校园密码。
5. **实验模块**：F06/F07/F08/F11/F18 单独解决后才考虑 VPN 发行，不纳入当前包的完成范围。

用户最新反馈以“内容能看到、能滚动”为优先。取消此前强制一屏不滚动的取舍，避免为追求一屏而截断内容。正文采用自然滚动，导航保持可达，小窗单列；不把所有功能堆进首页。

## 6. 开发与发布约定

- 页面壳源文件 `desktop/index.html`，行为/样式/规则位于 `desktop/assets/garden/`；`desktop/assets/index.html`、`desktop/internal/ui/assets/` 为生成副本，勿手改。HTML id 修改须检查 JS 引用；不手改 embed 副本。默认构建包含 index.html、图标、garden 目录，以及允许列表内的 Fusion 字体、CSS 和 OFL 声明；不复制整个旧 fonts/art 目录。
- 不再锁死页面高度；滚动验证要到达真实底部，不能用 overflow:hidden 隐藏失败。
- 外网可达不等于已认证；Detect 的 Probed 区分未探测/失败，认证操作用完整 Probe 的协议证据。
- 接入点编号不写死：手动 > 按网口缓存 > 可信跳转 > 猜测；探测不使用门户自身制造默认编号。深澜错误须读取 Error/ErrorMsg/SucMsg/Res。
- 普通用户默认不见 ac_id，相关错误才展开；不要给用户暴露源码维护要求。
- 测试必须用 SZUNET_CONFIG_DIR 隔离，不使用真实账号，只终止自己启动的 PID，不调整用户现有代理。
- 验证：node desktop/check-ui.mjs、go test ./...、go vet ./...、gofmt、python desktop/build-windows.py、python desktop/smoke_windows.py；界面另做实际浏览器检查。
- 业务聚合使用允许目标，不新增任意转发；不绕认证，不代交付款/选课/预约/签到，不新增保存官方业务密码。
- 代码/素材先核实来源及版本许可，不能把未核实实现宣传成独立实现；F11 未关闭前相关发布受阻。
- 不将假数字当真实数据。规划和状态只改本文，历史资料冻结，不再同步多套清单。
- 仓库公开，不迁入真实凭据、内网拓扑和未公开接口；原始调研保留本地作为证据。
- 标签触发发布 CI；版本号只有一个来源 `internal/version/VERSION`：两个 main.go 通过 `internal/version` 包读取（go:embed 编进二进制，不依赖 ldflags），页面顶栏与关于页从 `/api/status` 的 `app_version` 取值，构建与打包脚本读同一个文件。页面源码里不允许再写死版本号，`check-ui.mjs` 与 `smoke_windows.py` 各有回归守住这一点。README / 发布说明中的版本文字仍在发版时手工更新。历史阶段仅授权 PR；2026-09-19 用户已明确要求推送安装包，授权合并已通过检查的修复并发布 beta0.5.1。

## 7. 第一轮验证记录（2026-09-18，beta0.4 基线，保留历史）

- 已修改：失效 DOM 引用、认证/注销选择、同源/方法/JSON 防护、会话日志、测试进程清理、表单对比度、动态假数据、跳转与保存文案及代理归属。
- 新增回归：真实 HTML id 状态刷新；外网无指纹拒绝成功；在线教学区/宿舍区自动登录注销请求；CLI 区域选择；跨站/其他本地端口/伪 Host/错误类型不改凭据，写接口拒绝 GET。
- node desktop/check-ui.mjs：通过。
- go test ./...：通过；go vet ./...：通过；新增 Go 文件已格式化。
- Windows 构建：通过，主 HTML 与两个生成副本同步；冒烟测试全部通过（隔离测试凭据，无真实登录）。
- 浏览器核验：已用构建后客户端在 1366×768 检查概览、校园网账号页、VPN 和动态页；标签实际为深墨色 rgb(74,44,18)、14px，动态占位与其他代理归属显示正确，浏览器无 error 日志。U01/U02 的内容裁切仍存在，未标完成。
- 未测试真实账号认证、校外隧道、真实自动注销；未做许可证事实核验；全站布局仍待 U01 等。

## 8. beta0.5 伙伴与农田完整初版

**参照与取舍**：参考 [Habitica 官方介绍](https://habitica.com/static/home) 的任务→成长联系、[Farmhand 原项目](https://github.com/jeremyckahn/farmhand) 的种植→收获→经营循环。只参考机制，自行编写规则，不复制其代码或素材。场景背景为本轮 AI 生成，非真实校园地图；保留项目 SVG 吉祥物与猫咪。

### 8.1 伙伴

- 属性：饱食、精力、心情、亲密度、成长值/等级。摸摸 +2 成长、10 秒冷却；玩耍 +5 成长、消耗 10 精力、30 秒冷却；喂食 +30 饱食、+8 成长。
- 离线结算：饱食每小时 -2、心情 -1；醒着精力 -1，睡眠每小时 +30。单次衰减按最多 48 小时算，下限 15/20，伙伴不死亡；作物成熟后不枯萎。
- 每 50 成长升一级，上限 20。Lv.2 解锁蓝莓，Lv.3 解锁荔枝；改名、3 种永久装饰购买/收起/摆放。
- 普通食物 8 币一份；农田收成可直接喂食。食物紧缺有每日补给，不做付费等待。

### 8.2 农田经济

| 作物 | 生长时间 | 种子价格 | 每次产量 | 单个售价 | 解锁等级 |
| --- | --- | --- | --- | --- | --- |
| 小萝卜 | 1 分钟 | 4 | 2 | 6 | 1 |
| 草莓 | 5 分钟 | 12 | 2 | 12 | 1 |
| 蓝莓 | 20 分钟 | 24 | 2 | 23 | 2 |
| 荔枝 | 60 分钟 | 40 | 3 | 38 | 3 |

开局 40 币、3 份食物、4 颗萝卜/2 颗草莓种子，3 块可用土地，其中 1 块预种萝卜；其余 3 块分别花 60/90/120 币开垦。每块作物可浇水一次，剩余时长缩短 25%。收获 +10 成长值。循环为种植→浇水→离线生长→收获→出售/喂食→补种/装饰/扩地。币只代表本机游戏资源，不充值、不提现、不联网交易。

### 8.3 长期目标与学习联动

- 每日补给 20 币、1 份食物、2 颗萝卜种子；每日照料/种植/收获/专注目标，各可领取一次。
- 6 个纪念章、4 种作物图鉴、最近 30 条庭院事件；奖励领取防重复。
- 完成 5/25/45 分钟专注获等量币及 10 成长；结束时手动领取一次，不宣称监测了真实学习。
- 每条待办第一次完成给 2 成长，反复勾选不能重复领取。同日时间回拨不重复刷新补给。
- 本地单机存档可被用户编辑，不做防作弊承诺。未来跨设备/排行必须改用服务器验证规则，不能直接信任这些本机数据。

### 8.4 本轮新增项分类

| ID | 项目 | 重要 | 难度 | 状态 |
| --- | --- | --- | --- | --- |
| R01 | 卡号默认隐藏，主动查看；禁止新账号混用旧密码 | P1 | S | 已完成 |
| R02 | 页面滚动与单标题栏 | P1 | M | 已完成 |
| R03 | Windows GUI 构建、隐藏路由探测子进程、设置内退出 | P1 | S | 已完成 |
| R04 | 稳定存档、原子替换、并发保护、损坏时不覆盖 | P1 | M | 已完成 |
| R05 | 原创校园背景、导航/字体/留白统一 | P1 | M | 已完成 |
| R06 | 伙伴与农田经营闭环、每日目标/成就/装饰 | P1 | M | 已完成（上述初版范围） |
| R07 | 待办/专注/绩点/教学周/备份恢复 | P2 | S–M | 已完成（手动数据范围） |
| R08 | 公告与目录后端、来源/更新时间/缓存 | P2 | M | 方案已完成，部署待做 |
| R09 | 学校个人业务/多设备同步 | P2 | L | 等接口与授权边界 |
| R10 | 旧版浏览器存档迁移助手 | — | — | 已取消：用户确认没有老版本用户 |

## 9. 校内服务器能承担哪些业务

可以将公共业务放在校内服务器，桌面本机服务继续负责账号凭据和网络认证。校内托管本身不会自动获得教务等系统的 API 权限，也不意味着校外用户可以直接访问该服务器。

| 业务 | 是否需要后端 | 优先/难度 | 建议方案与边界 |
| --- | --- | --- | --- |
| 官方服务目录、常用入口、应用版本清单 | 推荐 | P2 / S | 可先用静态 JSON + HTTPS；每项有来源、校区、更新时间，离线回退本地目录 |
| 学校公告、讲座、办事通知聚合 | 需要 | P2 / M | 定时抓取允许访问的公开来源，去重、缓存、搜索；保留原文链接与抓取时间，不复制整篇文章冒充原创 |
| 官方校历/考试周/公共地点资料 | 推荐 | P2 / S–M | 后台统一录入核实数据，客户端缓存；个人教学周仍能本地手动设定 |
| 公告关键词订阅、提醒收件箱 | 需要 | P2 / M | 用户选择订阅范围；首期应用内提醒，邮件/外部推送需单独选择，不默认发消息 |
| 跨设备庭院/待办同步 | 需要 | P3 / L | 自愿注册或授权登录，存档版本、冲突副本、删除/导出、备份与速率限制；不上传校园密码 |
| 反馈与崩溃上报 | 可选 | P2 / M | 用户主动提交，脱敏后预览；日志不含账号密码和会话 token，不默认采集 |
| 排行、访客、多人庭院、交易 | 需要 | P3 / L | 服务器校验经济规则与身份、反滥用和内容管理；初版不做 |
| 课表/成绩/余额/借阅 | 视接入方式 | P2 / L | 优先官方接口和明确授权；个人会话本地持有或可撤销短期票据，不替用户长期保管统一认证密码 |
| 校园网登录/注销、网络诊断 | 不应迁移 | P1 / M | 与使用者网卡/出口相关，留在本机执行；后端不能代表用户的网口完成认证 |
| 单机伙伴、种地、专注、绩点、备份 | 不需要 | 已完成 | 本机离线工作，避免服务器故障拖垮基础体验 |

**首期架构**：HTTPS 反向代理 → 一个只读公共 API 服务 → SQLite/轻量数据库与定时采集。个人账户出现后再引入会话和按用户隔离的数据表，避免一开始做过大的后台。可使用 Go 与现有代码统一；数据库定期备份并演练恢复。

**首期接口建议**：`GET /v1/catalog`、`GET /v1/notices?source=&cursor=`、`GET /v1/calendar?term=`、`GET /v1/releases/latest`、`GET /healthz`。响应含 schemaVersion/source/updatedAt，ETag 与过期缓存；数据源采用允许列表，不提供任意 URL 转发。创建订阅和同步接口等到认证方案确定后单独接入。

**部署前仍需确定**：校内主机与域名、HTTPS 证书、允许的外网访问方式、服务运行用户/备份位置、公开数据源授权与抓取频率。当前没有这些部署信息，因此没有擅自创建远程服务，也没有假称后端已经上线。

## 10. beta0.5 验证、交付与宣传边界

- Node 规则回归 10 组通过：种植/浇水/一次收获/出售、资源与等级、日期/重复奖励、专注跨刷新、离线保底、待办一次奖励、损坏导入、GPA、装饰/成就、HTML id 唯一性。
- Go 全项目测试与 vet 通过；存档独立 Server 并发只允许一个相同 revision 写入、损坏文件保留；模拟校园认证与 API 来源保护沿用回归。
- 实际 Windows exe 冒烟通过：GUI Subsystem=2、全部内嵌资源逐字节相符、旧 art 404、卡号默认隐藏/主动查看、密码不返回、跨来源拒绝、正常退出、跨进程且换端口存档恢复。
- 浏览器已实际操作：待办新增/完成；种植/浇水/成熟/收获/出售/兑换装饰/摸摸；GPA 录入/刷新恢复；专注开始/刷新/取消确认；服务搜索。1366×768、1440×900、390×844 检查，无横向溢出，窄窗滚动到页脚；未见浏览器 error 日志。
- 学校服务为官方网页入口：教务网站本轮可读取，其余部分校内网站在外部抓取超时；不宣称全部链接已在真实校园环境登录验收。
- **未验收**：实际教学区/宿舍账号登录与注销、校内后端部署、实验 VPN、数字签名、长期经济平衡。不能将候选版宣传成所有学校业务均已接入。
- 发布候选产物：`dist/szudesktop-windows-amd64.exe`、`dist/szudesktop-beta0.5-windows-amd64.zip` 与旁边 SHA256。发布包不含开发测试存档。
- 可用宣传表述：“szuDesktop beta0.5：校园网工具、官方服务导航与一座可离线养成的荔枝庭院。支持伙伴照料、种植经营、学习专注和本机备份。Windows 单文件，无需安装。”加注非官方测试版与个人学校业务仍以官方入口为准。
- **GitHub 交付方式**：PR #1 已合并；本次风格恢复已推送 [PR #2](https://github.com/SzuDesktopTeam/szudesktop/pull/2)（`fix/restore-pixel-campus`）。源码审核与正式发布分开，本轮不打 tag、不发布 Release。


## 11. 恢复原有星露谷式界面并精修（2026-09-18）

用户明确要求保留原风格。上一轮将主体改成常规绿色面板的方向撤回，后续视觉优化必须以原来的像素校园、美术和角色为基础。

- **恢复**：暖棕木牌导航、奶油纸面、橙金选中态、像素字体/水果图标、荔宝对话框，以及原项目自绘花草。顶部为校园场景与木招牌，不重新加入仿系统窗口条。
- **精修**：缩减招牌区和庭院说明占位，资源与每日补给合并；统一像素边框和按钮按压反馈；长说明与输入保持清晰。小屋恢复木墙、地板、窗框和地毯，农田加入草地与土垄，图鉴和市场沿用同一套木纸样式。
- **保留修复**：自然滚动、窄窗布局、卡号默认隐藏、GUI 启动、稳定存档、伙伴农田经营与学习工具均保留；荔宝对话框位于文档正常流，不遮挡操作。
- **字体与素材**：复用本地 Fusion Pixel 字体，并附上[官方 OFL 声明](https://github.com/TakWolf/fusion-pixel-font/blob/master/LICENSE-OFL)。原版 SVBold/SVThin 和来源未核实的游戏贴图继续排除，不妨碍恢复风格。花草来自仓库自身 `gen_flora.py` 和已有 `art/flora` 绘制结果。
- **交付方式**：核查时发现 PR #1 已合并，因此从最新 main 建立 `fix/restore-pixel-campus`，已提交 [后续修正 PR #2](https://github.com/SzuDesktopTeam/szudesktop/pull/2)。仍只维护本文，不建立第二份视觉任务清单。
- **本轮验收通过**：Node 10 组规则检查、Windows GUI 重新构建、实际 exe 冒烟全通过；字体样式表与全部分片、许可、原创花草均可从成品读取。卡号默认隐藏、跨来源拒绝、正常退出和换进程/端口恢复存档均通过。
- **实际画面与交互**：1366×768 和 1440×900 检查首页/伙伴/农田；390×844 检查网络与农田滚到底、服务搜索，无横向溢出，卡号输入为空。每日补给与摸摸头能正确更新资源、亲密度和日志。浏览器 error 日志为空。截图已更新为 `docs/screenshot-desktop.png` 和 `docs/screenshot-garden.png`。
- **测试脚本修正**：Windows 下 urllib 关闭连接的方式偶发导致来源拒绝/退出检查收到连接重置；改用直接回环 HTTP 连接，避开系统代理并完整读取响应。应用来源保护未放宽。
- **上一轮风格恢复产物（已由第 13 节新版替代）**：exe 11,440,640 字节；ZIP 6,823,130 字节，包内 exe 与构建结果逐字节一致，附快速开始、项目许可及字体 OFL。ZIP SHA256：`f0fa5e952e6bf1213756b513c5113e519d2b4c5e8848d19833251ae4b0fd2e2a`。

## 12. 从首次使用到公开发布的用户体验复核（2026-09-18）

**结论：适合定向、小范围 Beta 试用；暂不建议按“已完善的校园助手”面向全校宣传。** 已有可运行的初版，剩余工作以首次体验、运行/退出、真实校园验证和发布渠道为主，不需要先完成校内后端或继续扩充玩法。本节是发布前的剩余清单，前文“已完成”只表示对应实现和已列明的本地验证，不代表整个产品已达到公开首发标准。

### 12.1 本轮证据与边界

- 用实际 beta0.5 exe 和全新临时存档检查首页、学习、设置；实际新增待办、启动 5 分钟专注并跨页查看。首页能显示“继续当前专注”，设置提供本地备份和完整退出，账号没有预填。
- 在 1366×768 首屏，顶部招牌、导航及欢迎区到约 450px；待办和常用服务位于首屏下缘。可滚动但首次到达常用任务的效率仍有提升空间。
- 同一临时存档启动两个进程，两个窗口先读取同一状态；第一个新增待办后，第二个点击摸摸头，显示“另一个窗口更新了存档，请重新加载后继续”。冲突保护有效，但用户需要自己刷新。关闭第二个页面后两个进程仍存在；随后通过第一个窗口的设置→退出应用确认其对应进程结束，页面显示已退出。另一个残留的测试进程单独清理。
- 当前界面未提供应用内反馈入口、打开下载页/更新说明入口或首次使用引导。专注页面有倒计时与完成领取；源码未实现跨页到时提醒或后台系统提醒。本轮没有等待完整 5 分钟，不将提醒缺失写成到时实测结果。
- 宠物默认为黄色猫咪且叫“荔宝”，对话框同名角色是粉色荔枝形象，角色身份尚未明确区分。背景细节、粗像素花草和角色的像素尺度不统一。
- 实时核对 GitHub：PR #2 仍 OPEN，代码提交 e55048c 的测试及各平台构建均成功，release 步骤跳过；公开最新预发布仍是 beta0.4（2026-09-17），不能告诉新用户已能从 Release 下载 beta0.5。
- 本轮没有真实校园账号、教学区/宿舍现场登录，没有第二台干净 Windows 设备，也没有验证系统信誉提示；这些是验收缺口，不直接断言对应功能失败。

### 12.2 剩余问题按重要程度与难度分类

| ID | 用户实际遇到的问题 | 重要 / 难度 | 处理和验收标准 | 状态 |
| --- | --- | --- | --- | --- |
| Q01 | 校园网是主要卖点，但真实成功/失败/注销尚未现场验收 | P0 / M，依赖现场 | 教学区和宿舍各完成登录、错误密码、注销、重连；只记录脱敏结果。未验收前标明实验性，不能宣传全场景可用 | 待现场验证 |
| Q02 | 关窗口后仍在后台；重复启动累积进程并触发存档冲突 | P1 / M | 按存档单实例；关闭所有窗口约 10 秒退出；刷新/多窗口保持运行；冲突后自动同步且保留表单草稿 | 已修复，见第 13 节 |
| Q03 | 宣传新版，但下载页还是旧版，首次获取容易走错 | P1 / S | 待发布决策后合并验证过的源码、生成对应 beta0.5 预发布包、截图/更新说明/校验值保持一致；从下载包在干净设备打开 | 待发布，当前未擅自合并或发布 |
| Q04 | 应用内没有反馈及下载入口 | P1 / S | 设置增加 GitHub 问题反馈与下载/更新说明链接；自动更新和诊断包另行规划 | 已补齐入口 |
| Q05 | 首次进入功能很多，不清楚先做什么 | P1 / S–M | 沿用 U15/X01，增加可跳过的简短引导：功能范围、存档在本机、如何退出；引导一次领取/种植即可，不增加冗长教程 | 已完成（初版，见第 29 节） |
| Q06 | 首屏装饰/欢迎区挤压待办与常用入口，常用操作需下滚 | P1 / S–M | 保留像素风，缩小常驻招牌/欢迎区；1366×768 首屏能开始主要操作；不以隐藏滚动或裁切内容换取一屏 | 待精修 |
| Q07 | 背景、花草、角色精细程度不同；两个不同形象都叫荔宝 | P1 / M（对视觉宣传重要） | 统一像素尺度、光影/色板与视角，确定荔宝和猫咪的身份与文案；保留木框/纸面风格，不再次换成通用后台 | 角色身份已区分；美术统一仍待精修 |
| Q08 | 旧版本进度迁移 | — | 用户明确目前没有老版本用户；取消迁移及相应发布门槛，保留现有导出/导入备份 | 已取消 |
| Q09 | 专注切页后缺少进行中/结束提示 | P2 / S–M | 所有页增加专注状态条、标题倒计时、完成提示；不申请系统通知权限、不承诺关窗后推送 | 页面内已修复 |
| Q10 | 养成已形成循环，但成长与长期目标偏短，种植收益鼓励频繁点按 | P2 / M | 未浇水且不计点击耗时：萝卜净收益 8 币/分钟、荔枝 74 币/小时，前者理论每小时约为后者 6.5 倍。以休闲陪伴为目标测试平衡，增加少量有辨识度的成长反馈，不靠大量新系统堆时长 | 待体验调参 |
| Q11 | 互动冷却和资源不足需要点击后才知道 | P2 / S | 摸摸/玩耍按钮显示剩余秒数；睡眠/低精力提示；购种/食物/装饰/开地资源不足禁用，没种子可前往集市 | 已修复 |
| Q12 | 学院/一句话没有展示用途 | P2 / S | 保存后在首页欢迎区展示用户自填内容 | 已修复 |

### 12.3 有限范围的发布建议

1. 先完成 Q02/Q04/Q05/Q06 的小规模收尾；Q07 做一次统一，不重新设计整套产品。Q01 需要实际校园环境验收，代码检查不能替代。
2. 找 10–20 位同学定向试用，覆盖教学区、宿舍和校外。请他们独立完成“打开应用、找官方服务、使用庭院/待办、退出再打开”；记录卡住的位置和是否愿意第二天继续用。人数是建议，不是已完成测试。
3. 公开 Beta 门槛：核心登录按宣传范围完成实测；重复启动/退出和存档不误导；新版下载一致；至少一个反馈入口；当前没有老版本用户，不设置迁移门槛。根据实测修复问题后再宣传。
4. 初版不必等待云同步、排行榜、多宠物、多人庭院、自动课表/余额、完整校内后端。现阶段这些不是阻止初版发布的原因。
5. 宣传定位采用“像素风校园小工具 + 本地学习/庭院陪伴”，明确学校服务主要是官方入口；可作为 Beta 发布，不写成已接入所有学校业务的正式 1.0。

以上为上一轮检查记录，修复进展以表中状态及第 13 节为准。

## 13. 现有 Bug 修复与无老用户范围确认（2026-09-18）

用户明确：现在没有老版本用户。本轮不做迁移助手，移除应用内及发布说明里的旧版迁移负担；保留本机备份恢复。发布复核任务编号改为 Q01–Q12，避免与早期业务清单 B11/B12 重名。

- **重复启动**：使用按配置目录隔离的操作系统文件锁；重复启动复用现有服务和地址，不启动第二份后台服务；异常退出遗留的发现文件不阻止重开。激活接口校验随机实例令牌，客户端只连接回环地址。
- **关闭/刷新**：每个窗口建立独立持续连接，服务端检测连接断开；关闭最后一个窗口后约 10 秒退出，刷新有宽限期，另一个窗口还在则保持运行。连接由服务端每 15 秒维持活跃，不依赖浏览器关窗通知或后台 JS 计时器；异常失联还设有过期回收，系统睡眠唤醒有恢复时间。设置仍可立即退出。
- **多窗口存档**：保留版本冲突保护；冲突时读取最新存档，明确当前操作尚未保存。表单草稿（包括设置选项）保留，可直接重试；不会静默覆盖其他窗口的记录，也不会永久锁住全部保存操作。
- **登录输入**：已保存卡号以独立文本显示，查看/隐藏不会改写账号输入框或清除用户正在填写的账号。账号先去除前后空格，再判断是否与密码同时填写。
- **专注与互动**：跨页状态条、页面标题倒计时和结束提示；互动按钮显示冷却秒数、睡眠/精力不足状态；资源不足的购买/开地按钮禁用并提示，种子耗尽提供去集市入口。
- **展示与帮助**：学院/一句话保存后出现在首页；粉色助手仍叫荔宝，默认猫咪叫栗栗，动态日志遵循用户为伙伴取的名字；设置补上反馈、下载与更新说明入口。
- **界面边界**：保留木框/纸面/像素风，本轮未重做背景和首屏，不把新手引导、经济平衡、后端建设当作 Bug 修复扩张。
- **验证**：新增单实例锁/遗留文件、关闭/刷新/多窗口、休眠/失联和迟到心跳的回归；现有 Node 10 组规则通过，Go 全项目测试及 vet 通过。实际 Windows 成品初轮验证通过：重复启动子进程正常结束、主进程保留、来源/令牌保护、跨端口存档恢复、最后窗口关闭自动退出。
- **实际页面已核对**：两个窗口的待办冲突能同步并保留草稿，重试后两条待办同时存在；关一个窗口不影响另一个；刷新后恢复；查看虚构测试卡号时账号/密码输入仍为空；个人展示保存有效；摸摸后显示禁用的 10 秒倒计时。

真实教学区/宿舍认证仍需现场验证；目前没有真实账号现场测试记录。

- **最终版验收**：重新构建后的 exe 冒烟全部通过，新增持续连接断开测试确认后台退出。真实专注跑满 5 分钟，首页出现完成提示，领取后累计 1 次/5 分钟，重启后资源和学习记录保留；390px 窄窗状态条无横向溢出。实际市场资金不足时按钮显示原因且禁用。
- **上一轮交付（由第 14 节替代）**：exe 11,484,672 字节；ZIP 6,837,525 字节，包内 exe 与构建一致。ZIP SHA256：`b8dd057333f43bbcf51cc2ebdabd03f50d7a6dfd281518c1bec8ec39fc8b9bce`。
- **真实浏览器关窗验证通过**：最终版双窗口下，关掉一个窗口后另一个仍可新增待办；关闭最后窗口后，Windows 后台进程自行结束。浏览器 error 日志为空。


## 14. 官方业务接入第一批与预约实测（2026-09-18）

本轮目标覆盖本科生和研究生，优先推进自习空间预约。保留原有像素木框与纸面风格；所有剩余工作仍在本文维护。**当前是部分交付，不能宣传成个人成绩和预约接口已全部接通。**

### 14.1 已实现与已核实

- **公开公告**：本机后端按需读取教务部与研究生院官方页面，展示日期、标题、原文和读取时间；10 分钟缓存，失败后限频，有旧数据时明确提示。请求仅限预设官方来源，不提供任意网址代理。现场分别读到 5 / 30 条，界面最多展示最新 12 条；不是全站完整检索。
- **成绩与绩点**：支持本科 / 研究生成绩表粘贴、CSV / TSV / TXT 文件、UTF-8 / GB18030；预览、逐行错误、合并去重、300 门课程、学期 / 层次筛选和不计入开关。本科 A+ 至 F 按[学校规则](https://jwb.szu.edu.cn/info/1357/2003.htm)换算；已有官方绩点优先；研究生仅采用表内官方绩点。重修记录保留供用户选择，不自行假定学校重修口径。无 PDF / 图片 / XLSX 解析，无在线成绩自动同步。
- **预约入口辨别**：用户图片二维码指向[一站式社区静音舱](https://swzx.webvpn.szu.edu.cn/#/pages/booth/szu-booth-list)，实际包含共享会议室、网络面试间、琴房、心晴小屋；不能将这些空间统称为普通图书馆自习座位。
- **真实官方页面验证**：用户完成 WebVPN 二次验证；经用户同意勾选场地使用须知后，进入文山湖社区共享会议室 1 的时段页，看到日期切换、半小时时段及空闲 / 已预定 / 不可选图例。此处只查询，未点击申请预约。页面登录成功不代表本机应用已取得可复用会话或预约 API。
- **本机提醒**：手动登记地点和未来时间，随存档保存，可导出标准 ICS 日历（开始前 15 分钟提醒，由日历软件决定实际通知）。明确提示不代表预约成功，移除本机提醒不取消学校预约。
- **图书馆区别**：研讨间保留[官方入口](https://www.lib.szu.edu.cn/space-and-facilities/discussion-room)；阅览座位按[官方选座规则](https://www.lib.szu.edu.cn/space-and-facilities/seat)绑定服务号、现场扫码并选座，不混用社区接口。
- **凭据处理**：账号密码、短信 / 企业微信验证码、登录 Cookie 均未加入源码、文档和发布包。认证诊断临时程序不属于产品功能。

### 14.2 剩余任务及完成前的重要程度

| 关联 ID | 下一步 | 重要 / 难度 | 当前缺口与验收标准 |
| --- | --- | --- | --- |
| C28 | 应用内实时场地、日期、空闲时段查询 | P0 / L | 官方网页已查到；还需稳定的用户登录会话接入和只读接口适配。验证会话过期、权限差异、无空位和断网，不能以打开网页当完成 |
| C28 | 应用内预约、取消与结果查询 | P0 / L | 在查询适配完成后实现；提交前展示场地和时间并由用户确认，失败不可误报成功；本轮未提交任何真实预约 |
| C2 | 本科 / 研究生个人成绩在线读取 | P1 / L | 两套系统分别核实只读接口、字段与登录状态，不把入口、文件导入称为自动同步 |
| C27 | 图书馆座位 / 研讨间业务 | P1 / L | 与静音舱分开接入，遵循学校现场签到及账号权限 |
| B11 | 公告增补与解析维护 | P2 / M | 当前仅两处公开页面；其他来源逐个核实，页面结构改变时提示并保留原页 |
| R08 | 校内公共服务后端 | P2 / M | 可以承载公告缓存、目录和校历；尚未提供服务器部署信息，未部署，也不集中保存同学校园密码 |

### 14.2.1 共同瓶颈：本机尚未持有可复用登录会话

上表中 **C28（实时空位 / 预约）、C2（成绩在线读取）、C27（图书馆业务）不是各自独立的活**，
它们卡在同一个地基上：

- 当前只有**用户在浏览器里登录后的页面**能看到数据。官方页面登录成功
  **不等于**本机应用取得了可复用的会话或接口权限（见 14.1）。
- 本机既没有会话，也没有那些接口的**真实请求样本**（返回结构、字段、分页、权限差异
  都是未知）。此时写解析器只能靠猜，猜出来的东西会**报“成功/空位”但不真实** ——
  直接违反本项目的发布红线。
- 因此这三项在会话桥打通前不做，也不在发布说明里暗示已接通。

下一步若要推进，按这个顺序，且每一步都要有真实样本：

1. **会话桥最小验证**：用户自己登录后把会话交给本机（只存本机加密，不进源码、不进发布包），
   验证能用它重复取到同一份页面。账号密码与验证码始终不落地。
2. **抓一个真实只读请求**：确认时段数据到底是 HTML 渲染还是 JSON，弄清字段与分页。
3. **先只做查询**：写解析器并验证会话过期、无权限、无空位、断网四种情形都报错而不误报成功。
4. **预约提交最后做**，且属于写操作：必须由用户确认场地与时间后提交，失败不能报成功。
   在项目「不代替用户提交」的原则下，这一项始终要人亲自点确认。

### 14.3 验证记录

- Node 原有 10 组与新增 6 组规则通过；新增检查覆盖本科 / 研究生换算边界、带引号表格、错误行、去重与重修、超过旧 80 门限制、提醒校验及 ICS 转义 / UTF-8 折行。
- 公告后端测试覆盖来源限制、日期 / 链接解析、缓存、失败回退与重试限频；Go 全项目测试与 vet 通过。
- 浏览器实际粘贴本科测试课程：3 学分 A+ 与 1 学分 F 得到 3.38，取消 F 的计入后为 4.50；研究生 A 等级提示缺少官方绩点，明确填入 3.7 后能保存且分层次统计为 3.70。测试使用隔离存档和虚构课程。
- 已实际保存本机提醒、触发日历导出，并在页面读取研究生院真实公告。日历格式经自动测试验证，未声称已在第三方日历软件完成提醒验收。
- CI 依赖升级（F19）后由 PR CI 全量验证：test、build-desktop-windows 与 6 个平台 CLI 全部通过；action 版本为 checkout v7.0.1 / setup-go v7.0.0 / setup-python v7.0.0 / upload-artifact v7.0.1 / download-artifact v8.0.1 / action-gh-release v3.0.3。
- 电话簿（A3）新增回归规则：每条号码必须带官方来源链接、深圳号码带区号；不含号码的部门只能给入口且不得带 tel/mail。7 组校园检查与 10 组界面检查通过，Go vet 干净，exe 冒烟全过。号码取自图书馆官网公开页脚，未核实的一律不写。
- 最终构建验收（2026-09-19）：实际 Windows exe 冒烟全部通过，包含新增公告来源 / 方法 / 跨来源限制及两份新增内嵌模块；最终版手工新增研究生课程能正确保存培养层次，重启保留此前课程、计入选择和提醒。桌面及 390px 窄窗检查无横向溢出，浏览器 error 日志为空。
- **最新本地交付（含电话簿 A3 后重新打包）**：`dist/szudesktop-windows-amd64.exe`（11,610,112 字节，SHA256 `2dd2d0967e978436a194fe6d684775e2f50684f603b341f09ac77774a439c797`）；`dist/szudesktop-beta0.5-windows-amd64.zip`（6,882,860 字节，SHA256 `5a55f2bb1c7007ca2edfd65c8d07623d3bd6bf3778eae4c80b382bbf00690a10`）。ZIP 内 exe 与构建结果逐字节一致，仅含程序、快速开始和许可证。
- **已发布**：tag `beta0.5`，GitHub Release「szuDesktop beta0.5」为预发布版，7 个附件。发版前已完成：PR #2 合并入 main（`cc5f274`）、CI action 升到 Node 24 兼容版本、电话簿补录与来源校验。
- 临时认证诊断进程与研究文件的清理被自动审批以“策略阻止”拒绝，未重试绕过；临时目录未加入版本控制，发布包也不含这些文件。

## 15. 学校系统会话桥与在线成绩读取（2026-09-19）

用户授权参考 [tools4szu](https://github.com/MattDong123/tools4szu)（作者 Matt，深大校友，用于学术交流），
本轮把它的**接口事实**（URL、字段名、认证形态）用于实现，代码为 Go 重写，未复制其源码。

### 15.1 会话接入方案与待验证边界

§14.2.1 判定 C28/C2/C27 卡在「本机没有可复用登录会话 + 没有真实请求样本」。
参考项目提供了接口线索，但没有替代本项目的真实账号验收：

- 教务（`jwapp`）、研究生（`gsapp`）、场馆预约（`qljfwapp`）在同一 `ehall.szu.edu.cn` 主机下，存在复用登录会话的可能。
  Cookie 路径和账号业务权限可能不同，必须分别验证；社区静音舱位于另一站点，不能据此关闭 C28。
- 它的认证方式极朴素：**用户自己登录后，把浏览器里的 Cookie 交给程序**，不做登录逆向、
  不复现二次验证。该方案尚需验证不同业务的 Cookie 范围和真实返回数据。

因此调整为：**先做会话地基（粘贴 Cookie → 本机加密 → 复用 → 过期明确报错），
WebView 交互（D2）降级为后续「多一种获取方式」，不阻塞当前进度。**

### 15.2 本轮实现

| 项 | 位置 | 说明 |
| --- | --- | --- |
| 会话存储 | `internal/credential/session.go` + 三端 keyring | Windows DPAPI / macOS 钥匙串 / Linux Secret Service；安全存储不可用时拒绝保存，不降级为明文；与校园网密码分开存 |
| ehall 客户端 | `desktop/internal/ui/ehall.go` | 固定域名、显式绕代理、限制响应体大小、跳转拦截 |
| 成绩读取 | `desktop/internal/ui/scores.go` | 本科 `xscjcx` / 研究生 `xscjcx_dqx`，只读 |
| 本地接口 | `desktop/internal/ui/session.go` | `/api/session`（GET/POST/DELETE）、`/api/session/check`、`/api/scores?level=` |
| 界面 | 学习工具 → 成绩与绩点 → 「从学校系统直接读取」 | 含统一身份认证学号密码登录表单（新增），以及原 5 步取 Cookie 图文说明作为备选；两者独立，保存后都清空输入框 |

**接口事实（已核实自参考项目，本机未联网实测）**：本科 `POST /jwapp/sys/cjcx/modules/cjcx/xscjcx.do`，
字段 `JXBID/KCM/XF/ZCJ/JD/XNXQDM`，数据集 `xscjcx`；研究生 `POST /gsapp/sys/xscjglapp/modules/xscjcx/xscjcx_dqx.do`，
字段 `JXBID/KCMC/XF/DYBFZCJ`，数据集 `xscjcx_dqx`；返回外层 `{code,msg,datas}`。

### 15.3 安全与诚实性约束（已进回归）

- 会话**不落明文盘**、不进日志、不进源码、不进发布包；状态接口**只回报长度不回报内容**。
- 保存后立即清空输入框；清除会话**不影响校园网账号密码**（两者独立，有测试焊死）。
- **读不到 ≠ 没有数据**（本项目红线）：会话失效返回明确的 401 提示；服务端返回了行但认不出
  课程字段时**报错并让用户去官方核对**，绝不显示成「暂无成绩」。四种情形各有测试。
- 只读。不代提交预约、不代评教；参考项目里的自动签到与自动选课**明确不搬**（违反本项目原则）。

### 15.4 验证记录

- Go 全项目测试与 vet 通过；新增 13 组测试，含假 ehall 服务的端到端读取
  （本科/研究生分数据集、会话失效、接口改版、真·空结果、无会话不发请求）。
- Node 检查 10 组 + 校园 9 组通过；新增两条回归：会话不得回显进页面/源码、在线读取不得含写操作
  且必须区分「过期」与「无数据」。
- 成品 exe 冒烟 48 项全部通过。
- 真实浏览器走查（1366×768 与 390×844）：卡片可见，5 步说明齐全，4 个按钮均可点击，
  textarea 宽 1060/288px，无横向溢出，无 console 错误。
- 端到端接口验证 9 项通过：含「假 Cookie 被明确拒绝」与「磁盘上无明文会话」。
- **未验证**：真实 ehall 会话能否读到真实成绩。需用户在本机应用内输入会话后逐业务验收，不在聊天中提供 Cookie。

### 15.5 参考项目里明确不做的事

| 它的功能 | 为什么不做 |
| --- | --- |
| 学习通自动签到 | 代签到属于学术不端，违反项目原则 |
| 研究生自动选课 | 代提交且属抢课；X09 已写明不自动抢课 |
| 场馆自动预约（`postBook`） | 写操作。将来只能做成「查到空场 → 用户确认后由用户提交」 |
| 把 Cookie 明文写进 `.py` 文件、写进日志 | 我们改为系统安全存储 + 日志脱敏 |

### 15.6 致谢

接口调研参考 [MattDong123/tools4szu](https://github.com/MattDong123/tools4szu)（作者 Matt），
经作者授权用于本项目实现。本项目按其记录的接口事实用 Go 重新实现，未复制其源代码；
其中代签到、自动选课、自动提交预约等功能因与本项目原则冲突而未采纳。

### 15.7 下一批（仍待做）

| 关联 | 下一步 | 重要/难度 | 缺口 |
| --- | --- | --- | --- |
| C12 | 体育场馆空场查询（只读） | P2 / M | 接口 `getSportVenueData` / `getTimeList` / `getOpeningRoom` 已记录；需真实会话实测字段 |
| C12 | 场馆预约提交 | P3 / L | 写操作，必须用户确认场地与时间后提交，失败不得报成功 |
| A4 | 研究生百分制→绩点换算 | P2 / S | 参考项目有换算表，**须先核实是否为学校官方口径**再落地 |
| C17 | 宿舍电费查询 | P2 / L | 接口是校园内网地址，仅宿舍网可达。**具体地址不写进本文件**，只留在 `.local-docs/` 的调研笔记里 |
| — | 成绩在线结果并入绩点统计 | P2 / S | 目前只展示原始记录，未自动并入本机绩点计算 |


## 16. 会话桥交接复核（2026-09-19）

本节复核提交 `4d4d9e5`，以当前源码和产物为准，修正第 15 节中超出证据的结论。该次复核没有使用真实 Cookie，没有学校预约写操作；下表保留问题发现时的证据，修复状态见第 17 节。

- 本地 main 与远端 main 都是 `4d4d9e5`；复核开始时工作区干净。Go 全项目测试 / vet、Node 10 + 9 项检查重新运行通过，但现有测试没有覆盖下表中的关键边界。
- beta0.5 已有 GitHub 预发布；不能再沿用文档开头“未发布”的历史描述。该发布先于本轮会话桥提交，不能据此认定公开包含会话桥。
- 当前 EXE 为 11,675,136 字节，SHA256 `f3c589f668ac924fc7c57929d5db188e7034be529433702db06c3a8345f701a1`；当前本地 ZIP 为 6,882,860 字节，SHA256 `5a55f2bb1c7007ca2edfd65c8d07623d3bd6bf3778eae4c80b382bbf00690a10`。逐字节核对确认 ZIP 内 EXE 与独立 EXE **不一致**。

| ID | 问题及影响 | 重要 / 难度 | 修复与验收要求 |
| --- | --- | --- | --- |
| S01 | `keyring_linux.go` 在 Secret Service 缺失或保存失败时写明文 JSON；Windows 无法确定配置目录时也退回明文。与“三端会话不落明文盘”的承诺矛盾 | P0 / M | 安全存储失败应明确拒绝保存，不静默降级；模拟失败验证磁盘无明文，界面如实说明 |
| S02 | `ehall.go` 的重定向检查只比较 Host，没有阻止同主机 HTTPS → HTTP。手动设置的 Cookie 可能随降级跳转发送 | P0 / S | 请求和重定向均限制 HTTPS 及预期主机；用假传输验证降级请求不发出、跨域不带会话 |
| S03 | `ehallRows` 把缺少 `rows`、`rows:null` 和数据集 `null` 解析成空列表，结构变更会误判为空成绩，探活也可能报成功 | P1 / S | 只有显式合法的数组允许解析；缺字段 / null / 错误类型必须报错，增加测试 |
| S04 | 真空成绩时 `scoreResult.Items` 未初始化，JSON 为 `items:null`；前端直接读取 `r.items.length`，会产生异常 | P1 / S | 空结果始终返回数组，前端防御检查；通过实际 API JSON → 页面验证空成绩提示 |
| S05 | 会话检查固定访问本科成绩接口，不依据用户选择的培养层次；无本科权限的研究生可能被误报会话失效 | P1 / M | 按层次验证对应应用；区分未登录和无该业务权限，不用一个业务探活证明所有业务均可用 |
| S06 | 只取第一页，忽略服务端总数；用当前页条数当 total、少于 200 条当 full。学校若限制每页数量，可把部分成绩报成全部；“按学期分批读取”的操作也未实现 | P1 / M | 核实并处理总数 / 分页；未证实完整时明确显示部分数据，不提供不存在的操作建议 |
| S07 | `XFJD` 同时被当作学分和绩点的备用字段，语义未核实；GPA 用 float64 + omitempty，官方 0 绩点与缺少绩点无法区分 | P1 / M | 仅映射已核实字段，缺失与零值分开；并入本机统计前补真实字段验证及回归 |
| S08 | 清除会话的说明写成“让它失效”，实际只删本机副本，没有向学校撤销浏览器会话 | P1 / S | 文案说明仅清除本机保存；学校会话是否失效以官方注销为准 |
| S09 | 最新 EXE 与本地发布 ZIP 不一致，下载 ZIP 得不到同一轮构建 | P1 / S | 修复后统一构建 / 打包，逐字节核对，更新唯一交付记录；更新远端附件须另按发布范围执行 |
| S10 | “同域 Cookie 一次打通三业务”的结论过强，Cookie 还有 Path / 业务权限限制，且社区静音舱在另一站点；体育场馆并不等于用户优先要求的社区自习空间 | P1 / S | 将结论改为待验证的会话复用候选，分别验收本科、研究生、体育场馆和社区静音舱；不能关闭 C28 |

后续顺序：先 S01/S02，再 S03–S08，最后 S09 并统一修正文档口径。真实会话验收仍缺，但不应把提供真实 Cookie 当作修复上述本地问题的前置条件。真实 Cookie 只应在本机应用中输入，不放进聊天、源码或本文件。


## 17. 会话与成绩安全修复（2026-09-19）

范围为第 16 节 S01–S10；不以真实 Cookie 为本地修复前提，不新增预约提交或研究生分数换算。

| 问题 | 修复状态与实现 |
| --- | --- |
| S01 | 已移除会话明文文件兜底。Linux 安全服务不可用、写入失败或 Windows 无法确定安全存储位置时明确拒绝；读取失败不伪装成“未保存”。Linux 调用带 10 秒超时，凭据仅经标准输入传入，不回传工具输出 |
| S02 | 请求只允许 HTTPS 和预设成绩路径；重定向拒绝降级、跨主机、用户信息及非成绩路径，拦截后不继续发送 Cookie。上游错误不原样回传响应消息或跳转地址 |
| S03 | 仅接受显式 rows 数组；缺字段、null、错误类型和无效行均报错。混合有效 / 无法识别课程也拒绝，不静默丢失 |
| S04 | 无成绩返回 items:[]；前端对非数组显示格式错误，不崩溃。空列表提示仅代表本次查询，并提示核对总数 |
| S05 | 前端选择层次后按对应业务探活，研究生不请求本科接口；401 表示登录失效，403 表示业务权限不足。成功只说明所选成绩业务可访问 |
| S06 | 保留有界的第一页读取。只采用返回中的 totalSize；总数缺失时 full=false，不猜测完整；总数大于本页条数时明确“尚未取全”。移除未实现的“按学期分批读取”建议。自动翻页及真实分页字段仍待后续验收，不宣称已实现 |
| S07 | 移除 XFJD 等未经核实的数字备用映射；学分只读取 XF、绩点只读取 JD。指针字段区分缺失与 0；拒绝非有限数和异常范围。不同成绩 / 重修保留，只有完全相同记录去重 |
| S08 | 文案明确清除本机副本不会注销官方浏览器会话。保存新会话或切换层次时清除上次在线成绩，避免把上一账号 / 层次的结果显示在新选择下 |
| S09 | 统一重新构建与打包，最终校验值见本节末尾；不覆盖远端旧版附件 |
| S10 | 修改“同域即全部打通”的源码注释和说明；界面标明实验读取尚待真实成绩验收，社区静音舱和体育场馆仍分别验收 |

验证覆盖安全存储失败无文件落地、Windows DPAPI 往返、HTTPS 重定向发送次数、缺字段 / 空数组、零值 / 缺失值、部分成绩、层次探活和权限 / 过期区别。前端行为检查覆盖空结果、异常响应、转义、零值、部分提示、切换层次和清除历史结果。

本机修复与模拟测试不等于学校真实接口验收。下一步仍为用户在本机应用中输入对应业务会话后只读核对；本科、研究生、体育场馆、社区静音舱各自验证。不要在聊天或公共日志中发送 Cookie。


- 回归结果：Go 全项目测试及 vet 通过；Node 既有 10 + 9 项及新增 6 项会话界面行为检查通过。Linux / macOS 目标的相关包交叉编译通过；安全服务失败行为通过可替换命令后端测试，未声称在真实 Linux 桌面 / macOS 钥匙串现场验收。
- Windows 实际成品 56 项冒烟通过：新增学校会话状态、层次参数、来源保护、加密文件和清除验证；未连接学校真实个人业务。浏览器实际保存虚构测试会话后输入清空，研究生选择保留，清除后回到未保存状态；桌面和 390px 窄窗无横向溢出、无 JS 异常。未保存会话的探活产生预期 409，不属于页面脚本异常。
- 本轮构建脚本正常完成，未采用重命名目录来规避清理审批。无真实账号、Cookie、测试存档进入发布包。

- **本轮最终本地候选包**：EXE 11,674,112 字节，SHA256 `e4da6928c73cb626fca198212c610c0f78257a3430bf91ee11212cf92ec10a80`；ZIP 6,906,129 字节，SHA256 `b175c873ad486dc6e6d7b8affd54038579ee56459baf1c06d0d92ec8464764ba`。路径仍为 `dist/szudesktop-windows-amd64.exe` 和 `dist/szudesktop-beta0.5-windows-amd64.zip`；包内程序与独立 EXE 逐字节一致。打包脚本现在自动验证一致性，并分别标明 EXE / ZIP 校验值，避免混淆。


## 18. beta0.5.1 发布（2026-09-19）

- 用户明确要求将最新修复和下载包发布到 GitHub。修复 PR #3 已通过测试及全部平台构建，并合并到 main（`a6246e1`）。
- 版本号统一为 beta0.5.1，保留历史 beta0.5 及附件；标签触发 GitHub Actions 从对应源码重新构建 Windows EXE、免安装 ZIP 与命令行程序。
- 发布内容：学校会话安全存储、成绩权限与失效区分、缺失绩点与零绩点区分、不完整成绩结果提示，以及切换培养层次后清除旧查询结果。原有像素风界面、庭院与本机存档保留。
- 在线成绩仍为实验性功能，尚待真实学校成绩验收；目前只读取第一页。社区静音舱仍在官方页面预约，未实现自动查询空位或代提交预约。
- 目标发布页：https://github.com/SzuDesktopTeam/szudesktop/releases/tag/beta0.5.1 。发布成功与附件校验结果以本节后续记录为准。

- **已发布并核对**：`beta0.5.1` 标签对应源码 `a06344d`；[发布流水线](https://github.com/SzuDesktopTeam/szudesktop/actions/runs/35438357252) 全部成功，Release 为公开预发布，共 7 个附件。本地版本检查 25 项、Windows 运行冒烟 56 项通过；CI 再次完成测试、各平台构建和 Windows 冒烟。
- 已从 GitHub 下载 ZIP 并核对：6,906,115 字节，SHA256 `06320062d9c2ff371a4d5eecff357b75bb15961a4c3a6eb57bdf412384514d60`。包内 EXE 的 SHA256 与 GitHub 独立 EXE 附件相同：`86f764dbfb4b808d28ca799417e66f46f582274eb6429c94b1edb1b0e8f27303`（11,674,112 字节）。快速开始版本为 beta0.5.1，包内仅程序、快速开始和两份许可证。以上为实际公开 CI 产物，替代第 17 节的本地候选包作为当前下载记录。


## 19. 出口已被占用时的提示修复（2026-09-20）

起因：用户在教学区网络上点登录，看到「这个网络出口已经有账号在线了，本次登录没有生效」，据此认为登录失败、上不了网。

### 19.1 实测结论：那次登录其实生效了

直接向深澜门户的 `rad_user_info` 发只读查询（绕开本项目的客户端，避免被自己的解析逻辑误导），拿到的事实：

- `error: "ok"` —— 这个出口当时已处于已认证状态。
- 会话 `add_time` 就在用户操作的那一分钟内，`keepalive_time` 与查询时刻只差几秒 —— 会话是活的。
- `online_device_total: "1"`，`online_device_detail` 里那台设备上报的 `os_name` 是 `Mac OS`。
- 而 `Mac OS` / `Macintosh` 正是本项目登录请求里上报的参数（见 `srun.go` 的 `os` / `name`），所以那个在线会话就是本次登录建立的。

也就是说：**用户全程在线、能上外网**，只是被工具的提示误导了。

### 19.2 三个真实缺陷

1. **把"没生效"报成了成功**。`error: "ok"` + `suc_msg: "ip_already_online_error"` 被当成认证成功返回（`OK: true`），却又配了一句「本次登录没有生效」——同一句话里既说成功又说没生效。
   关键在于深澜的 **IP 级短路**：这个出口已有会话时，服务端在校验账号密码和 `ac_id` **之前**就返回了。它既没验证过这组凭据，也没验证过这个接入点编号。

2. **把没验证过的 `ac_id` 写进了缓存**。既然服务端是在校验 `ac_id` 之前短路的，这个编号就没有被证实过；缓存它会让用户下次在别的网络里拿着错值去认证，反而更难查。

3. **能上外网时反而跳过在线查询**。命令行与桌面界面各写了一份相同判断，理由是"不需要认证"。但用户问的是"我到底登上了没"，而"能上网"恰恰是已登录的最强证据。跳过查询的结果是一句干巴巴的"没查到"，用户以为掉线，跑去反复点登录，然后被 `ip_already_online` 挡回来——正好掉进最让人困惑的那个状态。
   附带问题：两处都把查询错误用 `_` 吞掉了，用户连为什么"没查到"都看不到。

### 19.3 修复

- `Login()`：`ip_already_online` 不再返回成功；提示改为能直接回答"我现在能不能上网"，并顺手查一次在线信息，把设备数、在线 IP 与设备描述带出来。
- `ac_id` 缓存：只在认证真正被服务端接受之后写入。
- 新增 `portal.QueryOnline()`，把"按区域查在线状态"收成**一处**，命令行与桌面界面共用（原先的重复实现已删除）。联网时两套协议都问一次——`Detect()` 联网时会提前返回、没跑协议指纹，无从得知当初是哪套协议认证的——谁在线采信谁。
- `OnlineStatus` 增加在线设备数与设备描述；`status` 命令与 `/api/status` 在联网时也给出结论，查询失败时显示失败原因，不再笼统地说"没查到"。

### 19.4 验证

- **真实网络复现**：同一台机器、同一时刻，仅二进制不同 —— 修复前 `账号状态: 没查到`；修复后 `账号状态: 已在线` + 在线 IP + `在线设备: Mac OS · ...`。
- 新增测试 8 项：出口被占用不报成功且不写缓存；账号自身在线仍算成功且正常缓存；在线设备字段解析（含「JSON 字符串里再套一层 JSON」的真实格式）；补充字段读坏不影响在线判断；`error` 字段是唯一在线判据（实测返回里没有 `user_name`）；联网时必须查询；深澜无结果时回落到宿舍区协议；校外不发起查询。
- `go test ./...` 全部通过，`go vet` 通过，Windows 构建成品冒烟全部通过。

### 19.5 遇到这条提示时该怎么办

先看工具给出的在线状态：

- **显示"已在线"且外网连通** —— 会话有效，不需要再登录。
- **想换成另一个账号** —— 必须先让出口上原有的会话下线；同一个出口 IP 只能挂一个账号。
- **"出口被占用"同时外网也不通** —— 这才是真需要处理的：原来那个会话很可能已失效但没有释放。

不要在"已在线"的状态下反复点登录：每次都会走一遍 `ac_id` 探测，而且这次登录同样不会生效。


## 20. 当前使用路径复核与修复（2026-09-20）

基于 `3fa3277` 检查实际 Windows 程序、网络状态显示、学校会话清理、农田交互和多窗口备份。保留现有像素风外观；所有测试使用独立存档和虚构测试会话，不登录学校个人业务、不提交预约。

| 编号 | 问题与影响 | 重要性 / 难度 | 处理结果 |
|---|---|---|---|
| R01 | 后台在线查询没有显示在页面；不保存账号就不查询，查询失败或异常响应可能被当成离线 | P1 / M | 已修复。出口认证与账号保存分开，前后端区分已在线、未认证、未知；两套门户失败不吞掉，状态页不回传卡号和设备地址 |
| R02 | 刷新失败保留之前的在线状态；手动刷新仍提示成功；出口被占用后不刷新状态 | P1 / S | 已修复。失败清除旧状态并提示未确认，检查期间重复点击提示正在刷新，登录请求结束后刷新出口状态 |
| R03 | 农田某种种子耗尽，换成有库存的种子仍显示“种子不足” | P2 / S | 已修复。切换种子立即重绘空地操作按钮 |
| R04 | 多窗口使用时，旧窗口导出的备份漏掉另一窗口已保存的新记录 | P1 / S | 已修复。导出前读取最新本机存档；读取失败不生成旧快照备份，仍保留写入版本冲突保护 |
| R05 | 清除学校登录状态后，粘贴但未保存的 Cookie 仍留在输入框 | P1 / S | 已修复。清除成功后同步清空输入框 |
| R06 | macOS 会话钥匙串读取失败统一当未保存，删除错误被忽略 | P2 / M | 已完成（见第 29 节）。区分「没有这一条」与「钥匙串读不到」，删除失败不再报成功；判断逻辑无构建标签，Linux CI 可测。真机 macOS 仍未现场验收。同一轮复核另发现 F21（密码经 argv 传给 `security`） |

验证：

- `go test ./...`、`go vet ./...` 通过；网络后端新增模拟门户回归，覆盖未存账号、未知状态、双协议错误和隐私字段。
- 前端规则 / 行为共 31 项通过（10 庭院、9 校园、6 会话、3 网络、3 存档）。新增检查已接入 CI。
- Windows 成品 57 项冒烟全部通过，包含新网络状态资源、无命令行窗口、加密会话、跨端口存档恢复和最后窗口关闭退出。
- 实际浏览器确认：空账号页面能显示当前出口已在线；萝卜库存为 0 时换草莓立即出现播种按钮；清除会话后文本框为空；页面无脚本异常。桌面与 390px 窄窗没有横向溢出，页面下方入口可访问。
- “在途切换成绩导致旧结果回填”被现有禁用交互阻止；“复用确认框后 Escape 误确认”在实际旧成品未复现。两项均未当作确定缺陷，也未添加推测性修补。

交付：本轮源码已推送 [PR #4](https://github.com/SzuDesktopTeam/szudesktop/pull/4)，分支 `fix/desktop-state-review`。本地候选包 `dist/szudesktop-review-20260920-windows-amd64.zip`，SHA256 `3e68ca47a8edfa84e4e76300fda10be7e6e4e331acea271df11a424510b3d856`；包内 EXE 与本轮构建一致（SHA256 `24f0201df3d76ba4caa8a3669e40e073cad843063b95d8630ec2dc33dd579722`）。这是待发布修复构建，内部版本仍为 beta0.5.1；没有覆盖 GitHub 已发布的 beta0.5.1 附件。

仍需完善的主要业务保持原清单：本科 / 研究生真实成绩验收与完整分页、社区自习室空位查询及预约接入。模拟响应和本机检查不替代学校接口验收；这些不是本轮已完成内容。

## 21. 自动校历、课表入口与校园背景（2026-09-20）

继续在 PR #4 的分支上推进用户提出的四项需求。以下为开发构建状态，不代表已替换 GitHub 的 beta0.5.1 公开附件。

| 编号 | 需求 / 问题 | 重要性 / 难度 | 当前结果与下一步 |
|---|---|---|---|
| A21 | 校历必须手动填日期；原算法在周日比官方周次少一周 | P1 / M | 已完成自动校历。本机读取学校官网、缓存日期，程序运行期间每日检查更新；Windows 使用系统文字识别解析更新后的校历图片。保留手动覆盖和恢复自动，不把寒暑假继续累加成教学周 |
| A22 | 学习空间预约需要跳转、校园网登录不能复用为所有业务登录 | P1 / L | 尚未完成应用内预约。已有浏览器会话本次进入学校二次验证页，等待用户完成验证；未读取受保护的实时场地数据，未提交预约 |
| A23 | 没有课表入口，也没有本科 / 研究生课表查询适配 | P1 / M–L | 研究生登录、验证码与真实课表接口已做成接入预览，等待本人完成验证码后实拉验收，详见第 23 节。本科入口实为全校课表查询，个人课表仍待接入；不由成绩接口可用推断课表权限 |
| A24 | 背景层次不足，简单装饰与新场景叠加显得杂乱 | P2 / S | 已换为生成的湖畔黄昏像素校园，增加顶部景色空间，移除与树冠重复的顶栏花草；木框、纸张、像素字体保持原风格 |

### 21.1 官方校历依据及自动更新边界

- 来源：[深圳大学校历](https://www.szu.edu.cn/xxgk/xl.htm)。本轮直接读取官网最新图片并人工看过说明与日历网格；网页检索缓存出现过旧图片，因此没有用检索摘要替代官网文件。
- 2026–2027 第一学期：2026-08-28 至 2027-01-22，老生 2026-08-31 开始上课；日历网格第 1 周起始日为 2026-08-30（周日）。因此 2026-09-20 是第 4 周。新生本科和研究生开始上课日期不同，界面显示的是全校校历周次，提醒以个人教学通知为准。
- 2025–2026 第二学期：2026-03-04 至 2026-07-17，2026-03-09 开始上课；第 1 周从 2026-03-08 开始。官网尚未公布的下一学期不自行生成。
- 新增只读 `/api/campus/calendar`。首次显示随包核实的日期或本机缓存；页面异步检查官网，每小时检查缓存是否超过一天；仅官方图片清单变化时下载并识别。网络失败、布局无法识别或缺少系统识别语言包时保留旧日期，明确提示更新失败并给出官方页和手动设置。
- Windows 识别使用自带 Windows Runtime OCR，不需要额外安装第三方程序，也不上传图片或个人数据到识别服务；后台启动隐藏控制台。macOS / Linux 目前只能检查图片是否变化和使用已核实日期，新图片识别尚未实现。若校方改变周次编排规则，需要重新核对网格，当前按已核实的周日换周规则计算。
- 仅刷新校历摘要，不重绘手动日期表单；刷新期间尚未保存的输入不会丢失。日期计算固定为深圳时区。

### 21.2 预约和课表的后续实现顺序

1. 用户在学校官方页完成二次验证后，先核验静音舱的场地、用途、时段、本人预约记录，仅做读取。学校会话与校园网凭据分开处理，现有 ehall 成绩 Cookie 不发送到其他域名。
2. 依据实际认证机制决定接入方式：独立业务接口配合本机安全会话存储，或使用原生 WebView2 承载学校登录。当前 Edge / Chrome 应用窗口并非原生嵌入式浏览器，不能直接假定 iframe 能保留登录。不会移除学校的 iframe / CSP 防护来强行嵌入。
3. 时段查询和本人记录验收后，再做用户选定场地、日期、时段并确认提交的预约操作；不做自动抢位。本轮没有提交任何预约。
4. 本科入口来自[教务部](https://jwb.szu.edu.cn/)的课程表查询链接；研究生入口使用[学校学生页面](https://www.szu.edu.cn/jsrk/xs.htm)给出的研究生选课系统。当前应用只是提供官方查询入口，不声称已有本地课表。

### 21.3 背景及验证

- 新背景：`desktop/assets/garden/campus.png`，使用内置 imagegen 生成。提示词摘要：原创 16-bit 像素画；深圳亚热带校园、湖面倒影、傍晚桃色云层、远处教学楼、荔枝树和棕榈；细节沿两侧分布，中部留出纸张界面的阅读空间；禁止文字、商标、游戏角色及已知游戏场景。不是实际校园地图，不使用星露谷原版资产。
- 自动校历真实链路验收：使用隔离配置故意设置过期图片清单，让实际 Windows 成品重新下载并识别官网四张图片；成功解析两个学期，日期与人工核对一致。无学校账号或个人会话参与。
- `go test ./...`、`go vet ./...` 通过；前端 35 项通过，包含新校历 4 项。Windows 成品 61 项冒烟通过，包括离线校历、跨来源限制、新资源打包和无控制台启动。
- 浏览器实测：默认显示第 4 周；手动设置 2026-09-14 后显示第 1 周；恢复自动后回到第 4 周；点击更新未清空未保存的日期。桌面和 390px 窄窗检查通过，无横向溢出。
- 本地预览包：`dist/szudesktop-campus-preview-20260920-windows-amd64.zip`，6,959,907 字节，SHA256 `1eb127dfccf274f38f607d83c007cc01f2068e0ad410e641e791e6e106a06ddd`；包内程序与本轮成品一致，EXE SHA256 `7ba68f0d4e5c40d4be4dda9b04970d406052369fcd1450b8214c47c9f41c56fa`。内部版本仍为 beta0.5.1，压缩包名称及说明已标为预览构建。

## 22. 根据真实深大照片重绘像素校园（2026-09-20）

用户要求将泛校园背景改为深圳大学实景，并尽可能将场景细节、小图标替换成星露谷物语、泰拉瑞亚、Minecraft 风格的物件。

| 项目 | 重要性 / 难度 | 完成内容 |
|---|---|---|
| B01 文山湖实景背景 | P2 / S | 已替换 `desktop/assets/garden/campus.png`。保留文山湖棕榈、湖岸步道、圆形花钵及校园楼群；加入结伴散步、读书、推自行车的同学、鸭子、小猫、灯笼、史莱姆和草方块 |
| B02 游戏物件图标 | P2 / S | 新增 18 个项目内绘制的像素 SVG 物件：木屋、水晶、路牌、浇水壶、书本、工作台、金币、面包、爱心、箱子、幼苗、镐、花朵、灯笼、信件、卷轴、沙漏、树。覆盖六个导航、庭院分区、农田操作、背包数值、专注和校园服务操作，保留文字标签 |
| B03 场景展示与细节统一 | P2 / S | 顶部标明“深圳大学 · 文山湖”，增加“看看像素校园”整图浏览；支持按钮返回与 Escape 关闭。重绘首页树木装饰，背景展示高度与窄窗排版一起调整 |

参考照片与素材：

- 地貌及建筑依据：[Lake Wenshan of Shenzhen University](https://commons.wikimedia.org/wiki/File:Lake_Wenshan_of_Shenzhen_University.jpg)，Jauhnn，2010 年照片，作者在原页声明 Public Domain。
- 校园生活参考：[深圳大学校园环境提升 / 翰博设计](https://www.gooood.cn/shenzhen-university-campus-environment-enhancement.htm)，2023-12-11；使用文山湖边背包同学散步照片作为生活氛围参考。原项目页面署名 DID studio、HOPE 翰博设计 杨洋。原照片仅留在忽略目录作输入，没有打包或提交。
- 新背景通过内置 imagegen 以两张照片为参考重新生成；场景为游戏化插画，非校园精确地图。图标在现有 SVG 系统中重新绘制，没有提取游戏安装包中的图片或代码。荔宝、伙伴养成与应用操作逻辑保持现有设计。

<details><summary>最终图像提示词（内置 imagegen，输入 1 为地貌照片，输入 2 为生活照片）</summary>

Use case: style-transfer. Asset: final full-bleed desktop app background, wide 16:9 pixel-art illustration at 1920x1080 or larger. Create PIXEL SHENZHEN UNIVERSITY, specifically the real Wenshan Lake 粤海校区文山湖 in the supplied photographs. Image 1 is the primary real-place architecture and spatial reference: retain the unmistakable palm trunks framing both sides, stone steps descending toward the lake, round pale planters, hedge-lined path, curved shore, cream low-rise campus buildings across the lake with taller urban blocks behind. Image 2 provides student-life reference and the real stepped lakeside terraces with circular tree planters and backpack-wearing students walking in pairs. Recompose these into a coherent illustrated scene of this actual university, no invented European clocktower, castle or generic fantasy university. THE MAIN CHANGE IS TO RENDER EVERY VISIBLE ELEMENT AS A DETAILED GAME SPRITE OR GAME TILE. Strong Stardew Valley style for cozy dense palm and lychee foliage, small 16-bit backpack students strolling, chatting, reading on benches; Terraria style for detailed wood lanterns, tiny orange campus cats, occasional peaceful blue slime tucked beside the path and layered plants; Minecraft-style block textures for grass-topped earth edges, stone-brick steps, blocky flower pots, wooden benches, tiny decorative chest and lanterns. Make the three influences clearly recognizable but unified in a single crisp 2D pixel-art language, not a collage of screenshots or flat modern icons, not 3D voxel rendering. A lively ordinary university afternoon: roughly 8-12 tiny students around the shoreline/path, a student holding a book, another wheeling a bicycle, two ducks on the lake. The campus remains the subject, game items are integrated into daily life rather than dominating. Keep human figures small. Rich harmonious greens, clear jade water with pixel reflections, warm afternoon light, muted pink flowers, pale blue sky. Composition optimized for a wallpaper behind parchment UI: keep the primary scene and lively campus details visible in the upper 45 percent and outer sides; put the lake horizon and distant campus around upper third; keep the top center relatively quiet to fit a wooden title sign. The descending path and round pots can occupy the foreground. Light airy water area, layered depth, crisp deliberate square pixel clusters and selective dark outlines. No smooth painted gradients, no photoreal texture remnants. No text, no labels, no watermark, no interface panels. The result should feel like Shenzhen University has become a playable Stardew/Terraria/Minecraft crossover campus.

</details>

验证与交付：

- 相关前端回归 31 项通过；18 个新增 SVG 物件的引用均可解析。实际浏览器验证导航、桌面与 390px 窄窗、整图加载、Escape 与返回按钮；无横向溢出、无脚本错误。
- Windows 构建同步通过；61 项成品冒烟通过。最后只调整首页树木在桌面布局上的垂直位置后重新构建，未重复无关后端测试。
- 新预览包：`dist/szudesktop-pixel-szu-20260920-windows-amd64.zip`，6,924,130 字节，SHA256 `688209d005d1aae2edb0d00ba3cac34552eecf0919a6fa65b7a38a3acb309560`。包内 EXE 与最终构建相同，SHA256 `a7131a1e335002d592185fbd2ee7833e0a06d19df70bf5d712756b6aaf4e0e89`。
- 继续更新现有 PR #4；此包是预览构建，内部版本仍为 beta0.5.1，没有覆盖公开 Release。第 21 节的预约和课表待办保持原状态。

## 23. 教务登录、真实课表接入与校历更新检测（2026-09-20）

用户要求在应用内提供教务登录，本轮授权使用本人账号测试，并要求接入真实课表。以下区分代码完成与学校实测，不把模拟通过写成真实个人业务验收。

| 编号 | 问题 / 工作 | 重要性 / 难度 | 本轮结果 |
|---|---|---|---|
| J01 | 课表只能打开外链，缺少本地教务登录 | P1 / M | 已实现研究生教务接入预览：本地学号 / 密码表单、学校图片验证码、登录、读取课表及清除本次登录。密码不保存；会话仅在内存，退出应用即清除。已实际取得学校验证码；等待用户完成验证码后的真实账号验收 |
| J02 | 研究生课表缺少实际数据适配 | P1 / M | 根据学校当前页面脚本实现只读接口，展示星期、节次、原始周次说明、教师、教室、节次方案；已选但未排定时间的课程另列。只提供当前选课学期，不声称支持历史学期。真实本地登录后的完整响应尚待验收 |
| J03 | 本科入口名称容易被理解为个人课表 | P1 / M–L | 实际页面标题为“全校课表查询”，已修正入口名称。本科个人课表尚未接入；本轮统一认证尝试返回用户名或密码错误后停止，没有继续猜测或反复登录 |
| J04 | 校方覆盖同地址的校历图片时不能发现更新 | P1 / S | 已修复。每天联网检查时比较校历图片内容的 SHA256；地址不变但内容变化也会重新本机识别。未变化不重复识别，下载或识别失败保留旧日期与旧指纹 |

### 23.1 官方接口依据与实测范围

- [研究生选课官方入口](https://ehall.szu.edu.cn/yjsxk)当前脚本：[登录与课表操作](https://ehall.szu.edu.cn/yjsxkapp/sys/xsxkapp/public/index.js)、[课表展示字段](https://ehall.szu.edu.cn/yjsxkapp/sys/xsxkapp/public/kb.js)。仅根据接口协议和字段事实用 Go 重新实现，未将学校前端脚本打包。
- 只访问明确列出的学校 HTTPS 地址：验证码、登录校验、本人学生信息、本人课表及公开学期信息。研究生课表查询为 `/yjsxkapp/sys/xsxkapp/xsxkCourse/loadKbxx.do`；没有选课、退课、预约或签到提交路径。
- 官方密码编码存在与标准 DES 不同的密钥位排列，已按公开协议转换后调用标准库，使用四组虚构输入与学校公开编码器核对。此编码只是接口兼容；安全传输依靠 HTTPS，不把协议编码当成本地密码加密存储。
- 浏览器中已有的本人研究生会话可进入“查看我的课表”；官方页面当前显示“没有课程安排记录”。这仅证实官方页面结果，不等于本地登录与解析已经验收，也不说明其他学期没有课程。
- 本地预览已取得官方验证码并准备登录表单。验证码必须由本人完成；截至记录时，尚未收到成功登录结果。未使用已有浏览器 Cookie 绕过这一独立验证，未保存真实账号、密码、Cookie、姓名或课表到源码、测试样例和发布包。
- 本科统一认证与研究生选课为不同链路；本科认证研究仅留在本地忽略目录，没有将未完成的认证代码作为正式功能发布。当前研究生模块的登录不会被当作成绩系统或预约系统已登录。

### 23.2 校历是怎样得到的

1. 本机访问[官方校历页](https://www.szu.edu.cn/xxgk/xl.htm)，读取校历图片地址并下载图片；不需要学生账号。
2. 每日检查图片内容是否变化。Windows 使用系统自带 OCR 在本机读取“校历说明”中的学期起止和老生上课日期，不调用云端识别。
3. 按此前人工核实的周日换周规则计算教学周，固定使用深圳日期；允许手动覆盖，假期不继续累加成教学周。
4. 首次离线可使用随包核实的日期。下载失败、文字识别失败或学校布局变化时保留缓存并提示，不自动猜下一学期。若校方改变周次规则仍需重新核验；macOS / Linux 的新版图片识别仍未实现。

### 23.3 验证与交付

- Go 全项目测试及 vet 通过。新增回归覆盖协议编码、固定 HTTPS 目标、跨来源拒绝、验证码失效及重复提交、登录后的业务读取、清除登录、空课表与无效响应区分、未排课课程保留，以及同 URL 校历图片替换。
- 前端共 40 项检查通过，其中新增教务 5 项；Windows 实际成品冒烟 66 项通过。页面实际取得验证码；桌面与 390px 窄窗无横向溢出、无脚本错误。包含课程的展示使用虚构数据做回归，不能替代真实非空课表验收。
- 新模块显示“接入测试”。本科个人课表、预约、真实成绩完整分页仍沿用原清单继续推进；本轮没有完成这些业务，也没有替换公开 Release。
- 本地预览包：`dist/szudesktop-academic-preview-20260920-windows-amd64.zip`，6,970,064 字节，SHA256 `17a48fb3c6641c6cf6d0ebd3ce03f625ffc88c7fac05eb08e77fc73b42f2b48d`。包内 EXE 与本轮构建逐字节一致，11,840,512 字节，SHA256 `c97a6c8ed75bcf3ca8931e0ac23593c061f4b3171dbefc80fb2f46a63e48b8b4`。版本仍为 beta0.5.1，压缩包及快速开始明确标为教务接入预览。


## 24. 状态颜色与像素物品图标（2026-09-20）

| 编号 | 问题 / 工作 | 重要性 / 难度 | 本轮结果 |
|---|---|---|---|
| V01 | 网络状态文字都用棕色，成功、失败和未确认难区分 | P1 / S | 已完成。外网与校园认证分两行，按各自状态使用草绿 / 红 / 琥珀色；查询失败立即清除旧绿色。颜色之外保留完整文字和图标，不把外网可用当成校园认证通过 |
| V02 | 标题、数值、徽章层级缺少色彩 | P2 / S | 已完成。学习与信息用湖蓝、庭院和校历用草绿、成长与成绩用紫、伙伴心情用玫红、币值和待确认用琥珀色；正文维持纸张上的深色阅读对比度 |
| V03 | 服务入口重复使用同一图标，卡片较空 | P2 / S | 已完成。扩充现有原生 SVG 像素图标，新增 12 个：联网、断网、盾牌、日历、羽毛笔、勋章、挎包、茶杯、指南针、云、钥匙和铃铛。用于状态、标题、日期、教务操作及各个校园入口；均为本项目绘制，没有打包游戏原始贴图 |
| V04 | 新日期图标使 390px 顶栏出现横向溢出 | P1 / S | 实际浏览检查发现并修复。窄窗继续隐藏生活提示，六个主页宽度均与可用视口一致 |

验证：41 项既有及相关前端检查通过；网络回归增加颜色状态独立性、失败清除绿色以及接口错误文本转义。Windows 已重新构建，实际运行检查首页、校园网、校园服务、庭院、学习、设置，390px 窄窗无横向溢出，无缺失图标或控制台错误。本轮仅改界面，未重复整套后端实网验收；教务和预约的待办边界仍见第 23 节。

交付：`dist/szudesktop-color-pixel-preview-20260920-windows-amd64.zip`，6,974,193 字节，SHA256 `3da7902bd5e2a39b14bcf8fc55b23ef686c2822ec987a255b3f086ade3ad31a0`。包内 EXE 与本次构建一致，11,852,800 字节，SHA256 `9ac8041f6e894bac677ce5281a0883ee088bbd43d7a6cf5989fda84bcbf47f1f`。预览仍基于 beta0.5.1，未覆盖公开 Release。继续更新现有 PR #4。


## 25. 本科个人课表与社区场地接入推进（2026-09-20）

本节是第 23 节 J01–J03 和预约待办的最新状态；所有未完成项仍保留在这一个文档中。没有提交任何真实预约，没有把模拟账号检查记为真实业务验收。

| 编号 | 工作 / 剩余问题 | 重要性 / 难度 | 当前结果与下一步 |
|---|---|---|---|
| J01 | 研究生教务真实登录验收 | P1 / S（需本人验证） | 再次确认官方浏览器会话可查看本人课表，当前官方页无课程安排；本机登录仍未完成。已向用户交接图片验证码表单，未收到成功结果。需要本人填写当前教务密码和验证码，再核对本地响应 |
| J03 | 本科个人课表 | P1 / M | 已添加 `wdkb`「我的课表」接口与本地展示，修正将 `kcbcx` 全校查询当个人课表的入口。当前通过本科业务的 ehall Cookie 读取；本轮官方个人课表入口在现有浏览器显示 403，原因尚未核实，仍需有效的本科业务会话验收。不能由研究生登录推断本科可用 |
| R01 | 应用内真实空位 | P1 / M | 已完成校内网络实测：本次构建读到 15 个场地、指定日期 20 个开放半小时时段及真实空闲状态。用户可在应用内选择场地、日期并刷新；不再只能打开外链 |
| R02 | 预约登录与本人记录 | P1 / M | 保留服务端适配代码，尚未完成真实登录验收。用户侧手动预约 Cookie 输入及本人记录界面已撤下；等待正常的登录衔接后再恢复本地个人数据展示。当前由学校原页处理登录和本人记录 |
| R03 | 应用内提交预约 | P1 / M | 服务端确认提交适配仅模拟验收通过，未做真实预约。当前已撤下本地提交表单与可选时段，避免误导为完整可用；用户在学校原页选择时段并提交。原网页内嵌与独立接口提交不能混作已完成 |
| R04 | 应用内承载学校原页及正常登录 | P1 / L | 尚未完成。当前为 Edge / Chrome 应用窗口，本轮 iframe 原型持续空白，不能据此断言学校禁止嵌入；直接打开可到达官方统一认证页。后续需验证原生 WebView 承载下的完整二次认证、场地与结果流程，不能以复制 Cookie 代替正常登录 |
| R05 | 图书馆阅览座位 / 研讨间 | P2 / L | 属于另一套系统，本轮没有接入。继续保留官方入口，与社区会议室、面试间、琴房分开说明 |
| R06 | 预约要求 F12 / Cookie，选中时段后无可用下一步 | P1 / S | 本地已改为「登录并预约」直接打开学校原页；移除预约 Cookie 输入、开发者工具教程与未验收的提交表单。空位改为只读预览，保留接口查询。15 项相关前端检查、Windows 构建及真实页面查询检查通过，尚未更新 GitHub 安装包 |

### 25.1 实际协议与数据边界

- 本科事实参考：[LoveSzu TimetableApi.java](https://github.com/teleostnacl/LoveSzu/blob/main/Timetable/src/main/java/com/teleostnacl/szu/timetable/retrofit/TimetableApi.java)。个人课表为 `/jwapp/sys/wdkb/modules/xskcb/xskcb.do`，学期参数 `XNXQDM`；当前学期查询为 `jshkcb/dqxnxq.do`。本项目独立实现，没有复制参考项目代码，已加入中英 README 致谢。该参考实现较旧，当前账号实测仍是必要验收条件。
- 本科保留 `KCM / KCH / KXH / SKJS / YPSJDD` 课程、代码、课序、教师及时间地点原文；不猜测拆分周次和节次，不混用研究生规则。登录页、权限错误、无法识别的课程、条数不完整都不会变成空课表。缺少当前学期时允许用户填写校方学期。
- 社区接口依据是学校当前公开页面脚本。本轮实际读取 `/venue-api/booth/list`、`booth/info/{id}`、`boothType/info/{id}`、`booth/{id}/available-time`，与官方脚本的 48 格半小时和开放时段位图核对：1 为空闲，0 为已预约，-1 为不可选，未知值显示未确认。缺日期或不足 48 格明确报错。
- 公开查询走学校校内 HTTP 服务，不携带任何 Cookie 或账号；学校该主机 HTTPS 本轮不可用。带登录状态的请求固定走 `https://swzx.webvpn.szu.edu.cn/venue-api`，不跟随重定向，不降级到 HTTP。匿名请求本人记录实际返回 401，WebVPN 则转到认证页面。
- 预约 Cookie 先通过本人记录接口验证再标记连接；只在内存保管，不写庭院存档、日志或发布包。界面不展示校园卡号、开锁密码、手机号。本人记录支持 30 条分页，读取失败不显示为「没有预约」。
- 预约日期使用深圳时区，以学校返回的开放天数与上限检查。已开始的时段在本应用内不允许选择，界面明确说明；提交时再次通过 HTTPS 核验空位，最终业务权限和每日累计上限由学校裁定。不含抢位、自动循环提交、开锁或管理接口。

### 25.2 验证与交付

- Go 全项目测试及 vet 通过。新增检查覆盖实际半小时状态映射、深圳日期边界、公开请求不带会话、登录清除、权限与非 JSON 区分、空位被占、重复时段、未同意规则、确认凭证复用、提交超时不重试，以及本科原文保留和不完整响应拒绝。
- 前端 47 项通过（含新增预约 5 项、本科展示 1 项）。Windows 实际构建的既有 66 项冒烟通过；另外实测新接口未登录拒绝、无确认凭证拒绝、跨来源拒绝。
- 本轮 Windows 实际成品连学校成功读取 15 个场地、2026-09-21 的真实开放时段；浏览器查询、选中/取消状态正常，桌面和 390px 窄窗无横向溢出，无页面脚本错误。没有用模拟数据冒充这一实网结果。
- 预览包：`dist/szudesktop-booking-timetable-preview-20260920-windows-amd64.zip`，6,999,925 字节，SHA256 `ec2116a1f25e2daedebe97a24ad819ce89b6fe47f767f182c3ed98f13acc3536`。包内 EXE 与构建相同，11,930,112 字节，SHA256 `8d722619454c59982877e9a45ee62c1795c78716b82bdef1ed5d4ca33c09a135`。
- 继续更新 PR #4；内部版本仍 beta0.5.1，明确标为接入预览，不覆盖公开 Release。可宣传的新增能力是「校内社区场地实时查询」，暂不能宣称「本科课表与预约全流程已完成真实验收」。

## 26. beta0.6 公开预发布（2026-09-20）

用户在了解第 25 节真实验收边界后要求发布。本次发布目标为 `beta0.6`，沿用公开测试版标记，不覆盖旧的 beta0.5.1 附件。程序、命令行、页面、关于页和包内说明同步版本号，下载包附 SHA256 校验文件。

- 新版包含文山湖像素背景、彩色状态、30 个像素物件、网络与存档修复、自动校历、社区实时场地/空位查询，以及教务和预约接入测试。
- 真实已验证：校内公开场地和空位查询、官方校历本机读取；上轮 47 项前端检查、66 项 Windows 成品检查及 Go 测试/vet 均通过，PR #4 的测试、跨平台构建与 Windows 构建也全部通过。
- 继续保留 J01、J03、R02–R05：研究生真实登录、本科权限/响应、预约真实会话/提交、自动承接浏览器登录、图书馆独立预约。发布不会把这些条目标成完成；没有代用户提交任何真实预约。
- 发布流程为更新 PR #4 → 检查通过后合并 → 标记 `beta0.6` → GitHub 构建 → 核对公开下载文件及压缩包内程序。公开入口：[beta0.6](https://github.com/SzuDesktopTeam/szudesktop/releases/tag/beta0.6)。

### 26.1 GitHub 发布结果

- PR #4 已合并，发布标签 `beta0.6` 指向 `224ce2a2e42b57de3dca5c93e7fcc7ca4658aec0`；合并后的代码树与通过检查的 PR 完全一致。
- [发布构建 35508307598](https://github.com/SzuDesktopTeam/szudesktop/actions/runs/35508307598) 全部成功，GitHub 已公开预发布，附 8 个文件：Windows 桌面 ZIP、独立 EXE、ZIP 校验文件及 5 个平台/架构的命令行文件。已发布 Windows 成品以 GitHub 构建为准，本机构建的二进制及 ZIP 哈希可因构建环境不同而不同。
- Windows ZIP：6,999,916 字节，SHA256 `e8d189a0a2466a7b80cc6ebccbe5973c32c00b7d656cde838baef497b6f220d4`。
- Windows EXE：11,930,624 字节，SHA256 `c8f285a684db10b55a6637b072e87257078d1b070fa042efc24fc35e4c8b2809`。
- [Windows 下载包](https://github.com/SzuDesktopTeam/szudesktop/releases/download/beta0.6/szudesktop-beta0.6-windows-amd64.zip) · [发布说明](https://github.com/SzuDesktopTeam/szudesktop/releases/tag/beta0.6)。旧版本附件保留，教务与预约的待验收项保持第 25 节状态。


### 26.2 发布后预约入口反馈（2026-09-20）

- 用户反馈「能选时段，但没有预约提交入口」。定位为前端仅在预约会话验证通过后渲染提交表单，未登录时没有明确的下一步按钮；官方网页登录也不会自动同步到应用。这是可用性缺陷，不能用「接入测试」标签代替操作引导。
- 本地修复 R06：未登录时显示已选数量和「连接登录，继续预约」；点击展开并定位预约登录输入，保留时段选择；同时提供「去学校页面预约」。明确提示应用内提交尚未完成真实验证。真正的登录衔接与提交验收仍在 R02–R04，将 R04 提升为 P1。
- 本轮不带任何登录凭据直接请求学校接口，场地列表返回 15 处，2026-09-21 返回 48 格半小时数据；相同匿名请求访问本人预约接口返回 HTTP 401。公开查询可用不等于账号有预约权限。
- 验证：6 项预约前端检查通过；Windows 重新构建，确认内嵌预约模块与源码一致。实际窗口连学校查询后选中一个空闲时段，点击新入口成功展开登录并聚焦输入，已选时段仍为 1，未发生横向溢出。没有发送真实预约请求。
- 交付为本地 `dist/szudesktop-windows-amd64.exe`，尚未提交或更新 GitHub 的 beta0.6 安装包。


### 26.3 预约回归官方页面交互（2026-09-20，已随 beta0.6.1 发布；服务端提交端点未一并撤下，见 F23）

- 用户明确指出 F12 → 复制 Cookie → 粘贴会话的流程不适合普通同学，并提出「应用承载学校原页」或「读取接口、自行展示」。采用分工：公开空位由本机读取并渲染，登录、预约及结果确认交给学校原页面；应用内承载官方网页作为 R04 继续推进。
- 当前界面撤下预约 Cookie 输入、F12 教程、实验性本人记录和本地提交表单。主入口为「登录并预约」，明确在浏览器打开学校官方页面。空位改为静态状态格，不再允许在无法提交的本地表单前选择时段。图书馆仍使用独立官方入口。
- iframe 可行性原型未通过：当前浏览器中的内嵌页面一直空白，直接打开则导航到学校统一认证页；未完成二次认证及预约，不将空白直接归因为校方限制。原型未加入产品。原生 WebView 的完整承载仍未实现，不能把本轮外链调整称为应用内预约已完成。
- 预约服务端适配代码及测试保留，前端不再调用会话、本人记录或提交接口；后续接入正常登录前不重新开放给普通用户。本轮不改成绩和教务模块的登录方式。
- 验证：预约前端 6 项、校园业务 9 项通过；Windows 重新构建并核对内嵌模块。实际页面读取到 15 个场地及 2026-09-21 的 20 个开放半小时时段，静态空位展示正常；预约卡片内无 Cookie 输入或可选时段、无横向溢出、无控制台错误。没有提交任何预约。
- 成品仍为本地 `dist/szudesktop-windows-amd64.exe`。中英 README 当时明确这是尚未发布的源码调整；GitHub beta0.6 原下载包未被覆盖。第 26.2 节的「连接登录」临时入口已由本节方案替代。**后续更正**：本节的前端调整已随 beta0.6.1 发布，但服务端 `/api/booking/{session,prepare,commit}` 并未一并撤下，见 F23 与第 36.2 节。


## 27. 学校公告按学院查看（2026-09-20，已随 beta0.6.1 发布）

用户提出按学院查看学校公告。本轮按**发布单位的公开栏目**分类，不根据标题关键词猜学院，也不把未读取的院系显示成空公告。

| 工作 / 剩余问题 | 重要性 / 难度 | 当前结果 |
|---|---|---|
| 学院来源选择与自动读取 | P2 / M | 已完成本地实现。来源分为「全校通知」「学院与学部」，收录 28 个院系入口，另保留教务部和研究生院。选择可读学院后自动读取，原页链接与学院同步变化 |
| 公开栏目日期与链接适配 | P2 / M | 17 个学院栏目已通过本轮下载的真实 HTML 样本核对。支持标题外侧日期、分行年月日、中文月份及可省略的列表结束标签；优先取发布日期，避免拿摘要中的活动日期替代。使用已有依赖版本的 HTML 解析器 |
| 其他 11 个院系应用内读取 | P2 / M–L | 目前仅提供官方入口。计软网站本轮对直接请求返回 412；外国语学院的公开教务栏目没有可核对发布日期；其他院系尚未完成栏目适配。没有绕过校方限制，也没有宣称全部学院已接入 |
| 公文通及需登录的校内通知 | P2 / L | 未接入。本轮只读取公开页面，不包含需登录内容；也不包含学院所有历史通知或全部外链公告 |

实现与验证：

- 院系入口核对来源：[深圳大学学部学院目录](https://www.szu.edu.cn/yxjg/xbxy.htm)。公告地址来自对应院网实际链接。来源集中维护于 `desktop/internal/ui/notice_sources.go`，前端通过只读来源列表接口加载，避免前后端各维护一份名单。
- 保留原有固定来源限制、10 分钟缓存、失败提示和原文链接。各院系缓存隔离；切换学院立即清除旧内容，较早请求晚到时不能覆盖当前学院结果。最多展示当前栏目首页 12 条可核对日期的本站公告，未将外链或历史全量检索计为已完成。
- 直接读取的 17 个来源：教育学部、马克思主义学院、经济学院、法学院、心理学院、体育学院、数学科学学院、物理与光电工程学院、化学与环境工程学院、生命与海洋科学学院、机电与控制工程学院、材料学院、人工智能学院、建筑与城市规划学院、政府管理学院、高等研究院、国际交流学院。其中学生工作、本科教务或研究生教育等子栏目在页面上单独注明。
- 公告前端 5 项、校园业务 9 项检查通过；UI 后端全包测试及 vet 通过。日期格式、相邻文章不串日期、不同学院缓存隔离及未接入来源提示有回归检查，新增公告检查已加入 CI。
- Windows 已重新构建，实际成品的来源接口返回 30 个来源（28 个院系，其中 17 个直读）；内嵌公告模块与源码一致。实际浏览器选择教育学部读到 11 条、材料学院读到 8 条；切换计软后显示官网入口并清除前一学院公告。无控制台错误，无横向溢出，已检查筛选栏实际排版。
- 本地成品：`dist/szudesktop-windows-amd64.exe`。此次源码提交包含学院公告筛选、预约体验调整及中英文 README 同步更新；未推送或覆盖 GitHub beta0.6 下载包。


## 28. beta0.6.1 发布（2026-09-20）

- 发布范围：学院/部门公告筛选（28 个学院与学部入口、17 个可直接读取栏目）、预约只读空位预览与学校原页办理入口。
- 桌面界面、命令行版本、中英文 README 和压缩包内快速开始说明统一更新为 beta0.6.1。
- 发布验收：由 GitHub Actions 执行回归、Go 检查与测试、Windows 构建和整机冒烟，再生成桌面包及五个平台命令行程序；任务完成后核实产物。
- 本次发布不代表课表/成绩真实账号验收或应用内预约已经完成。

- 发布结果：GitHub Actions [35514050275](https://github.com/SzuDesktopTeam/szudesktop/actions/runs/35514050275) 全部成功，包含 Windows 整机冒烟。公开预发布 [beta0.6.1](https://github.com/SzuDesktopTeam/szudesktop/releases/tag/beta0.6.1) 已上传 8 个文件；标签对应 `20fbe2a`。
- GitHub 发布文件摘要：Windows ZIP SHA256 `b29359fe33add5a37963dc187461f35723c19a92586b40cb03ec1e35388d28e0`；EXE SHA256 `59bfccdc0a6274eccdbb75576ba9c054edf6e7b20b31e3557c73aa245a28e5c5`。

## 29. 工程债清理：版本号单一来源、开机自启接线、首次引导、macOS 钥匙串与统一验收标注（2026-09-21，已随 beta0.7 发布）

本轮做用户选定的「工程收尾与体验补齐」。不改已发布附件，内部版本仍为 beta0.6.1。

| 编号 | 问题 / 工作 | 重要性 / 难度 | 本轮结果 |
|---|---|---|---|
| X05 | 版本号散在 6 处（两个 main.go、页面 meta、顶栏徽章、关于页、打包脚本），改一处忘一处 | P2 / S | 已完成。唯一来源 `internal/version/VERSION`，Go 用 go:embed 编进二进制（不依赖 ldflags，用户自己 `go build` 拿到的也一致）；页面顶栏、meta 和关于页改由 `/api/status` 的 `app_version` 填充；`build-windows.py`、`make_release.py` 读同一个文件，读不到就报错退出，不再回退成 `0.0.0`。前端源码里再写死版本号会被 `check-ui.mjs` 和冒烟测试拦下 |
| X02 | CLI 有 `autostart`，桌面设置页看不到也改不了 | P2 / M | 已完成。注册表逻辑从 `cmd/szunet` 的 package main 抽到 `internal/autostart`，命令行与桌面共用；新增 `GET/POST /api/autostart`，设置页显示真实状态并可开关。顺带修掉一处静默降级：原来读不到注册表就显示「未开启」，现在区分「未开启」与「状态未知」 |
| X01 / Q05 / U15 | 首次打开功能很多，不知道先做什么 | P2 / M | 已完成初版。首次启动弹一张四段短引导（这是什么、数据只在本机、怎么退出、先做什么），按钮或 Esc 都能关，状态写进 `preferences.onboarded`，不会每次打开都弹；保存设置不再把这个标记丢掉 |
| R06 | macOS 钥匙串读取失败被当成「未保存」，删除错误被忽略 | P2 / M | 已完成。`security` 退出码 44 / errSecItemNotFound 才算「没有这一条」；钥匙串被锁、授权被拒、命令缺失一律报 `ErrSessionStorageUnavailable`，删除失败不再报成功。判断逻辑放在无构建标签的 `keychain_error.go`，Linux CI 也能测到；真机 macOS 仍未现场验收 |
| — | 「接入测试」「实验功能」各页措辞不一，用户看不出哪些结果能信 | P1 / S | 已完成。统一标记收敛到 `desktop/assets/garden/labels.mjs`，研究生教务、本科课表、在线成绩三处用同一个 `接入测试 · 未经真实验收` warning 徽章（带悬浮说明）；已真实验证的官方校历保持 success 色，不混用 |
| F21（本轮新发现） | macOS 保存凭据时把密码 JSON 当命令行参数传给 `security -w`，本机其他用户或进程用 `ps` 就能看到 | P0 / M | 当时记为待做，见第 3.1 节 F21。Linux 版第 17 节已改成只经标准输入，Windows 走 DPAPI，只有 darwin 这条还漏着。**后续更正**：已于第 33 节修复并随 beta0.7.1 发布（不属于 beta0.7 的范围）；macOS 真机验收仍未做，见 F25 |

### 29.1 验证

- Go：`go vet ./...` 与 `go test ./...` 全部通过。新增 `internal/version` 版本号格式测试；`internal/credential` 钥匙串错误分类测试（4 项子测试，用真实子进程造退出码，不手工拼 `ExitError`——它的 `ProcessState` 为 nil 时 `ExitCode()` 会 panic）；`desktop/internal/ui` 开机自启 4 组测试（跨来源 403、PUT 405、缺字段与坏 JSON 400、开关按当前状态取反、写失败不得报成功、状态未知不得显示成未开启）。测试一律注入替身，不碰真实注册表。
- 交叉编译：windows / darwin-arm64 / linux-amd64 `go build ./...` 通过，darwin `go vet` 通过。
- 前端：10 个检查脚本共 65 项通过（新增 `check-autostart-ui.mjs` 5 项、`check-ui.mjs` 2 项、`check-network-ui.mjs` 1 项、`check-workspace-ui.mjs` 4 项），已接入 CI。
- Windows 成品：重新构建后 `smoke_windows.py` 74 项全部通过，含新增的「exe 自报版本 = VERSION 文件」「/api/status 版本一致」「页面不含写死版本号」「开机自启状态可读且拒绝跨来源 / PUT / 坏 body」。冒烟只读开机自启状态，绝不 POST 切换，避免改掉跑测试那台机器的启动项。
- 真实浏览器（内嵌 CDP 页面）实测：顶栏显示 `beta0.6.1 · 非官方应用`、关于页显示 `szuDesktop beta0.6.1 · 荔枝庭院`、设置页开机自启卡片读到真实注册表状态「未开启」并给出「打开开机自启」按钮、学习工具页三处统一徽章与悬浮说明正确、无控制台报错。
- **未验证 / 环境限制**：该内嵌浏览器不派发 `<dialog>` 的 `close` 事件——用一个不含任何项目代码的最小 dialog 复现：表单提交和 `dlg.close()` 都不触发事件，但 `returnValue` 与 `open` 变化正常；同时视口为 0×0、截图不可用。因此「点按钮关掉引导后写进存档」没能在浏览器里端到端跑通，改为手动派发 `close` 验证处理链路（存档 revision 1→2、`onboarded` 变 true、`theme` / `motion` 未被改动）。现有 `confirm()` 依赖同一事件机制，此前已在真实浏览器验收过。本轮也没有做 1366×768 / 390×844 布局走查。开机自启的实际开关会真的写注册表，留给用户自己点。

### 29.2 交付与仓库状态

- 本轮当时为源码改动：未打 tag、未发布、未覆盖 GitHub 上 beta0.6.1 的附件。`dist/szudesktop-windows-amd64.exe` 只用于本轮验证。**后续更正**：本节内容已随 beta0.7 发布（见第 31.4 节）。
- 分支清理：`feat/beta05-lychee-garden`、`fix/desktop-state-review`、`fix/restore-pixel-campus`、`fix/session-score-safety` 的本地与远端副本已按用户确认删除。前两个内容已完全并入 main；后两个各含一个被 main 上新版本取代的旧提交，直接合并会回退代码与文档（`fix/session-score-safety` 合并将删掉 3737 行，含 F20 修复）。仓库现在只有 main。

## 30. 仓库瘦身与背景呈现修复（2026-09-21，已随 beta0.7 发布）

用户要求清理冗余文件（本地与云端）并指出背景仍不好看。清理范围由我判断，删除项与理由如下。

### 30.1 仓库里删除的内容

| 项 | 体积 | 为什么删 |
|---|---|---|
| `desktop/assets/art/`（33 个文件） | 279KB | 构建与页面都不引用（构建只打包 `garden/` 与 CSS 引用的字体）；其中 `junimo-*`、`dwarf.png`、`m1–m4.png`、`bird.gif`、`coursor.png` 是未核实的旧游戏素材，属 F11 的许可风险；`flora/` 与运行时用的 `garden/flora/` 重复 |
| `fonts/svbold.ttf`、`svthin.ttf` | — | 从第三方仓库抓的星露谷字体，构建早已排除，但一直留在公开仓库里 |
| `design/` 下 25 个一次性脚本 | — | 抓素材 / 协议探针 / 截图 / 切片工具，构建与 CI 都不引用。保留 4 个素材生成器：`gen_icon.py`（构建脚本提示）、`gen_flora.py`（自绘花草来源）、`gen_libao.py`、`gen_avatar.py`（角色与头像 SVG 来源） |
| `design/vpn-notes.md` | 5KB | 学校 VPN 认证流程笔记，不宜放在公开仓库；移到本地 `.local-docs/`（已忽略）并更新 `internal/vpn/vpn.go` 里指向它的注释 |

顺带修掉两个生成器的硬编码绝对路径（`D:\szudesktop\...`）：`gen_flora.py` 默认输出改为运行时目录 `assets/garden/flora`，`gen_libao.py` 改为从脚本位置推导 `index.html`。

### 30.2 背景呈现

- **量化**：`campus.png` 从 1672×941 / **2.68MB** 压到 **694KB**（128 色 PNG-8，-74%）。像素画在放大对比图上看不出差别，q64 才会让湖面反光发平，故选 128 色。嵌入资源总量 3840KB → 1785KB，Windows 成品 11.6MB → **9.6MB**。
- **压暗**：`body::before` 从 4% 平铺薄纱改为竖向渐变（顶部 8%，250px 处 13%，460px 后 40–45%）。顶部保留清晰的文山湖场景给木牌，进入内容区后背景退到纸面卡片后面。原因：原先整屏高饱和细节与卡片抢注意力，纸面与背景之间是硬边。
- 未改动美术本身；`看看像素校园` 大图浏览读的是原图，不受压暗影响。若之后仍觉得画面不好看，再按「黄昏暖调、低饱和、中间留白、去掉游戏梗物件」重新生成。

### 30.3 本地与云端

- 本地删除 `dist/` 里的历史包（0.2.0 → beta0.6、各预览包、旧平台二进制）、`outputs/`、`.tmp-campus/`、`.scratch_probe/szunet-test.exe` 与 `shots/`，合计 244MB → 38MB。保留当前构建的 `dist/szudesktop-windows-amd64.exe`、`.local-docs/`（冻结证据）与 `.scratch_probe` 其余部分（预约登录衔接与本科课表还没做完，里面可能还有可复用的接口样本）。
- 云端删除 GitHub Actions 的历史构建产物 154 个 / 133MB；**Release 附件不受影响，8 个预发布全部保留**作为交付历史（其中 beta0.1–0.4 虽旧，仍是可下载的交付记录，删除会让用户拿不到旧版，未做）。

### 30.4 验证

- 65 项前端检查、`go vet`、`go test ./...`（8 个包）全部通过；`build-windows.py` 重新构建成功，`smoke_windows.py` 74 项通过（含「旧游戏素材未打包」「旧游戏字体未打包」两条仍为 404 的断言）。
- 浏览器实测：`body::before` 计算样式确认为新渐变、`z-index:-1` 不变；页面引用的 `campus.png` 实际 694KB；无控制台报错。**没有真实截图**：本环境内嵌浏览器视口 0×0，截不了图，压暗效果是按同一组参数用 Pillow 合成的示意图确认的，真实观感请在打开应用后自行判断。

## 31. beta0.7 发布：真机浏览器核验与门面更新（2026-09-21）

用户在清理与背景修复后要求「能优化的继续优化并发布」。本轮先把一直缺的**真实浏览器核验**补上，再发版。

### 31.1 核验手段的变化

这个环境里没有 Chrome，但装了 Edge（Chromium 153）。`--screenshot` 命令行参数在 Edge 上不生效（不报错也不出图），于是改用 CDP：`.scratch_probe/snap.mjs` 直连 `--remote-debugging-port`，用 Node 自带的 fetch/WebSocket 驱动，可设视口、跑 JS、截全页图。此前记录在案的「视口 0×0、截不了图」限制因此解除。

### 31.2 核验结果

- **首次引导端到端通过**（第 29 节遗留的未验证项）：真实浏览器里首次打开弹出引导 → 点击按钮 → 对话框关闭 → 存档 `preferences.onboarded` 由 false 变 true、revision 1→2、`theme` 未被改动。此前只能手动派发 `close` 事件验证处理链路，现在事件与存盘都实测了。
- **布局**：1366×768、1440×900、390×844 三个视口 `scrollWidth == clientWidth`，无横向溢出；页面高度 1624 / 2267 px，自然滚动到底部。
- **界面实际观感**：确认压暗层生效——顶部文山湖场景保持清晰，内容区背景明显后退，纸面卡片与木框导航读起来干净。宠物肚皮的白色方块经查是 `cat-happy` 符号里设计的 `#FFF6E0` 色块，与整体方块风格一致，不是漏填色，未改。

### 31.3 门面：README 预览图

仓库里的三张预览图还是 beta0.5 时代的旧界面（旧背景、旧版本号徽章、旧卡片布局），与当前界面完全不同。本轮用真实截图替换：

- `docs/screenshot-desktop.png`（今日）、`docs/screenshot-garden.png`（荔枝庭院）、`docs/screenshot-login.png`（校园网）
- 1440 宽整页捕捉，量化到 256 色后约 283–285KB，与旧图体积相当；量化前后的文字对比图确认可读性无差别。

### 31.4 发布内容（beta0.7）

- 版本号单一来源 `internal/version/VERSION` 升到 `beta0.7`；页面顶栏、关于页、打包脚本与快速开始都从它取。
- 相对 beta0.6.1 的用户可见变化：设置页可开关开机自启并显示真实状态；首次打开的短引导；成绩/课表/在线读取统一为「接入测试 · 未经真实验收」徽章；背景压暗与整图量化（Windows 成品 11.6MB → 9.6MB）。
- 仓库层面：删除未核实的旧游戏素材与第三方字体（F11 风险收窄）、25 个一次性脚本、VPN 协议笔记移出公开仓库。
- 仍未完成、不因本版改变：本科/研究生真实账号验收、应用内预约衔接与提交、余额/流量数据层、实验 VPN 的 F06/F07/F08。

### 31.5 发布结果与附件核对（2026-09-21）

- 标签 `beta0.7` 触发 [CI 35562728027](https://github.com/SzuDesktopTeam/szudesktop/actions/runs/35562728027)：test、5 个平台 CLI、Windows 桌面构建、release 全部成功。
- 公开 [beta0.7](https://github.com/SzuDesktopTeam/szudesktop/releases/tag/beta0.7) 为预发布，8 个附件。已从 GitHub 下载核对：
  - ZIP 5,003,257 字节，SHA256 `6f6f4957ffa655713af90eb6445471cbc6a406f83266ba017e1cf08a88eae1c6`（比 beta0.6.1 的 7,073,704 字节小约 2MB，主要是背景图量化的结果）
  - 独立 EXE 10,049,024 字节，SHA256 `85da336c7d867829d427c2cd4bca8b7186bee0b4e98eeef28f76466df2b74d65`，与 ZIP 内 `szudesktop.exe` 逐字节一致
  - 包内程序与独立 EXE 自报版本均为 beta0.7；包内快速开始首行为 `szuDesktop beta0.7 · 荔枝庭院（Windows x64）`
- **发布后发现并修复**：`.sha256` 附件带 CRLF——CI 的 windows 任务里 `Path.write_text` 文本模式把 `\n` 翻成 `\r\n`，Linux / macOS 用户执行 `sha256sum -c` 会因为行尾的 `\r` 直接报错（coreutils 会把它当成文件名的一部分）。`make_release.py` 已改为 `newline="\n"`，今后不再出现；beta0.7 的这个附件同时替换为 LF 版本。**ZIP 与 EXE 未改动、哈希值不变**，替换后用线上文件实测 `sha256sum -c` 通过。

## 32. 当前总清单（2026-09-26 更新）

本节统一维护优先级、难度和剩余验收；不另建任务清单。完成度按证据区分代码完成、实际窗口检查与学校账号验收。

本轮范围决定：用户暂不做部署。1.0 先完成 Windows 桌面版及已经提出的校园业务，执行清单统一见第 50.2 节；校内后端、Docker 部署和服务器运维延期，不作为正式版前置。

| 顺序 | 重要程度 / 难度 | 事项 | 当前状态与下一步 |
|---|---|---|---|
| 1 | P0 / S | 合并必须通过 test；同步中英 README | 已配置 main 必需检查 test，保留原保护；两种 README 同步。新增 PR/问题模板 |
| 2 | P1 / M | 中文校历 OCR | 已整合 #10；优先中文、全乱码提示、保留缓存。英文用户语言的完整环境待对应设备复核 |
| 3 | P1 / S–M | 常驻宠物、托盘、大小/动作与伙伴选择 | beta0.9.2 已发布四伙伴和双向切换，旧存档补齐、真实窗口同步、重启恢复、窄窗和跨版本安装检查通过；物理多显示器待设备 |
| 4 | P1 / L | 学校原页面在应用内登录与预约 | 已修复 WebVPN 认证域名漏放行；本人已完成二次验证。对照浏览器能选时段并打开确认表单，未提交；原生详情为空且连接仍不稳定，完整流程及会话接回未通过。认证跳转修复已随 beta0.9.1 发布 |
| 5 | P1 / L | 本科/研究生课表与成绩 | 研究生备用登录、空排课与待安排课程已有证据；非空排课、安装版会话接回、本科权限和真实成绩仍待验收。beta0.9.2 已补完整分页、40 秒总时限和取消；缺总数或分页失败不假报完整 |
| 6 | P2 / M | 学院琴房只读查询 | 已整合 #11，密码用后清空、分页、切换失败清旧登录；本轮连接在登录前被关闭，不能据此判定账号权限。仍需可达网络及该系统授权账号，不同于社区共享琴房 |
| 7 | P1 / M | beta0.9.2 安装包交付 | 已合并 #17、发布标签与 10 个附件；PR 和最终发布检查通过，实际下载安装包/便携 ZIP 校验一致，详见第 51 节 |
| 8 | P2 / M | 其余 11 个学院公告栏目 | 仍为官方入口，逐个核实公开 HTML 后适配 |
| 9 | P2 / L | 图书馆选座、余额/流量、成绩自动并入统计 | 后续扩展；需要官方接口与权限证据，不能套用另一系统登录成功。成绩分页取全属于已有成绩功能的完整性问题，纳入第 50.2 节正式版清单 |
| 10 | P2 / M | 安装版自启、宠物通知与常驻偏好 | 后续工作；当前只支持手动开启，尺寸记忆已做，宠物隐藏状态尚不跨重启记忆 |
| 11 | P2 / M | 校内公共后端首期 | 按用户本轮决定延期部署，不作为 1.0 前置。第 9 节和第 50.3 节保留公共内容方案，尚未建设或部署 Docker/服务器流程 |
| 12 | P1 / L | 1.0 范围与交付收口 | 第 50.2 节维护十项任务及完成情况；四伙伴已交付，本次升级已验证，学校业务与 1.0 最终候选包仍有待办。不把部署与远期扩展设为发布前置 |
| 13 | P2 / L | 内容刷新与程序自动更新 | 自动更新尚未实现；数据/素材刷新与程序整包更新分开规划。当前安装包未签名，beta0.9.1 → beta0.9.2 手动升级已实测；先版本提示，再安排可信下载、重启安装和恢复方案，见第 50.4 节 |

### 32.1 设计与验证约束

- 安装版窗口层为 Electron，Go 保留业务和存档；便携版继续 Edge/Chrome 窗口，不再描述为「尚无 Electron」。
- 学校页面使用独立内存会话、无 Node/预加载桥、不开启权限、不改学校 CSP。主界面只持有选定业务名称；会话在主进程与本机 Go 服务之间传递，仅对学校 HTTPS 业务发出。普通安装版隐藏复制 Cookie 流程；便携版保留折叠的备用入口。
- 认证成功必须验证个人业务，不以 HTTP 200、页面打开、Cookie 存在或学校当前学期公共信息作为成功证据。验证码/MFA 在学校页面由本人完成。没有具体预约指令时仅查询，发布验收也不占用真实场地。
- 宠物采用本地像素立绘全身动画，30 秒刷新庭院状态。0.4–2.0 倍缩放持久化在安装版配置目录；当前显示器工作区定位，系统减少动态效果生效。本机仅一块显示器，不把几何测试称为多屏实测。
- 所有任务和验收结论留在本文；过时缩放实施清单已并入本节，历史内容可从 Git 找回。

### 32.2 保留的工程与来源事项

- 实验 VPN 的 F06/F07/F08（状态、超时、证书）与 F11 来源尚未闭环；它们不在默认发布构建中。
- 组织级默认权限、成员建仓权限、额外邀请等没有在本轮改变；不把新增协作者自动视为需要扩大权限。
- main 保护现为 PR、0 必须批准、test 成功且分支更新、禁止强推/删分支；本轮不通过管理员绕过检查。
- `roles` 为 null 时不推断预约资格；学校裸图片文件名不猜 URL。完整记录保留在对应历史章节。

## 33. F21 修复：macOS 凭据不再经命令行参数暴露（2026-09-21，已随 beta0.7.1 发布；macOS 真机仍未验收，见 F25）

**问题**：darwin 的保存路径用 `security add-generic-password ... -w <密码 JSON>` 写入，
密码会出现在子进程的命令行参数里，同机其它进程 `ps` 就能看到。Windows 走 DPAPI、
Linux 早已改成只经标准输入，只有 macOS 这条漏着，而 `szunet-darwin-*` 是随发布分发的。

**依据**：Apple man page 对 `-w` 的说明是 "Put at end of command to be prompted (recommended)"，
即官方推荐让 `security` 提示输入，而不是把密码写进命令行。提示走 `readpassphrase(3)`，
它在打不开 `/dev/tty` 时会退回读标准输入。

**做法**（`internal/credential/keychain_prompt.go` + `nocontrollingtty_*.go`）：

1. 执行 `security add-generic-password -a <账号> -s <条目> -U -w`，`-w` 放最后且不带值；
2. 子进程脱离控制终端（`Setsid`）。否则命令行版在终端里跑时，子进程会去读用户的键盘，
   我们喂进去的管道没人读，保存会卡住等输入；
3. 密码连同换行从标准输入喂入；命令行参数里只有账号与条目名，没有机密；
4. **写前自检**：先用一次性条目（`szunet-selftest-*`）跑一遍「写 → 读回 → 比对 → 删除」，
   确认这条路在本机可用。自检不过就明确报错，绝不退回 argv 写法，也不碰用户的真实凭据
   （直接在真实条目上试错的代价是：万一没喂进去，留下的就是空值或错值）；
5. **写后校验**：读回比对，不一致就报错，不静默成功。自检结果只缓存成功——
   失败不缓存，用户解锁钥匙串后重试还能成功。

**验证**：

- 可移植测试 5 项（`keychain_prompt_test.go`，用假命令）：密码不进 argv、存入值与读回一致、
  自检条目用完即清、自检不可用时拒绝保存且不写真实条目、读回不一致必须报错、
  自检只在成功后缓存。本机 Windows 实测通过。
- unix 真命令测试（`keychain_prompt_unix_test.go`）：把 `securityBin` 指向假脚本，
  走真实 exec（含 `Setsid` 与标准输入管道），断言命令行参数里没有密码、
  值经 stdin 完整送达、且确实执行了读回校验。由 Linux CI 执行。
- 交叉编译 darwin / linux / windows 通过；`go vet`（含 darwin 目标）通过；
  Windows 成品重新构建后 74 项冒烟通过。
- **未验证**：真实 macOS 上 `security` 的提示输入是否确实读标准输入（`readpassphrase` 的退回行为）。
  若不成立，`szunet config set` 在 macOS 上会**明确报错**而不是静默泄漏——这是有意的取舍：
  宁可失败并让用户知道，也不要偷偷退回会被 `ps` 看到的写法。真机验收前不要把这条记成「已现场验证」。

## 34. beta0.7.1 发布：macOS 凭据暴露修复（2026-09-21）

只有一个安全修复，按 beta0.5.1 的习惯用修复版号而不是 beta0.8。

- 标签 `beta0.7.1` 触发 [CI 35577757052](https://github.com/SzuDesktopTeam/szudesktop/actions/runs/35577757052)：test、5 个平台 CLI、Windows 桌面构建、release 全部成功。公开 [beta0.7.1](https://github.com/SzuDesktopTeam/szudesktop/releases/tag/beta0.7.1) 为预发布，8 个附件。
- 发布说明这次是手工补的：CI 用 `generate_release_notes` 只会生成一行 compare 链接（本项目是直接提交、没有 PR），所以用 `gh release edit --notes` 写清了「相对 beta0.7 的变化」「macOS 未真机验证的边界」「下载项说明」。
- 附件核对：
  - `.sha256` 附件行尾为 LF（`cat -A` 无 `^M`），值 `aa2cc634faa938a3e7a3582bb2e9bed792bb5a43431ce0f9a50835f0253b754b`；上一版修复的 `newline="\n"` 生效。
  - 独立 EXE 10,049,024 字节，SHA256 `4cdc5ca78d538597564ba96abf454c0151093eded61e587ad56fb5fd7fbdee9d`，运行后自报 `szuDesktop beta0.7.1`。
  - `szunet-darwin-amd64`：本地用同一提交交叉编译（`-trimpath -ldflags "-s -w"`）得到 6,815,600 字节，与线上附件**大小完全一致**；未 strip 的同一构建里能查到 `keychainSave`／`promptWrite`／`promptRead`／`ensurePromptWriteWorks` 符号，二进制内含 `beta0.7.1` 与 `szunet-selftest-` 字符串，确认 F21 的修复确实进了 macOS 命令行版。
  - **ZIP 核对**（网络一度不通，重试后补齐）：首次下载时 GitHub 资源站不可达（github.com 与 release-assets 均 TLS 超时，只下到 2.1MB / 5.0MB 就停滞），期间先用同一次 [CI 运行](https://github.com/SzuDesktopTeam/szudesktop/actions/runs/35577757052) 的构建产物交叉核对（内层 ZIP 哈希与线上 `.sha256` 声明值一致、包内 exe 与线上独立 exe 逐字节一致）。网络恢复后从发布页重新下载，完成完整核对：
    - `sha256sum -c` 通过（`.sha256` 为 LF）——这是上一轮 `newline="
"` 修复后的第一个新版本，Linux / macOS 用户不会再遇到行尾 `
` 导致的校验失败
    - 包内 `szudesktop.exe` 10,049,024 字节、SHA256 `4cdc5ca7…`，与线上独立 EXE **逐字节一致**，运行自报 `szuDesktop beta0.7.1`
    - 包内文件为 exe + 快速开始 + 字体许可 + 项目许可

## 35. 真实校园网验收：能测的都测了，并抓到一处 CLI 与桌面端不一致（2026-09-21）

用户提出「我们现在就在校园网环境，能不能直接测」——可以，而且**不需要账号**的那几条本轮已全部实测完成。

### 35.1 实测结果

| 验证项 | 结果 | 说明 |
|---|---|---|
| `detect` 判区与协议指纹 | 通过 | 判区=已联网；**深澜握手指纹=是**（真实 SRun 协议指纹在这张网上可用）、ePortal 指纹=否；给出「真掉线时按教学区协议登录」的结论正确 |
| `diag` 诊断 | 通过 | 证据与建议一致，第一句就是「只想上网不用做任何事」 |
| `status` 在线状态 | **先发现缺陷，修复后通过** | 见 35.2 |
| 社区场地列表 | 通过 | 15 个真实场地（时光/文山湖/沧海社区的共享琴房、会议室、网络面试间），**未携带任何 Cookie** |
| 空位时段 | 通过 | 2026-09-22 共享琴房1：20 个时段，**18 已预约 / 2 空闲**——真实分布，不是默认值 |
| 公开公告 | 通过 | 教务部 5 条真实公告，带发布日期与原文链接 |
| 官方校历 | 通过 | 两个学期，第 1 周起始日与人工核实一致 |
| 界面实测（真实浏览器 + 真实数据） | 通过 | 校园服务页场地列表 15 项、下拉与日期控件可用，状态行显示「已从学校读取 15 个场地」 |

顺带复现了「关闭最后一个窗口约 10 秒自动退出」：截图脚本关掉页面后，进程按设计自行退出。

### 35.2 抓到的缺陷：CLI 没账号时跳过在线查询（F22，已修）

- **现象**：同一台机器、同一时刻、同样没有保存账号——桌面端 `/api/status` 报 `online: true`，而 `szunet status` 报「账号状态: 没查到（没有可用的账号密码…）」。
- **原因**：`cmdStatus` 里是 `if credErr == nil { 查询 }`。门户查询本就按出口 IP 回答、与账号无关（桌面端正是这么调的），这个短路让没存账号的用户永远看不到在线状态——正是 F20 记录过的「用户只看到『没查到』而误判掉线」，当年桌面端修了、CLI 漏了。
- **修复**：去掉短路，`status` 与桌面端共用同一查询入口（新增 `statusOnline` 与可替换的 `queryOnline` 便于测试）；JSON 输出补 `online_known`；没账号且离线时补一句登录提示，但状态照报、不再拿「没查到」当结论。
- **验证**：新增 2 项 Go 测试（无账号仍查询、校外不查）；真实网络重跑——同一张网、同样无账号，修复后输出：

```
账号状态: 已在线
在线 IP : 172.27.47.91
在线设备: Mac OS · 172.27.47.91
```

### 35.3 仍需账号、只能由用户本人操作的验收

- **校园网真实登录 / 注销（教学区深澜）**：需要校园卡号与密码。当前出口已有一个在线会话，登录会命中 `ip_already_online` 分支——这本身就是对 F20 的现场验证（不得报成功、不得缓存 `ac_id`、应把在线设备信息带出来）。验收顺序建议：先在有会话时点登录看提示 → 再注销 → 再登录。
- **教务登录 / 成绩 / 课表（ehall）**：需要账号或按业务粘贴 Cookie，凭据只能由用户在本机应用界面输入；我只负责核对结果（会话长度不回显、磁盘无明文、只读第一页、过期与无权限分开报）。

## 36. 全项目严重问题复核（2026-09-21）

复核方法：**不沿用旧结论**，每条都回到代码与 CI 配置取证，给出 `文件:行号`；严重度按第 2 节的 P0–P3 判定，影响面与发生条件分开写。基线 HEAD `99a0b3a`，`internal/version/VERSION` = `beta0.7.1`，`go mod` 路径 = `github.com/SzuDesktopTeam/szudesktop`。本轮实跑 `go vet ./...` 退出码 0、`go test ./...` 12 个包全绿（8 个有测试）。

### 36.1 F11：未核实的「独立编写」保证仍随仓库公开（P0，完成度下调）

- **证据**：`internal/vpn/vpn.go:3-4` 包注释写「本包为**依据公开协议行为分析独立编写**的实现……代码为自行编写，**未复制任何厂商或第三方的源代码**」。`git ls-files internal/vpn/` 返回 8 个文件，全部被公开跟踪。
- **已核实为真的那半条**：实验 VPN 确实**不在任何发布件里**。`internal/vpn` 的唯一引用点是 `desktop/internal/ui/vpn.go:19`，该文件带 `//go:build campusvpn`（`vpn_disabled.go:1` 是 `!campusvpn` 的替身，返回「当前发布版不包含实验 VPN 协议」）；`release.yml`、`desktop/build-windows.py`、`Makefile` 都不含 `-tags`。所以这一条的影响面是**公开的法律/来源声明**，不是运行时安全。
- **为什么仍按 P0 记**：F11 的验收条件原文是「删除未经核实的独立实现保证」，而这句话还在公开仓库里；第 2 节把「代码许可来源」明确列为 P0。「部分完成」应读作「一半没做」。
- **修复与验收**：把包注释改成不含来源保证的中性表述（例如「协议流程依据公开交互行为整理，来源与授权尚未核实，不得用于商业用途」），或取得授权证据后再保留原句。验收：`grep -rn "未复制任何厂商" internal/` 无命中；`go build ./...`、`go vet ./...` 通过；`-tags campusvpn` 也能编译通过。

### 36.2 F23：预约的服务端写操作端点仍在发布包里（P0，文档与代码相反）

- **证据链**：
  - `desktop/internal/ui/server.go:312-315` 注册 `/api/booking/session`（GET/POST/DELETE）、`/api/booking/prepare`、`/api/booking/commit`；
  - `booking.go:306-338` 的 session POST 接受用户粘贴的预约 Cookie（`booking.go:320-323`）；
  - `booking.go:415-443` 的 commit 校验一次性 token 后**真的向学校提交**：`b.request(ctx, "/boothReservation/add", nil, payload, true, nil)`（:439），成功回「学校已接受预约请求」；
  - 前端已完全不用它：`grep -rn "booking/prepare|booking/commit|booking/session" desktop/assets desktop/index.html` **无命中**，`booking.mjs` 只打开学校原页；
  - 文档相反面：README.md:120「当前源码撤下了预约 Cookie 输入、F12 教程和未经真实验收的本地提交表单」、:128「预约会话输入与实验性本地提交入口已在 beta0.6.1 撤下」、:217「本应用不自动抢位或代提交」；本文第 4 节表头（:97）「预约/付款/选课/签到最终操作由用户在官方系统完成」。
- **影响面（分开说）**：
  - **不是远程可利用漏洞**：服务只监听回环（`server.go:166,171`，非回环地址直接拒绝启动），`api_guard.go:14-40` 还要求 Host 为回环、`Sec-Fetch-Site` 为 same-origin/none、`Origin` 严格同源、方法在白名单内。校园网里别人打不到，浏览器里的其它站点也打不到。
  - **真正的问题是三件事**：① 对外承诺与代码不符——说「撤下了」，其实只是前端不显示；② 一段**从未用真实账号验收**的写操作代码随每个 Windows 发布件分发，一旦被误用就会在学校系统里生成真实预约记录；③ commit 的错误分支自己就承认这个风险：「未能确认提交结果……不要直接重复提交」（:440）。
- **修复与验收（推荐第一条）**：**删掉**这三个端点，以及 `booking.go` 里只服务它们的 `pending` / `validateBooking` / commit 逻辑，只保留只读的 rooms / availability / history。验收：`grep -rn "booking/commit|booking/prepare|booking/session" desktop/` 无命中；`go test ./...` 通过；`smoke_windows.py` 74 项通过；`check-booking.mjs` 前端回归通过；README 与本文的「已撤下」表述从此是事实。若决定保留，则必须改文档写明「服务端仍有实验提交端点、未经真实验收」并补真实账号验收记录——不能维持现状。

### 36.3 F24：Linux 明文密码兜底静默成功，文档只承诺加密（P1）

- **证据**：`internal/credential/keyring_linux.go:34-37` 只在找得到 `secret-tool` 时启用 Secret Service；`:48-51` 不可用时直接走 `fileStore`；`:66-71` 写入失败时也退回文件并 `return nil`——用户看到的是「保存成功」。文件是 `~/.szunet/credentials.json`、权限 0600、**明文 JSON**（`store.go:63,72-84`）。
- **同一个包里标准不一致**：会话存储写明「School sessions require Secret Service. Never fall back to plaintext.」（`keyring_linux.go:40-46`），不可用就返回 `unavailableSessionStore`；`store.go:3` 的包注释也写着「密码不落明文盘」。凭据这条路径违反了自己写下的原则。
- **文档面**：README.md:88/103/206/216 与 README_en.md:101/116/216/227 只讲 Windows DPAPI；README.md:126 的「系统安全存储不可用时拒绝保存」按代码只对 Cookie 成立。下载 `szunet-linux-*` 的用户得不到任何提示。
- **发生条件与影响面**：仅限 Linux 且缺 Secret Service（或写入失败）的机器。文件 0600，同机其他用户读不到，但会被备份、云同步或误提交带走。
- **修复与验收**：二选一——(a) 与会话存储统一，不可用就报错不保存；(b) 保留兜底但**不静默**：把 `Describe()`（已经是「文件（~/.szunet/credentials.json，权限 600）」）回给调用方，并在 CLI / 界面明确提示「本机没有密钥环，密码将以受限权限明文保存」。两种都要在两份 README 补 Linux 实情。验收：新增测试覆盖「无 `secret-tool` 时不得静默报成功」；`go test ./...` 通过；README 对 Linux 存储方式有明确描述。

### 36.4 F25：macOS 凭据路径没有任何自动化覆盖（P1）

- **证据**：`release.yml:16` 的 `test` job 是 `runs-on: ubuntu-latest`，是全仓唯一执行测试的 job；`:53-59` 的 build-cli 矩阵含 darwin amd64/arm64，但只 `go build`（:72），从不运行。`keychain_prompt_unix_test.go` 由 Linux CI 执行，用的是假 `security` 脚本。
- **后果**：F21 的核心假设——真实 macOS 上 `security -w` 的提示会退回读标准输入（第 33 节明写「未验证」）——既没有真机验收，也不可能被 CI 发现回归。而 `szunet-darwin-*` 是每次发布都上传的附件。当前设计是「假设不成立就明确报错而不是退回 argv」，这个取舍是对的，但意味着 macOS 用户可能**根本存不了凭据**，而我们不会知道。
- **修复与验收**：加一个 `macos-latest` job 跑 `go test ./internal/credential/...`（macOS runner 上 `security` 真实存在），或找一台真机做一次 `szunet config set` → `config get` 往返并把结果记进本文。验收：CI 里出现 macOS 的测试结论，或本文有一条带日期的真机记录。在此之前任何文档都不得把 macOS 凭据写成「已验证」。

### 36.5 排除的误报：`detect.go` 跳过证书校验不涉及凭据

记下来，免得下次又当漏洞查一遍。

- `internal/portal/detect.go:225,248,273` 三处 `InsecureSkipVerify: true` 都在**不带凭据**的探测里：`internetReachable` 只看外网是否返回 204；`srunUsable` 用假账号 `username=probe` 只握手不登录（:293-300 的注释与代码一致）；`drcomUsable` 账号密码留空（:325-335）。
- 带凭据的登录走另一个客户端：`internal/portal/http.go:24-49` 的 `newHTTPClient` **保留**默认证书校验，只在指定 `--ip` 时补 `ServerName` 让 SNI 对上原域名（:41-47）——这正是正确做法。
- 剩余影响：中间人最多让「判区」结论出错，拿不到密码。归 P3，不列为严重问题。
- **但要如实告知用户**：宿舍 Dr.COM 默认是明文 HTTP（`internal/portal/drcom.go:15`，`http://172.30.255.42`）。这是学校协议的现状、不是本项目的选择；排查指南与 README 应说清「宿舍区认证走明文，不要把它当成加密连接」。

### 36.6 工程与协作面的缺口（P2，但会放大上面所有问题）

- **单文件文档已到 140KB / 1073 行**：本文同时承担需求、设计、任务、验收、发布记录五种角色，定位一条结论要靠 grep。建议拆成「当前状态 + 问题总表」「历史发布记录」「验收方法」三份，或至少加目录锚点。验收：新读者不滚屏就能查到「现在发布的是哪一版、还有哪些 P0」。
- **没有 CONTRIBUTING / SECURITY / issue 模板**：`.github/` 下只有 `workflows/`。仓库刚转到 SzuDesktopTeam 组织，外部协作者只能靠猜怎么跑测试。SECURITY 尤其该有——本项目处理校园网密码和教务 Cookie，需要一个私有报告渠道。
- **推送身份仍未落实**：deploy key 在组织转移后是只读，写权限待用户在 GitHub 设置里开；当前推送依赖 `git -c http.proxy=… push https://…`（环境只有 `http_proxy`、没有 `https_proxy`）。发布路径依赖一个人的本地环境。
- **`ubuntu-latest` 迁移**：2026-10-19 起指向 Ubuntu 26，届时 `secret-tool`、Node、Go 的行为都要重核。

### 36.7 本轮结论：按处理顺序

> **后续更正**：下表第 1、2、3、5 项已在同日完成，第 6 项的 SECURITY.md / CONTRIBUTING.md 也已补上，证据见第 37 节。第 4 项（发版）仍待做。

| 顺序 | 事项 | 编号 | 为什么排这里 |
|---|---|---|---|
| 1 | 删掉预约的三个服务端写端点，或改文档并补真实验收 | F23 | 对外承诺与代码相反，且带真实写操作；改动小、可回归 |
| 2 | 撤掉 `internal/vpn/vpn.go` 里未核实的来源保证 | F11 | P0 里唯一的纯文本改动，一次编辑即可闭环 |
| 3 | Linux 明文兜底不再静默成功 + README 补实情 | F24 | 凭据安全，改动集中在一个文件 |
| 4 | 发一个带 F22 / U16 的版本 | F22、U16 | 用户手上的 beta0.7.1 仍带着已修好的缺陷 |
| 5 | macOS 测试覆盖或真机验收 | F25 | 需要 CI 改动或用户的机器，周期最长 |
| 6 | SECURITY / CONTRIBUTING / 文档拆分 | — | 重要但不阻断，可排在上述之后 |

### 36.8 顺带更正的过期文档（本轮已改）

| 位置 | 原状态 | 更正 |
|---|---|---|
| 本文第 1 节 | 第 29 节记为「未打 tag、未发布」，且未提未发布的 F22 / U16 | 按实际发布归属重写，并列出未发布项 |
| 本文 §26.3 / §27 / §29 / §29.2 / §30 / §33 标题与正文 | 均标「未发布」 | 分别更正为随 beta0.6.1 / beta0.7 / beta0.7.1 发布 |
| 本文 §3.1 表格 | F20 与 F21 之间夹了一个空行，GitHub 会把 F21 / F22 两行渲染成断裂的表 | 删除空行 |
| 本文 B11 行 | 「未发布」 | 已随 beta0.6.1 发布 |
| README.md:60 | `[Releases](../../releases)` 在仓库首页解析到 `github.com/releases`（坏链） | 改为绝对链接 |
| README.md:105/115/120/128/217、README_en 对应处 | 称预约 Cookie 输入与本地提交表单「已撤下」 | 改为「界面入口已撤下，服务端实验端点仍在，见 F23」 |
| README.md:88/103/206/216、README_en:101/116/216/227 | 只讲 Windows DPAPI | 补 Linux 实情与 macOS 未验收边界 |
| README.md:135-144、README_en:140-149 | 子命令表缺 `szunet vpn` | 补一行（该子命令只是三条官方通道指引，不含实验 VPN 代码） |
| README.md:113 | 开机自启标 ✅ 未注平台 | 注明仅 Windows（`autostart_other.go:12` 返回 ErrUnsupported） |
| docs/blog/ac_id-接入点编号.md:229 | 「桌面端还没有让用户手动填 ac_id 的界面入口」 | 已过期：`app.mjs:53` 的高级设置有 `ac_id` 输入框 |
| docs/深圳大学校园网连不上排查指南.md:125-128 | 把同一个项目当成两个工具列了两次 | 合并为一条 |

## 37. 修掉复核发现的严重问题（2026-09-21，未发布）

按第 36.7 节的顺序做。修 bug 的部分一律**先写会失败的测试、亲眼看它红**，再改代码让它变绿；
纯文本与 CI 配置改动则先写下可核验的验收标准，再逐条核对。

### 37.1 F23：预约的服务端写端点已删除

- **删了什么**：`/api/booking/session`、`/api/booking/history`、`/api/booking/prepare`、
  `/api/booking/commit` 四个端点（`server.go`），以及只服务它们的
  `handleBookingSession` / `handleBookingHistory` / `handleBookingPrepare` / `handleBookingCommit` /
  `validateBooking` / `history()`、类型 `bookingInput` / `bookingPending` / `bookingRecord` /
  `bookingHistory`、`bookingService` 的 `cookie` 与 `pending` 字段、常量 `bookingSecure`、
  错误 `errBookingSession`。`booking.go` 从 444 行降到 223 行。
- **收敛了什么**：`request()` 去掉 `input` 与 `authenticated` 两个参数，现在**只能发 GET**——
  代码层面不再存在向学校提交预约的通路，而不是靠"前端不调用"来保证。
  `availability()` 同步去掉 `authenticated` 参数。
- **保留了什么**：只读的 `/api/booking/rooms` 与 `/api/booking/availability`（第 35.1 节已用真实数据实测过）。
- **TDD 记录**：先写 `TestBookingWriteEndpointsAreNotServed`（断言四个路径 × 三种方法都是 404），
  跑出来是 **`GET /api/booking/session 仍在提供服务，得到 200（应为 404）`**——红得符合预期，
  然后才动代码。另配 `TestBookingReadOnlyEndpointsStillServed` 防止删过头。
  测试替身现在对三件事零容忍：请求带 Cookie、请求不是 GET、目标不是学校公开地址；
  `/venue-api/boothReservation/*` 没有列进替身的 switch，任何代码走到那里都会撞上 default 而失败。
- **诚实说明**：`history`（我的预约）也一并删了——它依赖只有 `session` POST 能设置的 Cookie，
  界面从不调用它，留着就是不可达的死代码。将来若要在应用内办理预约，需要重新设计并做真实账号验收，
  **不是**把这几个端点恢复回来。

### 37.2 F24：凭据不再退回明文文件（范围比复核时判断的更大）

- **复核时低估了**：不只是 Linux。`keyring_windows.go` 在拿不到用户目录时也会写
  `fileStore{path: "credentials.json"}`——一个**相对路径**的明文文件。
- **做法**：删掉 `fileStore` 整个类型；新增 `store_unavailable.go`：
  `ErrStorageUnavailable` + `unavailableStore`（Save / Load 一律报错，`Describe()` 如实说明
  「系统安全存储不可用（原因），不使用明文文件」）。Linux 无 `secret-tool` 或写入失败 → 报错；
  Windows 拿不到用户目录 → 报错。与会话存储（`unavailableSessionStore`）终于是同一个标准，
  `store.go` 的包注释也去掉了「Linux 没有就退化成权限受限的文件」那句。
- **Delete 的取舍**：`Delete()` 仍然会删掉老版本可能留下的明文 `credentials.json`，
  因此可以如实报成功——没有密钥环时，明文文件就是密码唯一可能存在的地方。
  修好新路的同时不把旧坑留在用户盘上。
- **调用方已核对**：CLI `config set` 走 `fail(err)`（`main.go:531-534`）；
  桌面端 `POST /api/credential` 返回 500 且带错误文本（`server.go:635-637`），
  前端显示「认证已成功，但保存凭据失败：…」。不存在"报错了但界面说成功"的路径。
- **TDD 记录**：先放一个 `Save` 返回 nil 的桩，测试报
  **「没有密钥环时 Save 竟然报成功」**（红），实现后转绿；遗留明文清理那条同样先看它红
  （「删除不该失败：系统安全存储不可用…」）再实现。
- **已知局限（如实）**：Linux 的 `Load()` 仍分不清「密钥环里没这一条」和「密钥环被锁 / 服务没起来」，
  两种都当成没保存过。macOS 那边 R06 已按退出码区分，Linux 还没有可靠依据，**不猜**。
- **验证边界（如实）**：可移植测试 2 项在本机 Windows 实跑通过；`keyring_linux_test.go`
  带 `//go:build linux`，本机只能做 `GOOS=linux go vet` 编译验证，
  **它要在本次提交后的第一次 CI（ubuntu）里才算真正跑过**。

### 37.3 F11：撤掉未核实的来源保证

- `internal/vpn/vpn.go` 包注释里的「本包为依据公开协议行为分析独立编写的实现……
  代码为自行编写，未复制任何厂商或第三方的源代码」已删除，改为如实说明：
  来源与授权尚未核实、早期参考过第三方实验源码、在核实前不作独立编写保证、
  不得当成来源干净的实现宣传或再分发；并补上「本包不在任何发布件里（只有 `-tags campusvpn`
  才编译）」与「F06/F07/F08 未修，不要按可用功能对待」。
- **验收已核对**：`grep -rn "未复制任何厂商" internal/` **无命中**；
  `go build ./...` 与 `go build -tags campusvpn ./...` 都通过；
  `go vet ./...`（windows）与 `GOOS=linux go vet -tags campusvpn ./...` 都通过。
- **F11 仍不关闭**：撤掉保证 ≠ 核实来源。历史实验源码的授权依然没有确认，
  按第 6 节约定，相关宣传仍受限。

### 37.4 F25：CI 加 macOS job（**尚未运行验证**）

- 新增 `test-macos`（`runs-on: macos-latest`，job 超时 20 分钟）：
  ① darwin 上跑 `go vet ./...`；② `go test ./internal/credential/... -v`；
  ③ 一个**真实 `security` 命令的探针**——建一次性钥匙串、
  `printf '…' | security add-generic-password -a probe -s szunet-selftest-probe -U -w`、
  读回比对、清理。CI 进程本来就没有控制终端，正好复现 F21 依赖的那个条件
  （`readpassphrase` 打不开 `/dev/tty` 时退回读标准输入），所以这一步能在没有真机的情况下
  验证假设本身。
- **不阻断发布**：该 job 不在任何 `needs` 列表里，探针步骤另有 `continue-on-error: true`
  与 5 分钟超时（挂住也不会拖垮 job）。先拿证据，稳定后再考虑纳入 `needs`。
- **未验证（如实）**：本机没有 macOS，也没有 YAML 解析器（pyyaml / js-yaml 都不可用）。
  workflow 只做了**逐字节缩进核对**（`cat -A` 无 tab，层级与同级 `test` job 一致）。
  这个 job 跑不跑得通、探针结论是什么，**要等下一次推送后在 GitHub 上才知道**。
- 真机验收仍未做；在此之前任何文档不得把 macOS 凭据记成「已验证」。

### 37.5 协作面：补 SECURITY.md 与 CONTRIBUTING.md

- `SECURITY.md`：漏洞报告渠道（并如实写明 GitHub 私有漏洞报告入口需要仓库管理员在设置里启用）、
  三平台凭据现状表、本地服务只绑回环等约定、**已知未解决的安全项**（F06/F07/F08、F11、F21/F25、
  宿舍区明文 HTTP）、以及「哪些不算漏洞」的设计决定（无代码签名、不自动重连、
  探测跳过证书校验但不带凭据）。
- `CONTRIBUTING.md`：环境依赖、界面资源规矩（唯一源文件 + `sync-assets.py`，生成副本勿手改）、
  提交前必跑的 10 个前端回归与 Go 检查、**`gofmt` 在 Windows 工作副本上的 CRLF 陷阱**
  （`gofmt -l .` 会把几乎所有文件列出来，那不是格式问题，CI 也不跑 gofmt）、
  7 条红线、修 bug 的方式（先写失败测试，并指出仓库里 F22/F23/F24 三个现成例子）、
  发布流程与 `make_release.py` 的覆盖陷阱。
- issue 模板仍缺；本文已超过 1200 行，拆分建议仍在第 36.6 节。

### 37.6 本轮验证汇总

| 验证项 | 结果 |
|---|---|
| 前端 10 个检查脚本（`sync-assets.py` 后） | 全部通过，失败数 0 |
| `go vet ./...` | windows / linux / darwin **三平台**通过 |
| `go test ./...` | 8 个有测试的包全绿 |
| `go build ./...` 与 `-tags campusvpn` | 两种配置都通过 |
| `python desktop/build-windows.py` | 成功；嵌入资源两边一致（30800 字节，`6647b1a17e`） |
| `python desktop/smoke_windows.py` | **74 项全部通过**（数量与修复前一致，无回归） |
| **真实成品端点实测** | 4 个已删端点 × GET/POST/DELETE = **12 组合全部 404**；`/api/booking/rooms` 仍返回真实场地（时光社区共享琴房1 等）；隔离配置目录内**没有**生成 `credentials.json` |
| `test-macos` job | **未运行**，见 37.4 的未验证说明 |
| `keyring_linux_test.go` | **未在真实 Linux 上执行**，只做了 `GOOS=linux` 编译验证 |

### 37.7 仍未解决

- **以上全部未发布**：F22、U16、F23、F24、F11 的注释修正、F25 的 CI job、两份新文档都还在本地。
  **后续更正**：已随 beta0.7.2 发布，见第 39 节。
- F25 需要下一次 CI 运行才能确认；macOS 真机验收仍待做。
- F24 的 Linux 专属测试要在 CI 的 ubuntu 上跑过一次才算数。
- F11 的来源核实、F06/F07/F08、F15、D5、R05 都还没动。
- 真实账号验收（第 35.3 节）仍只能由用户本人操作。
- deploy key 写权限仍待用户在 GitHub 设置里开启。

## 38. 发布说明接上 CHANGELOG 单一来源（2026-09-22，未发布）

**起因**：用户问「每次更新并推送的时候会在 release 写清楚更新说明吗」。查线上实际情况：

- **beta0.7 的 Release 正文只有一行**：`**Full Changelog**: https://github.com/Alakazamc/szudesktop/compare/beta0.6.1...beta0.7`（88 字节，链接还指向转移前的旧组织名）。
- beta0.7.1 有 1590 字节的完整说明——那是发完之后用 `gh release edit --notes` **手写**的，不是 CI 生成的。

所以答案是**不会自动写清楚**。`release.yml` 的 release job 里只有 `generate_release_notes: true`，
而 GitHub 的自动生成靠 PR 标题与标签；本项目一直直接提交 main、没有 PR，它就只能吐一个 compare 链接。
仓库里也没有 `.github/release.yml`（那个文件同样只对 PR 分类），`make_release.py` 不生成任何说明文字。
**写不写说明，取决于当次有没有人记得**——beta0.7 就是没记得的那次。

**做法**：

1. 新增根目录 `CHANGELOG.md` 作为发布说明的唯一来源。已回填 beta0.6.1 / beta0.7 / beta0.7.1 三节
   （beta0.7.1 用线上正文原文，beta0.7 按第 31.4 节补记并注明是事后补记），当前批次写在 `## 未发布`。
2. 新增 `desktop/release_notes.py`：按版本号抽取对应小节，再拼上自动生成的下载清单。
   四种情况直接失败——没有这一节、正文为空、正文只有空白、正文里留着 `__VERSION__`；
   另外拒绝把「未发布」当版本号发布。抽取只认**完全相等**的标题，`beta0.7` 不会命中 `beta0.7.1`。
3. `release.yml` 的 release job 补 `checkout` + `setup-python`，先抽取写 `$RUNNER_TEMP/notes.md`，
   再作为 `body_path` 传给 action。**抽取失败就一个附件都不会上传。**
   保留 `generate_release_notes: true`：action 文档写明「提供了 body 时，body 在前、
   自动生成的内容追加在后」，所以 compare 链接仍会自动补上。
4. `make_release.py` 打包前调同一个校验。VERSION 一升、CHANGELOG 忘了写，
   **PR 阶段的 windows job 就会红**，不用等到打 tag 才发现。
5. 标签名不在 `run:` 里直接插值 `${{ }}`，改走 `env:`，避免成为脚本注入面。

**TDD**：先写 `desktop/check_release_notes.py`（11 项），跑出 `ModuleNotFoundError: release_notes`（红），
再实现模块转绿。

**验证**：

| 验证项 | 结果 |
|---|---|
| `python desktop/check_release_notes.py` | 11 项全部通过 |
| 抽取真实 CHANGELOG 的 `beta0.7.1` | 退出码 0，得到 1656 字节正文 + 下载清单 |
| 抽取 `未发布` / `beta0.9` | 都退出码 1，错误信息可直接照做 |
| 下载清单里的附件名 | 按线上 beta0.7.1 的 **8 个真实附件**核对过（独立 EXE 不带版本号、也没有单独的 `.sha256`，只有 ZIP 有） |
| `make_release.py` 的关卡 | 用**导入方式**验证，没有真的打包：`beta0.7.1` 放行，`beta0.9` 与 `未发布` 均退出码 1；`dist/` 确认未生成 zip |
| workflow 缩进 | `cat -A` 逐字节核对，无 tab，层级与同级 job 一致 |

**未验证（如实）**：本机没有 YAML 解析器（pyyaml / js-yaml 都不可用），也没法在本地跑 GitHub Actions。
release job 是否真按预期把 CHANGELOG 正文放前面、compare 链接接在后面，**要等下一次打 tag 才知道**。
「抽取失败会阻断发布」这一点是靠脚本退出码保证的，已在本地验证。
`test-macos` job 同样还没运行过（见第 37.4 节）。

> **后续更正**：本节写完当天就打了 beta0.7.2 标签，`test-macos` 与 release 的 `body_path`
> 拼接都真跑过了，结果见第 39 节——拼接符合预期，`test-macos` 则连抓出两个问题。

## 39. beta0.7.2 发布：新机制首次生效，macOS 探针当场抓出一个 P0（2026-09-22）

### 39.1 发布结果与附件核对

- 标签 `beta0.7.2` 指向 `f5ae2cb`，触发 [CI 35626817861](https://github.com/SzuDesktopTeam/szudesktop/actions/runs/35626817861)。
  `test`（含新加的「发布说明抽取回归」）、5 个平台 CLI、`build-desktop-windows`、`release` **全部成功**；
  `test-macos` 失败，但它当时不在 `needs` 里，没有阻断发布（见 39.3）。
- 公开 [beta0.7.2](https://github.com/SzuDesktopTeam/szudesktop/releases/tag/beta0.7.2) 为预发布，8 个附件。从发布页下载核对：
  - `.sha256` 行尾为 LF（`cat -A` 无 `^M`），值 `666a555f1ead155db41c7996c0403d14ff5e30dfc4ddc2b3d08b09c8b5e18586`；`sha256sum -c` **通过**
  - ZIP 4,994,833 字节；包内 `szudesktop.exe` 10,021,888 字节、SHA256 `036170f9feed70ecbcca031a6ea56619d7a6d48fde1f083b3cb02ce1e4871b44`，
    与线上独立 EXE **逐字节一致**；包内为 exe + 快速开始 + 字体许可 + 项目许可
  - **把线上成品跑起来实测**：自报 `szuDesktop beta0.7.2（内嵌 szunet 内核）`，`/api/status` 的
    `app_version` 为 `beta0.7.2`、`store_desc` 为 Windows DPAPI；
    4 个已删预约端点 × GET/POST/DELETE **12 组合全部 404**（F23 确实进了发布包）；
    `/api/booking/rooms` 仍返回真实场地；隔离配置目录内**没有**生成 `credentials.json`
  - 本地 `dist/` 的同名 ZIP 是打包脚本另跑一次的产物，与线上附件哈希不同属正常（CI 工具链与路径不同）

### 39.2 发布说明机制首次生效

- Release 正文 **2332 字节**，内容就是 `CHANGELOG.md` 的 `## beta0.7.2` 那一节；
  GitHub 自动生成的 compare 链接**追加在正文之后**，且指向转移后的新组织
  （`compare/beta0.7.1...beta0.7.2`）。与 `softprops/action-gh-release` 文档写的
  「提供了 body 时，body 在前、自动生成内容追加在后」一致。
- release job 的「从 CHANGELOG.md 抽取发布说明」这一步成功——第 38 节那两处
  「未验证」现在有了真机结论。beta0.7 那种「正文只有一行 compare 链接」不会再出现。

### 39.3 `test-macos` 首跑失败，连抓三个问题：一个是真缺陷，两个是我写的探针自身的 bug

按发生顺序：

1. **漏了 `sync-assets.py`**：`desktop/internal/ui/assets/` 是生成物、不进仓库，
   于是 `go vet` 直接报 `pattern all:assets: no matching files found`，
   后面的凭据测试与探针**全被跳过**。已在 `7ef9627` 修掉（vet 之前补同步资源）。
2. **`continue-on-error: true` 把真实失败显示成 success**：步骤日志里明明白白是
   `##[error]Process completed with exit code 44.`，job 的步骤结论却是 success。
   我据此对用户说了「success 意味着真的通过了」——**这句是错的**，
   正好踩了本项目「不许谎报成功」的红线。已去掉 `continue-on-error`，
   并把 `test-macos` 加进 release 的 `needs`：macOS 凭据这条路坏掉时，发布必须停下来。
3. **探针建了临时钥匙串、又用 `list-keychains -s` 把搜索列表指过去，但 `add` / `find` 都没带 `-k`**
   （run 35628967443）：写进去的是默认钥匙串，却只在临时钥匙串里找，于是必然报
   `SecKeychainSearchCopyNext: could not be found`（退出码 44）。这是探针自己的 bug，
   而且它把「两行喂法能不能往返」这个真正要验证的问题挡住了。
   已在 `ec8d628` 改成完全照抄生产参数：不建临时钥匙串、不改搜索列表，用完删除探针条目。

### 39.4 F26：部分 macOS 上凭据存不进去（P0，只有真机能抓到）

run 35627181165 的探针日志（真实 macOS runner）：

```
+ security add-generic-password -a probe -s szunet-selftest-probe -U -w
password data for new item: retype password for new item: passwords don't match
+ security find-generic-password -a probe -s szunet-selftest-probe -w
security: SecKeychainSearchCopyNext: The specified item could not be found in the keychain.
##[error]Process completed with exit code 44.
```

- **成因**：`security -w` 放末尾时会要求**输入两遍**（`password data for new item` 与
  `retype password for new item`），而 F21 的 `secretInput` 只喂了一行。
  第二遍读到空 → 两次不一致 → 条目根本没建起来。
- **这个行为不稳定，必须如实记**：同一镜像 `macos-26-arm64` / macOS 26.6.2 (25G83) 的
  后续 4 次运行，探针都记录为「只喂一行也能写入（没有要求确认）」。
  也就是说**问不问第二遍因机器/时刻而异**，所以探针只把它当记录、不当断言。
- **影响**：在会要求确认的机器上，`keychainSave` 的写前自检必然失败，于是 macOS 上
  `szunet config set` 与桌面端保存凭据 / 会话全部报错。设计上的「宁可失败也不退回 argv」生效了，
  所以**没有泄漏、也没有明文落盘**，但功能是坏的。
  **beta0.7.1 与 beta0.7.2 都带着这个缺陷**——在会要求确认的 macOS 上，
  F21 那个安全修复从来没真正可用过。
- **修复**：`secretInput` 改为喂「密码 + 确认」两行。只问一遍的机器上多出的那行
  只是没人读的剩余输入——**这一点已由真机验证**（见 39.5），不再是推测。
- **TDD 记录**：先把两个测试替身改成如实模拟两次提问（可移植的 `fakeKeychain`
  与 unix 版真 exec 的假脚本），5 个既有测试立刻变红，错误信息与真机日志**一字不差**
  （`exit status 44（passwords don't match）`）；再改 `secretInput` 转绿。
  新增 `TestKeychainWriteFeedsSecretTwiceForConfirmation` 直接断言 stdin 是两行且一致。
  这条性质现在本地与 Linux CI 都守得住，不必依赖 macOS。

### 39.5 验证

| 验证项 | 结果 |
|---|---|
| beta0.7.2 线上附件 | `sha256sum -c` 通过；`.sha256` 行尾为 LF；包内 exe 与独立 exe 逐字节一致（SHA256 `036170f9…`）；自报 `szuDesktop beta0.7.2`；4 个已删端点 × 3 方法 = **12 组合全 404**；rooms 返回真实场地；隔离配置目录内无 `credentials.json` |
| Release 正文 | CHANGELOG 正文在前、compare 链接在后，共 2332 字节，链接指向转移后的新组织 |
| `go test ./internal/credential/ -run TestKeychain` | 6 项全过（含新增的两行断言） |
| `go test ./...` / `go vet`（windows、linux、darwin） | 全绿 |
| 前端 10 项 + 发布说明 11 项回归 | 全通过 |
| **`test-macos` 真机（macOS 26.6.2）** | **4 次运行全部 success**（run 35629439698 / 35629849601 / 35629864438 / 35629875577），每次都验证「两行喂法可写入并读回一致」；其中 3 次日志可读，均记录为「只问一遍」状态 |
| `keyring_linux_test.go`（`//go:build linux`） | ubuntu 的 `test` job 里 `internal/credential` 包通过（`ok 0.014s`）。但该测试在机器装有 `secret-tool` 时会 skip，而 `go test ./...` 不带 `-v`，**这一次无法从日志判断它是真跑了还是被跳过**——记为待确认，不写成已验证 |

**仍未验证**：两行喂法在「会要求确认」那种状态下的通过，还没有直接观测到
（4 次采样都落在「只问一遍」）。逻辑上那正是它要求的输入，且探针每次都会记录所处状态，
所以将来一旦采到就会自动闭合。

### 39.6 仍未完成

- **F26 的修复尚未发布**：线上 beta0.7.2 在会要求确认的 macOS 上存不了凭据。
  **后续更正**：已随 beta0.7.3 发布，见第 40 节。
- 真机 macOS 上跑一次 `szunet config set` → `config get` 往返仍未做
  （探针验证的是 `security` 的行为与我们喂入方式的可行性，不是整条 Go 保存链路）。
- 「会要求确认」状态下的两行喂法尚未直接观测到（见 39.5）。
- `keyring_linux_test.go` 是否真的在 CI 上执行过（而非 skip）待确认。
- F11 来源核实、F06/F07/F08、F15、D5、R05、真实账号验收（第 35.3 节）都没动。

## 40. beta0.7.3 发布：F26 修复上线，发布链路第一次全绿（2026-09-22）

- 标签 `beta0.7.3` 指向 `57e96c3`，触发 [CI 35631956064](https://github.com/SzuDesktopTeam/szudesktop/actions/runs/35631956064)：
  **run 总体 completed / success**。这是 `test-macos` 进入 release 的 `needs` 之后第一次全绿的发布——
  test、test-macos、5 个平台 CLI、build-desktop-windows、release 全部 success，
  release job 的「从 CHANGELOG.md 抽取发布说明」步骤也 success。
- 发布范围只有 F26 修复与 CI 探针修正；**Windows / Linux 的行为与 beta0.7.2 相同**。
- 公开 [beta0.7.3](https://github.com/SzuDesktopTeam/szudesktop/releases/tag/beta0.7.3) 为预发布，8 个附件。核对结果：
  - `.sha256` 行尾为 LF，值 `813ef647f6dedcf51d3f2a7d2fc8ddc4dda532b60aff1e306a44cf45ba672ed1`；`sha256sum -c` **通过**
  - ZIP 4,994,825 字节；包内 `szudesktop.exe` 与线上独立 EXE **逐字节一致**，
    10,021,888 字节、SHA256 `9516d85a5412b742…`；包内为 exe + 快速开始 + 字体许可 + 项目许可
  - Release 正文 1398 字符 = CHANGELOG 的 `## beta0.7.3` 一节，末尾追加自动生成的
    compare 链接（`beta0.7.2...beta0.7.3`）
  - `szunet-darwin-arm64` 6,340,114 字节（beta0.7.2 为 6,323,602），差异来自 F26 的改动
- **把线上成品跑起来实测**：自报 `szuDesktop beta0.7.3（内嵌 szunet 内核）`；
  `/api/status` 的 `app_version` = `beta0.7.3`、`store_desc` = Windows DPAPI；
  4 个已删预约端点 × GET/POST/DELETE = **12 组合全 404**；`/api/booking/rooms` 返回真实场地；
  隔离配置目录内**没有**生成 `credentials.json`。
- **本次 macOS 探针记录**：macOS 26.6.2 (25G83)，状态为「只喂一行也能写入（没有要求确认）」，
  两行喂法写入并读回一致 → test-macos success。这是第 5 次采样落在「只问一遍」状态；
  「会要求确认」那种状态自 run 35627181165 之后未再采到，所以 F26 修复在那种状态下的
  **直接观测仍然缺**。探针每次运行都会记录所处状态，采到即自动闭合。
- **仍未验证**：真机 macOS 上完整跑一遍 `szunet config set` → `config get` 往返。
  探针验证的是 `security` 的行为与我们喂入方式的可行性，不是整条 Go 保存链路。
- 本地 `dist/` 里同时留着 beta0.7.2 与 beta0.7.3 两个 ZIP，前者未被覆盖。

## 41. 修「场地与琴房规则速查」带出来的渲染链路缺陷，以及命令行开机自启（2026-09-22，未发布）

复核 `34c5d57`（场地规则速查）时发现：这个功能本身是对的，但它把一组**新的失败模式**
引进了渲染链路。同时首轮就记下的 F27（命令行开机自启）仍在。本轮一并修掉。

### 41.1 渲染链路上的静默失败（P2-8 / P2-9）

- **P2-8**：`openHours()` 要对学校的 64 位 `availableTimePeriod` 做位运算，而 JS 的位运算会在
  32 位截断，只能用 BigInt。原实现遇到非整数、带千分位的字符串、超出 `Number.MAX_SAFE_INTEGER`
  的值都会抛异常；而它在 `services()` 里被无条件求值，结果整段赋给 `#main`。
  **一旦抛出，那句赋值就不执行 —— 整个「校园服务」页静默不更新，且不向用户报任何错。**
  现在不可信输入一律降级成「—」，函数本身绝不抛。
- **P2-9**：分组键原来是类型 `name`。不同类型重名时会把两类场地并成一组，并顺手用第一个场地的
  规则去描述另一类 —— 规则张冠李戴比不显示更糟。改用场地自带的 `typeId`。
- **另外补了两层兜底**：`services()` 里每张卡片各自 try/catch（坏掉一块只降级这一块），
  `render()` 里由 `pageHTML()` 兜底（整个页面函数抛异常时换成一页可读提示，而不是留空白，
  也不打断 `clocks()`）。原来的设计是「一处异常 → 整页白板 + 无提示」，这两层把爆炸半径收掉了。

### 41.2 两条读取路径的校验强度对齐（P2-10）

`availability` 一直校验 `Mask>>48`、单日上限、可提前天数；新出现的 `/booth/list` 路径没校验，
于是同一份数据「空位查询会拒绝、规则卡片照单全收」。把边界收成 `bookingType.validate()` 两处共用。

**但列表路径只收敛、不整份拒绝**：`availability` 用掩码决定显示哪些时段，掩码错一格就是错数据，
必须硬失败；列表只用 type 展示规则，为一个场地字段异常扣下整份列表会让用户什么都看不到。
所以列表路径把越界数字折成零值（前端显示「—」，等于如实说「这项未知」），场地本身照旧出现。

### 41.3 学校富文本的块级标签（P2-11）

`plainTextFromSchoolHTML` 原来只把 `</p>` `</li>` 换成换行，`<br>` 还只认小写三种写法。
学校改用 `div` / `h3` / `tr` 排版时整段须知会被挤成一行，`<!-- -->` 注释也会原样混进正文。
现在补齐块级结束标签（含表格与标题）、大小写与自闭合的 `<br>`，并丢掉注释。

### 41.4 同一份数据被打两遍（P2-12）

规则卡片与空位卡片各自请求 `/api/booking/rooms`。抽出 `createRoomsLoader(api, ttl)`：
带短 TTL、合并并发调用、**失败不写缓存**，两张卡片共用一份。只在前端共享，不为此给 Go 侧引入
并发状态 —— 这里要的是「两次点击打一次接口」，前端一次就够，给服务端加缓存只会让失效时机变复杂。
见 41.6：41.4 的原始分析是在没有 Go 工具链的前提下写的，本轮已补装并完整跑过编译与测试。

### 41.5 命令行开机自启（F27）

注册参数从 `login --auto` 改为 `login`，并给 `login` 保留 `--auto` 兼容开关，让已写进用户注册表的
旧启动项不改注册表就能恢复。详情见 F27 行。

### 41.6 本轮验证与未验证

本轮把 Go 工具链装到了 `D:\develop\go`（go1.26.6 windows/amd64，与 `go.mod` 一致），
清单里的「未执行」项因此全部补跑完毕。**先复现、再修、再验证**，不是只看代码。

- **修复前先复现（红）**：从 `34c5d57` 那个提交单独编出 CLI 做对照，`szunet login --auto`
  得到 **退出码 2** 与 `flag provided but not defined: -auto`，参数解析处直接死掉；
  同一二进制跑不带 `--auto` 的 `login` 得到退出码 1（越过解析、走到凭据检查）。
  两相对照坐实了 F27：旧启动项每次开机都在参数解析这一步静默退出。
- **修复后对照（绿）**：重建的 CLI 上 `login --auto` 与 `login` 都得到退出码 1，
  说明 `--auto` 已被接受、程序正常走到「没有可用账号密码」这一业务分支。
- **已跑通**：`desktop/sync-assets.py` 同步通过；`go vet ./...` 无输出；
  `go test ./...` **全包 ok**（含 `desktop/internal/ui` 6.0s、`internal/credential`、
  `internal/netpref` 等）；`check_release_notes.py` **11 项全绿**；10 个前端回归脚本 **74 项全绿**
  （`check-booking.mjs` 由 11 项增至 14 项，新增「不可信数字只降级不抛」「同名不同类型不并组」
  「两张卡共享一次请求、失败不入缓存」）。另用临时 Node 探针直接对 `venueRulesHTML` 喂
  11 种敌意掩码值（`'abc'` / `'1,234'` / `1.5` / `2**53` / `-1` / `0` / `null` / `undefined` /
  空串 / `{}` / `[]`）与 6 种非法输入形状，确认一律返回字符串且降级为「—」，查完即删。
- **新增并已执行**：`booking_rules_test.go` 新增 3 个 Go 测试（块级标签与注释、`validate()` 边界、
  列表路径收敛而非失败）；`cmd/szunet/autostart_args_windows_test.go` 新增 2 个 Windows 测试。
  5 个新测试逐个 `-v` 跑过，均 PASS。
- **真机产物**：`build-windows.py` 的第 4–5 步（编译 + 写图标/版本资源）手工执行成功，
  产出 `dist/szudesktop-windows-amd64.exe` 10,053,632 字节，`.rsrc` 段存在、8 种资源类型。
  `smoke_windows.py` **74 项全部 PASSED**（跑在这个真实产物上，含内嵌资源、凭据、会话、
  工作区、单实例、关机等）。内嵌资源另做了一次逐字节比对：`index.html`、`booking.mjs`、
  `campus-ui.mjs`、`app.mjs` 从运行中的 exe 取回，与磁盘上的源文件 md5 完全一致，
  用于确认「改了页面但没同步进二进制」这类历史事故没有发生。
- **仍未验证（本轮明确排除）**：真机开机自启是否真的拉起并连上网 —— 要在注册表里写入启动项
  再重启一台 Windows 才能看，本轮没有做，F27 只验证到「登记的参数 login 一定认得」这一层；
  学校真实返回的 `availableTimePeriod` 形态（P2-8 的实际触发概率取决于它）；以及
  `dist/` 里的产物未做真机界面点击（冒烟测试走的是 `--no-open` 无头路径，不开浏览器窗口）。

## 42. 第一个外部 PR 合并：协作者修掉我引入的渲染静默失败（2026-09-22）

**这是项目第一次有外部贡献者直接向 main 提 PR 并被合并**，值得单独记一节；技术细节见第 41 节。

### 42.1 背景

`34c5d57`（场地与琴房规则速查）上线后，协作者 StrangeWh 提了 [PR #5](https://github.com/SzuDesktopTeam/szudesktop/pull/5)，
标题「修掉场地规则速查带出的渲染静默失败，并修好命令行版开机自启」，单个提交 `97655cf`，
merge commit `97063da` 合入 main。**PR 描述本身按本项目的验收格式写的**（复现步骤、验证证据、未验证项分列），
说明 CONTRIBUTING.md 的约定被读进去了。

### 42.2 他修的第一个问题：我引入的渲染静默失败

**问题是我造成的。** `booking.mjs` 里的 `openHours()` 写的是 `BigInt(m||0)`：
学校返回的 `availableTimePeriod` 一旦是带千分位的字符串、小数、或超出 `Number.MAX_SAFE_INTEGER`，
这行就抛异常；而它在 `services()` 里被**无条件求值**，结果整段赋给 `#main`。
异常一抛，那句赋值就不执行——**整个「校园服务」页静默停止更新，用户看不到任何提示**。

我在真机验证时用的是正常值（`4397794590720`），所以没触发。**这个 bug 只在字段格式异常时现形**，
学校改一次字段格式就能让整页变白板。为了显示一个"开放 10 小时"赌上整页渲染，代价和收益完全不成比例。

**修法**（我认可）：
- `openHours` 与新增的 `ruleNum` 一律不抛，不可信输入降级成「—」
- `ruleNum` 用 `Number.isSafeInteger` 而非 `isFinite`——同时挡住小数、`Infinity`、`NaN`、超安全整数
- **0 也显示「—」**：服务端会把越界值收敛成 0（见 `bookingType.validate`），
  分不清「学校真报了 0」和「我们自己清零的」，宁可标成未知，也不拿 0 冒充学校数字
- 分组键从类型 `name` 改为 `typeId`：name 是展示字符串，两类重名会被并成一组，
  并顺手用第一个的规则描述另一类——规则张冠李戴比不显示更糟
- 两条读取路径**故意用不同失败策略**：`availability`（掩码决定显示哪些空位）硬失败，
  掩码错一格就是错数据；`/booth/list`（只展示规则）把越界值降级，不为一个场地字段扣下整份列表

### 42.3 他修的第二个问题：F27 命令行版开机自启静默失效

`internal/autostart/autostart_windows.go` 里命令行版开机自启登记的是 `szunet login --auto`，
而 `login` **从来没有 `--auto` 这个参数**；szunet 的 flag 集是 `ExitOnError`，遇到未知开关直接 `os.Exit(2)`。
**所以开过这个开关的用户，开机时程序什么都没做就退出了，且没有任何报错。**

修复：登记参数改为 `login`（它本来就是非交互的：按「命令行参数 > 环境变量 > 已保存凭据」取账号，
失败只反映在退出码上），抽成 `CLILoginArgs()` 常量避免三处各写各的；
同时给 `login` 保留 `--auto` 兼容开关，让已经写进用户注册表的旧启动项不必手工改注册表就能恢复。

**他是先复现再修的**：从 `34c5d57` 单独编出 CLI 做对照，`login --auto` 得到退出码 2 与
`flag provided but not defined: -auto`，修复后两种写法都是退出码 1、正常走到凭据检查。
这是实证，不是看代码猜。

### 42.4 顺带修掉的

- `plainTextFromSchoolHTML` 补齐块级结束标签（`div` / `h3` / 表格）、大小写与自闭合 `<br>`，并丢掉 HTML 注释。
  原来只把 `</p></li>` 换成换行，学校改用 `div` 排版时整段使用须知会被挤成一行
- 新增 `cmd/szunet/autostart_args_windows_test.go`：用 `ContinueOnError` 起一个与 `login` 同配置的 flag 集
  去解析登记的参数，把「注册了 `login` 不认的参数」拦在测试阶段
- `check-booking.mjs` 由 11 项增至 14 项，其中「不可信数字只降级不抛」刻意用逐字全「—」精确匹配，
  而不是「页面里出现过 —」——后者会被 `openHours` 单独产出的一个「—」蒙混过关

### 42.5 验证与边界

**已验证**：`go vet ./...` 无输出；`go test ./...` 全包 ok；前端 10 个回归脚本 74 项全绿；
`build-windows.py` 与 `smoke_windows.py` 74 项在真实产物上通过；内嵌资源另做逐字节比对
（`index.html` / `booking.mjs` / `campus-ui.mjs` / `app.mjs` 从运行中的 exe 取回，与磁盘源文件 md5 一致），
用于确认没有「改了页面但没同步进二进制」这类从外面完全看不出来的事故。
合并后的 main 我另行拉取复核：`go vet` 三平台、`go test ./...`、前端 10 脚本均通过。

**仍未验证**：真机开机自启是否真的拉起并连上网（要写注册表再重启一台 Windows）；
学校真实返回的 `availableTimePeriod` 形态；`dist/` 产物的真机界面点击（冒烟走 `--no-open` 无头路径）。

### 42.6 权限侧同期发生的变化

- rcwalter24（组织成员）与 StrangeWh（外部协作者）均提为 **write**
- main 分支保护配为「要求 0 个批准 + 禁止直接推」，`enforce_admins=false`
  ——GitHub 硬性禁止自我批准，所以"自己的 PR 自己合"只能靠把批准数降到 0 实现
- **合并方式用了 merge commit 而非 squash**，历史上多了一个 `Merge pull request #5` 提交。
  本项目此前一直是线性历史，今后要不要限定只允许 squash，是待决定项
- StrangeWh 进组织的邀请未发出（gh 缺 `admin:org` 范围），需要用户在网页上操作

## 43. Electron 外壳迁移：阶段 0/1 完成（2026-09-25，分支 `feat/electron-shell`，未合并未发布）

桌面版的窗口层正在从「浏览器 `--app` 窗口」迁到 **Electron 外壳**：Electron 主进程把现有 Go
桌面 exe 当 sidecar 子进程拉起（`--no-open`），解析其 stdout 里的回环地址，主窗口加载现有 UI，
退出时清理进程树。设计 spec 与执行计划见 `docs/superpowers/plans/2026-09-24-electron-phase-0-1-shell.md`
（已于 `809070f` 入库）。本节记录阶段 0（可行性）与阶段 1（落地）的完成证据、与 spec 的偏差、未决事项。
过渡期内**旧版单 exe 桌面版仍在构建和发布**，两者并存（README 已按此改写）。

### 43.1 落地内容（提交 `ce9b45b..4a53ec8`）

- `desktop/electron/listen-url.mjs`：`parseListenUrl` 纯函数，把 sidecar stdout 归一成回环 URL（`ce9b45b`）
- `desktop/electron/sidecar.mjs`：sidecar 监督器 `startSidecar` / `stopSidecar` —— 拉起、解析端口、
  `/api/status` 健康探测、失败路径先杀子进程再抛（不泄漏孤儿），win32 用 `taskkill /T` 清进程树兜底（`3607c95`、`c4c7198`）
- `desktop/electron/main.mjs`：应用入口 —— Electron 单实例锁、起 sidecar、主窗口加载现有 UI；
  外链只放行 http/https（`external-url.mjs`），杜绝 `file:` / `search-ms:` 之类任意 URI 交给系统处理器（`39445be`、`e9cc7e3`）
- `desktop/electron/build.mjs` + `electron-builder.yml`：一键构建 —— 先用 `desktop/build-windows.py`
  编出 Go sidecar，再把 `internal/version/VERSION` 去掉 `beta` 前缀后经 `--config.extraMetadata.version`
  注入版本（仓库里的 `package.json` 恒为 0.0.0，不再被构建改写），electron-builder 出 NSIS 包（`abb010a`、`12974d0`）
- `desktop/electron/check-sidecar.mjs`：8 项回归（端口解析、监督器拉起/健康/失败清理、外链白名单），
  用 node 替身而不是真实 exe，ubuntu CI 可跑
- CI（`.github/workflows/release.yml`）：test job 增加一步 `node desktop/electron/check-sidecar.mjs`（`4bfe7b1`）；
  新增 `build-desktop-electron-windows` job（windows-latest，npm ci + electron-builder，产出安装包 artifact；
  **未接入 `release.needs`**）（`4a53ec8`）

### 43.2 完成证据

- **sidecar 机制对真实 exe 成立**（阶段 0 的核心问题）：用 1.2 的真实监督器拉起真实构建产物
  `dist/szudesktop-windows-amd64.exe`（9.6MB，beta0.7.3，windowsgui 子系统、无控制台）→
  `RESOLVED http://127.0.0.1:54597`、`/api/status` 返回 200 与真实 JSON、`STOPPED_OK`、退出码 0。
  结论：windowsgui 子进程的 stdout 能被 Node 管道捕获并解析出端口，机制不是只对替身成立。
- **Electron dev 应用截图验证渲染出真实 UI**（Task 1.3）：经 `webContents.capturePage()` 存
  `.scratch_probe/main-shot.png`（保留，约 400KB）；控制方独立读图确认渲染出真实荔枝庭院界面：
  顶栏 beta0.7.3、导航、首启引导弹窗、校园连接卡、宠物卡。**只声明「渲染」**——透明合成 / 始终置顶
  这类性质截图验证不了，本阶段也未涉及。
- **Windows NSIS 安装包已产出**（Task 1.4）：`desktop/electron/release/szuDesktop-Setup-0.7.3.exe`，
  **85,801,230 bytes（约 85.8MB）**；包内 `resources/szudesktop-windows-amd64.exe` 与 dist 构建产物
  字节一致，app.asar 五个模块齐全，图标字节确认嵌入。**未安装、未运行**——「安装 → 启动 → 主窗口」
  的真机验收留给人工检查点，本节只声明「产出且内容清单符合预期」，不声明「装上去可用」。
- **全量回归 14/14 绿**（Task 1.7，2026-09-25）：前端 10 个 check-* 加 check-sidecar 合计 83 项断言全过；
  `go vet ./...` 无输出；`go test ./...`（另以 `-count=1` 重跑确认非缓存）8 个含测试包全部 ok；
  `python desktop/smoke_windows.py` 74 项 PASS、0 FAIL。无头 / 冒烟路径与迁移前一致，无回归。

### 43.3 与 spec 的偏差（有意为之，逐条记录）

1. **阶段 0 的一次性 Electron 尖峰（原 Task 0.2）取消了**。尖峰三个目标里「spawn + 解析端口 + 加载 UI」
   已被 1.2 的真实探针与 1.3 的正式外壳覆盖；唯一独有价值「透明置顶宠物窗」属阶段 2。省掉重复的
   ~150MB 下载与一次性重建代码，Electron 只装一次（`desktop/electron/`）。
2. **删除 `openBrowser` / autostart 登记改指 Electron / 单实例改用 Electron 锁 —— 挪到阶段 2**。
   spec 原要求删掉浏览器开窗代码，但过渡期旧版单 exe 仍在构建、发布（Releases 的 zip 还是它），
   提前删会破坏现状。因此单实例目前是「Go 引擎层的同存档复用」与「Electron
   `requestSingleInstanceLock`」双锁并存，阶段 2 退役旧 exe 时一并收敛。
3. **宠物窗整体推迟到阶段 2**（spec 本就把它排在后续阶段，这里明确阶段 0 也不再为它做尖峰）。

### 43.4 未决事项

- **mac / linux 的 Electron 打包推迟**：本轮只做 Windows NSIS；mac / linux 桌面维持旧形态。
- **发布接入 go/no-go：PENDING（未决定）**。是否把 `build-desktop-electron-windows` 接入
  `release.needs`、让安装包随下一个 tag 公开，交作者决定；目前意图仅产 artifact，但 release 的全量产物下载仍可能将它混入公开附件，见第 44 节 EL02。
  安装包未签名（electron-builder 明确 `signing is skipped`），用户首次运行会有 SmartScreen 提示；
  是否购代码签名证书同属发布决策。**未获明确同意不接入 release、不打 tag、不推送。**
- **winCodeSign 符号链接的 CI caveat**：electron-builder 下载的 winCodeSign-2.6.0 包内含 macOS
  符号链接，某些 Windows 环境（非管理员且未开开发者模式）7za 建链接无权限，报
  `ERR_ELECTRON_BUILDER_CANNOT_EXECUTE`——本机复现过，靠手动预置 `%LOCALAPPDATA%/electron-builder/Cache`
  解决（缺的 2 个 darwin 链接只服务 mac 签名，Windows NSIS 用不到）。GitHub 的 windows runner
  通常允许建符号链接、预计不炸，caveat 与绕法已写进 release.yml 该 job 的注释；以 CI 真跑为准。
- **setup-node 标签已核实存在**：2026-09-25 通过 GitHub API 确认 `actions/setup-node@v6.0.0`；Electron job 本身仍待首次 CI 运行验收。
- `desktop/electron/package.json` 的 `description` / `author` 已在 `3b13ce7` 补齐；现有安装包早于该提交，包内 metadata 尚未包含这些字段，需要修复收尾后重建。
- 监督器尚不覆盖「sidecar 中途崩溃后的重启」；阶段 2 常驻宠物使长会话成常态时单独立任务。
- 本机安装 electron / winCodeSign 等二进制需 npmmirror 镜像环境变量（直连 GitHub CDN 会 ECONNRESET，
  典型中国网络问题）；镜像配置只在本地用，**不进 lock / .npmrc / 仓库**，CI（美国 runner）走 npmjs 官方源。


## 44. Electron 发布前审查与优化顺序（2026-09-25）

审查基线：`feat/electron-shell` / `809070f`。**本节保留审查当时的发现与完成标准；随后按用户授权实施的修复见第 45 节**。审查当时未安装、合并、推送、打标签或公开发布。第 43 节证明基础外壳已经实现，不等于 Electron 可直接公开发行。所有新增任务只在本节维护，不另建问题清单。

### 44.1 结论与已成立的基础

- 暂不直接公开当前 Electron 安装包，先关闭下表 P1 项，再通过安装验收。保留像素校园、木框和纸张风格，无需为迁移重做整套前端。
- 当前公开版仍是 beta0.7.3 的旧单 EXE；Electron 分支有 14 个相对本地 main 的提交，远端尚无该分支。公开 Release 8 个附件中没有 Electron 安装包。
- 现有安装包确实包含 Go 引擎和四个运行模块加 package.json；上轮核对包内 Go 引擎与当前 dist 字节一致，归档截图显示真实荔枝庭院。安装、升级、卸载以及高 DPI 窗口操作没有据此获得验收。
- 保留了回环监听、API 来源/方法校验、关闭 Node integration、context isolation、外链协议白名单、本机存档和安全凭据存储。没有发现必须重写 Go 引擎或迁移存档格式的理由。

### 44.2 唯一新增任务清单

难度沿用第 2 节 S/M/L，是工作范围估计，不是工时承诺。P1 为这次 Electron 公开发布前必做；P2 为近期体验/安全加固；P3 可后置。复现和代码审查结论分别标注。

| 编号 | 优先级 | 难度 | 问题、证据与影响 | 完成标准 |
| --- | --- | --- | --- | --- |
| EL01 | P1 | M | **运行时停止支持**：package-lock.json 锁定 Electron 33.4.11，官方列出 33 系列于 2025-04-29 结束支持。不是已证明存在某个可利用漏洞，但不宜用停止维护的浏览器内核新发处理校园会话的应用。 | 升级到仍受支持的稳定版本，复核相关破坏性变更，并验证打包、启动、外链和退出。 |
| EL02 | P1 | S | **未授权发行的配置竞态**：release.yml:236 的 download-artifact 不限定名称，随后上传 dist/*。不加入 release.needs 只是不等待 Electron job；它若先完成，安装包仍可能被收进公开附件。代码配置确认。 | 用明确的产物允许列表；未开启 Electron 发布时，模拟其 artifact 已存在也不会上传。决定公开时再明确接入依赖和附件。 |
| EL03 | P1 | M | **已有 Go 实例导致 Electron 打不开**：main.mjs 固定 spawn；server.go:154-159 复用旧实例后退出且不打印地址。设置自启登记 Go exe --no-open，所以新版用户开启自启也会触发。真实 Go exe 隔离复现：第一实例健康、第二次报“sidecar 提前退出，码 0”、第一实例仍返回 200。 | 明确已有引擎的接管/所有权与窗口激活策略，修正 Electron 的自启目标或暂时禁用该入口；覆盖自启后打开、旧单 EXE 在后台、双击两次，不能误杀共享实例。 |
| EL04 | P1 | S | **启动失败未进入正常错误处理**：sidecar.mjs:6-9 未监听 child.error。不存在的 exe 已复现 Unhandled error / ENOENT，调用方 catch 接不住。 | 缺失文件、无权执行等错误返回受控拒绝，清理计时器与监听，并给用户明确的失败原因和退出入口。 |
| EL05 | P1 | S | **健康检查超时不约束请求**：sidecar.mjs:13-19 的 fetch 无取消。隔离替身设置 100ms 超时，实际到替身 1.5 秒后退出才解除等待，总计 1771ms。 | 给请求绑定剩余超时，达到期限就取消并回收本次启动的子进程；用不返回响应的替身验证。 |
| EL06 | P1 | S | **stdout 分块时截错端口**：listen-url.mjs:3-4 在收到数字前缀时立即匹配；隔离替身先输出端口前四位、120ms 后输出末位，监督器请求错误端口并启动失败。 | 等完整行或明确边界再解析；覆盖在端口中间拆分的一条真实格式日志。 |
| EL07 | P1 | M | **进程树退出缺少可靠等待**：sidecar.mjs:42-45 先 kill 父进程，再发 taskkill /T，后者可能找不到父 PID；错误被丢弃。main.mjs 的 async before-quit 也没有阻止并等待退出。当前测试只证明父进程消失，不能证明进程树全部退出。代码审查，未宣称已复现真实孤儿。 | 正常退出先请求 Go 有序关闭，再有期限地清理本次拥有的进程树；Windows 优先等待树清理。用自己的父/子替身验证不同退出入口，不按进程名杀用户实例。 |
| EL08 | P2 | S/M | **引擎中途退出无主动反馈**：main.mjs 启动后未处理 child.exit；旧页面留着，重新双击只聚焦故障窗口。启动失败也只 console.error，安装版用户难以诊断。代码路径确认，未制造用户 GUI 崩溃。 | 显示“引擎已停止”及重新打开/完整退出入口，保留可脱敏诊断；本期无需复杂的自动重启循环。 |
| EL09 | P2 | M | **窗口导航与权限策略尚未收紧**：目前只限制新开窗口；未设 will-navigate/重定向来源限制、会话权限处理器，HTML/响应无 CSP。属于加固缺口，不代表已验证攻击链。sandbox 未显式写出也不能直接等同于关闭。 | 主窗口保留在自己的回环来源，远端 http/https 外链交系统浏览器；权限按实际功能明确处理，配置符合现有资源的 CSP，验证正常跳转/导出不受影响。 |
| EL10 | P2 | M | **单次学校查询锁住全应用**：app.mjs:88 的全局 busy 禁用正文控件，:89 导航也直接忽略；只读公告/空位查询期间不能转去庭院。前端请求上限 45 秒，场地后端 20 秒。代码路径确认，未额外模拟 GUI 弱网。 | 只读加载状态限定到卡片，允许切页或取消；沿用已有结果防串页机制，存档写入仍按需串行。 |
| EL11 | P3 | S | **启动后持续保留全部 stdout**：sidecar.mjs:6-9 的 data listener 在成功后仍追加和扫描 buf。替身继续输出 16,388 字节后监听仍在。不会重复发健康请求；当前日志量不大。 | 启动 Promise 成功/失败后都移除解析监听和 timer；若需诊断日志，明确限定保存量。 |
| EL12 | P2 | S | **引导/包说明落后于外壳**：index.html:149 和 app.mjs:63 仍说关闭后约 10 秒退出，Electron 却立即开始清理；现有安装包没带最后补的作者信息。README 原来写“CI 也产出 artifact”，但实际新 job 尚未首跑。 | 按运行方式同步首启、设置、中英 README；重建包后核对实际 metadata、版本与发布说明，候选包不能冒充现行 Release。 |
| EL13 | P1 | M | **安装产品尚未验收**：已有证据是 dev 截图、替身回归及归档检查；CI 没运行 Electron 打包后的安装/启动测试，本地也未安装。不是已经证明安装失败。 | 用修复后的最终安装包走安装→启动→关闭→再开、旧进程共存、升级保留存档、卸载清理入口；核对安装路径含空格/中文，确保只清理本次测试的配置和进程。先完成本分支 CI 首跑。 |

### 44.3 建议执行顺序

1. 先处理低成本而影响直接的 EL02、EL04、EL05、EL06；EL11 可随启动监听清理一并完成，不额外造框架。
2. 升级运行时 EL01，并统一引擎所有权、自启及退出（EL03、EL07）。EL08 的故障提示适合一并落地。
3. 做 EL09、EL10、EL12 的边界和体验优化；保持现有视觉风格。EL10 可以独立开展。
4. 重建最终安装包并做 EL13，README 与 STATUS 同步记录实际通过项，再决定公开发布。宠物悬浮窗、托盘、多平台安装包和自动更新另按后续阶段推进，不混入此次必修范围。

### 44.4 验证范围与参考

- 复用了上轮 8/8 外壳回归及安装包字节核对，没有为了填写表格再跑全量 Go/前端测试。
- 本轮新增验证仅为隔离实例冲突、悬挂健康请求、分块端口日志和监听生命周期；不使用校园凭据、不修改注册表、不执行真实预约，相关探针均已退出。
- 未新做 GUI、高 DPI、安装/升级/卸载、云端 Electron job 或网络故障恢复验收；第 43 节的 14/14 属当时记录，不能替代本节新增问题的回归。
- 官方依据：[Electron 支持周期](https://releases.electronjs.org/schedule)、[Electron 安全建议](https://www.electronjs.org/docs/latest/tutorial/security)、[会话权限接口](https://www.electronjs.org/docs/latest/api/session)。2026-09-25 核对；选具体升级目标时再确认版本状态。

## 45. beta0.8.0：Electron 发布前修复与安装包交付（2026-09-25）

用户已明确授权“修复 → 提 PR → 合并 PR → 发布安装包 → 更新 README”，替代第 43 节的待定发布决定。继续使用 `feat/electron-shell` 汇总，版本唯一来源更新为 `beta0.8.0`，不覆盖旧 beta0.7.3 附件。

### 45.1 第 44 节问题的处理

- **EL01**：Electron 固定为 **44.4.5**，electron-builder 为 **26.15.3**；lock 使用 npmjs 地址，本地镜像环境变量不入库。运行时和构建依赖不再停留在 Electron 33。
- **EL02**：Electron 构建/安装验收加入 release.needs；只下载七个明确命名的发行 artifact、只上传十个发行文件。验收截图和日志单独保存，不会混入公开发行附件。
- **EL03**：Go 验证已运行实例后输出明确复用协议行；Electron 获得 `owned:false`，退出时不停止共享引擎。安装版暂不新建 Go 自启项，可关闭旧项；便携版和 CLI 行为保留，不提前引入托盘/后台新架构。
- **EL04/EL05/EL06/EL11**：捕获 spawn error；健康探测绑定总超时取消；只接受完整就绪行和合法端口；Promise 完成后释放解析监听与计时器并排空 stdout。
- **EL07**：主进程阻止默认退出，等待自己的引擎有序关闭；无响应时先 taskkill /T /F 并等待。复用引擎 stop 不发送 shutdown、不杀进程。正常 Go 退出和异常父子树回收分别验证，不承诺清理一个早已退出父进程遗留的任意后代。
- **EL08**：启动失败显示可读对话框；自己启动的引擎意外退出或复用服务失联，提示重新打开/退出。无自动无限重启。
- **EL09**：sandbox 显式启用；主窗口限制在本机服务来源，外链走浏览器；拒绝未声明权限；增加 CSP。preload 只暴露外壳标记与退出，IPC 校验发送窗口、主 frame 和来源。移除内联 onclick。
- **EL10**：公开学校查询使用明确只读动作清单和卡片内加载，不禁用导航；场地条件带版本防止旧响应覆盖。存档/凭据写入仍串行。
- **EL12**：首启、设置、中英文 README 与包内说明区分 Electron/便携版；新包 metadata 带作者和描述，版本由 VERSION 注入。保留旧便携版，不宣称本次完成课表/成绩真实验收或应用内预约。
- **EL13**：新增仅在 GitHub 干净 Windows runner 运行的安装闭环检查，检查失败就阻止发行。中文/空格路径、隔离配置、安装包内引擎比对、真实 DOM 与截图、正常退出/重开、便携后台共存、覆盖安装保留存档、卸载不删存档/不改启动项均有检查。此前没有公开 Electron 安装版，所以覆盖安装测试使用同版本包，不虚构跨版本验收。

### 45.2 验证与发布门禁

- 本地 `go vet ./...`、`go test ./...` 通过；Go 单实例复用测试通过。
- 外壳 12 项回归通过：含缺失 EXE、分块端口、挂起健康请求、有序 shutdown、复用后退出、Windows 真父子进程树回收。窗口来源/IPC/CSP 检查和发布说明 12 项检查通过。
- UI 相关 10 组回归共 80 项通过，新增交互 7 项已加入 CI，原 Windows 单 EXE 整机冒烟继续保留。
- 已运行新 Electron + 真实 Go beta0.8.0 的隔离启动/退出探针；打包后程序报告 packageVersion=0.8.0、Electron=44.4.5，截图确认真实荔枝庭院与新的退出说明。Windows 便携版整机冒烟 74 项通过。最终安装产物的验证结果以本 PR 与 beta0.8.0 标签流水线为准，不能用此截图替代安装验收。
- 本地旧 release/win-unpacked 目录被占用，不终止已有程序；本地候选构建改放 release/beta0.8.0，云端干净构建仍使用标准输出目录。本机随后在 NSIS 辅助工具下载遇到网络超时，最终安装包由通过完整验收的 CI 构建提供。
- PR #8 首轮 CI（36044329419）已通过安装到中文/空格路径、引擎字节比对与受控卸载，但抓到真实启动问题：外壳用 `/api/status` 判断就绪，该接口还会探测外网与学校门户，云端首开超过 8 秒。新增纯本机 `/api/health`，启动、存活与安装探针统一改用它；页面网络查询继续后台运行。旧引擎没有该接口时提示先退出旧版，不误杀共享服务、不回退到慢业务接口。安装脚本保留原始启动错误，不让“存档尚未创建”掩盖原因。
- 第二轮 CI（36045656691）本机健康检查已通过，但干净 Windows 的页面未完成渲染；本机独立中文配置首开通过。安装探针新增失败截图、页面状态、模块响应类型与控制台错误取证，仅在隔离测试模式记录，便于定位实际差异；未放宽真实页面通过条件。
- 第三轮 CI（36046559987）取证确认 `.mjs` 被返回为 `text/plain; charset=utf-8`，浏览器严格模块类型检查拒绝执行；preload 正常，页面停在“正在打开你的庭院”。Go 在 Windows 会读取注册表覆盖默认 MIME，本机开发环境没有这一覆盖。资源路由改为明确返回 JavaScript 模块类型，并用模拟错误类型的回归覆盖两条静态路径；修复同时惠及便携版。
- 发布说明由 CHANGELOG.md 唯一提供；发行内容为安装 EXE + SHA256、便携 EXE / ZIP + SHA256、五个平台 CLI。发布成功后应核对 Release 附件及摘要与 CI 结果。

## 46. beta0.9.0 整合与验收（2026-09-26）

本轮按用户确认整合校历、宠物、教务和琴房四条工作线。基线为 beta0.8.0；最终分支 `codex/beta090-release` 通过 PR #13 合并，提交 `9d82a57783ad527133a8c32986f65519b1c473aa`。#10、#11、#12 的实现由本次统一交付，旧 PR 已关闭。本节记录实际通过项与仍受外部条件影响的验收。

已发现并修复的实际问题：主窗销毁后无法从托盘重开；宠物设置模块遗漏于安装包；缩放事件绑定先于页面生成；一次性动作不恢复；自定义大小被 radio 菜单自动勾选；保存失败被吞掉；缩放 IPC 未检查来源；显示事件在 Electron ready 前注册。学校 CAS 分支还存在生产地址白名单不允许自身业务主页，以及未解析响应就宣告认证成功的问题。已修正并增加对应检查。

- 真实开发窗口通过宠物/托盘创建、关主窗后点击宠物恢复、托盘恢复、显隐、三档/自定义缩放、设置滑杆与动作恢复；截图经检查，小尺寸文字增加可读下限。证据留在本机 `.local-docs/release-evidence/beta0.9.0`，不进入公开仓库。
- 本地打包后的 Electron 程序已实际启动两次，版本均为 beta0.9.0 / 0.9.0，尺寸从首次 100% 调为 170% 后在第二次启动恢复；两次退出均回收自己的后台服务。
- 安装包自动验收现在等待宠物启动后再记录，检测实际窗口、菜单回调、主窗桥、重新启动尺寸恢复，保留截图；不再只证明主窗口能显示。
- 本科登录证明使用个人课表响应；研究生课表使用学生信息响应；成绩只验证所选培养层次。仅返回 HTML 登录页面会失败并清除状态。普通安装版通过学校窗口登录，备用 CAS/图形验证码路径仍可用于便携版。
- 学院琴房保留独立账号，不自动套用校园网密码；明确校内 HTTP 范围、清理旧登录、密码用后清空、列表分页。预约记录超出读取上限时提示到原系统查看，不伪装已读全。
- 官方校历已完成本机真实刷新：识别出 2026–2027 第一学期（上课 2026-08-31）及 2025–2026 第二学期，`stale=false`，未使用硬编码代替刷新。
- 本轮尚未完成学校真实账号验收。学校官网校历可达，教务/认证/WebVPN 连接失败；未提交任何真实预约，也未把失败称为完成。需要本人完成学校验证，以及本科与琴房实际授权账号。
- 发布已完成：[PR 检查 36220151127](https://github.com/SzuDesktopTeam/szudesktop/actions/runs/36220151127) 与 [标签检查 36220458700](https://github.com/SzuDesktopTeam/szudesktop/actions/runs/36220458700) 全部通过；[beta0.9.0 Release](https://github.com/SzuDesktopTeam/szudesktop/releases/tag/beta0.9.0) 为公开测试版，共 10 个附件。安装包与便携 ZIP 均附 SHA256。
- 最终安装包为 `szuDesktop-Setup-0.9.0.exe`，115,509,492 字节，SHA256 `9b5dfd37e31ac884b7b9a738823d7ea1c8c843322abb59229ce60dee41dd769e`，与标签 CI 的实际安装验收报告和 GitHub 附件摘要相同。便携 ZIP 的 SHA256 为 `648e07ea36cc2a078a755fdab34d6a7bcafa68d76f9209ec868845659ed5f07e`。
- 发布收尾同步修正中英文 README 的预览表格，补直接安装包链接和实际验收结果；任务仍只在本文维护。后续优先补齐学校真实账号验收，不把模拟通过或安装包通过等同于业务通过。

## 47. 学校真实账号复测（2026-09-26）

本轮使用用户授权的真实测试账号，修复分支为 `codex/school-live-acceptance`，基于已发布 beta0.9.0。以下状态替代第 46 节当时的“学校连接失败”结论；不改变历史发布证据。账号、密码、Cookie、个人课程和成绩不写入公开文档、测试样本或发布包。

### 47.1 已通过的实际操作

- 学校研究生选课原页完成学号、密码和图形验证码登录，显示个人培养信息。“查看我的课表”返回“没有课程安排记录”。统一身份认证与该子系统登录是两步，不能以其中一步代替另一步。
- 运行中的 beta0.9.0 Go 引擎，通过应用备用登录表单取得学校验证码并完成真实认证；不是手工导入浏览器 Cookie。随后 `/api/academic/session` 返回已认证，`/api/academic/timetable` 返回真实当前学期、空排课列表和非空已选课程列表。
- 应用实际点击“读取我的课表”后，显示学校学期、读取时间、无已排课程的说明及待安排课程。已检查实际渲染；未将空排课错误显示为完全没有课程。当前账号没有排课样本，不能据此验收教师、教室、周次和节次的非空映射。
- 修复后的 Electron 候选窗口已由本人完成 WebVPN 二次验证，实际打开官方静音舱列表，包含共享会议室、网络面试间和社区琴房。进入“我的预约记录”后页面显示“没有更多记录”；这只证明页面可打开，尚不证明预约业务会话及记录接口通过。
- 按此前已获同意的会议室使用须知进入详情，可见周日期、图例和“申请预约”按钮，但原生窗口的房间名称与可选时段未加载；切换到下一周仍无时段。未把空白界面当作无可用场地或预约全流程通过。
- 本人完成对照浏览器的二次验证后，同一会议室正常显示名称、08:00–22:00 时段及空闲/已预定状态。实际选择一个时段并点击“申请预约”，打开了包含联系方式、年级的“预约确认”表单；没有填写或点击最终确认，随后关闭表单。**这证明官方浏览器路径可到确认表单，不能代替原生安装版或最终预约成功的验收。**
- 原生窗口返回首页后出现学校自己的“连接服务器超时”提示；对照浏览器稍后刷新也出现 `ERR_CONNECTION_CLOSED`。因此目前不能把空白详情单独归因于外壳，也不能以一次成功加载宣称网络稳定；需要在连接稳定时核对原生业务登录和请求结果。
- 本机点击“查看场地空位”仍返回“无法连接学校预约服务；公开空位查询需要校园网”，没有伪造空列表。官方 WebVPN 登录页可达与本机可直接访问校内查询服务是两件事；后者本轮未通过。

### 47.2 本轮发现并修复

- **P1 / S：WebVPN 官方认证跳转被窗口拦截。** 学校实际跳转到 `authserver-443.webvpn.szu.edu.cn`，原精确域名列表遗漏此地址。补入这一官方 HTTPS 主机，保留对伪装域名、HTTP、自定义端口和其他子域的拒绝；不放宽成通配域名。
- **P2 / S：课表提示指向安装版已隐藏的旧登录卡片。** 初始说明正确，状态刷新后却恢复旧文案；统一为“在上方完成研究生教务登录”。
- 学校窗口连接错误补充受限格式的网络错误代码，不展示可能包含认证参数的完整错误地址。网络恢复后可由学校窗口菜单刷新；本次实际刷新后进入认证页。

上述修改已随 beta0.9.1 发布，不属于旧 beta0.9.0 附件。修复后的外壳已构建并实际启动；域名策略回归与 7 项课表前端检查通过。没有修改系统代理、证书验证或学校安全策略。

### 47.3 尚未通过与继续条件

| 事项 | 本轮实际结果 | 下一步 |
|---|---|---|
| 本科课表 | 当前测试账号进入官方路径后返回 403；不能断言是接口失效，也不能把研究生权限视为本科权限 | 用有该业务权限的账号核对官方入口，再对照本机读取 |
| 研究生成绩 | 原页面出现应用标题但未渲染成绩列表，页面报告 JSON 解析错误 | 恢复官方列表后核对业务会话与真实成绩字段 |
| 安装版会话接回 | 代码和模拟检查已有，尚未用本轮原生窗口的成功教务登录完成接回 | 在安装版学校窗口完成对应教务登录，再点“读取本次登录” |
| 学校预约全流程 | 官方浏览器已选时段并打开确认表单，未提交；原生详情仍为空，返回首页有连接超时，对照浏览器后续也连接中断 | 在连接稳定时核对原生业务登录与请求；只有明确的真实场地和时段指令才创建预约 |
| 学院琴房 | 系统在登录前关闭连接，直接连接与系统代理路径均未成功；未发送学院凭据 | 校内服务恢复后，以该系统授权账号验证权限与只读列表 |

本轮没有选课、退课、创建或取消预约。先完成可读查询与权限核对，尚不能宣称学校业务全部验收通过。

## 48. 像素校园界面打磨（2026-09-26）

按用户本轮要求保留“像素深大 + 木质导航 + 纸张手帐”，改善日常工具的排版与可读性。使用设计技能中适合现有工具界面的审查与渐进改造方法；不引入营销页结构或新的 UI 框架。设计参数为布局变化 4 / 动效 2 / 信息密度 5，保留六个主导航名称、页面路径、账号隐私和现有操作。

| 问题 | 重要性 / 难度 | 本轮处理 |
|---|---|---|
| 顶部场景与欢迎区占用过多首屏，常用信息过于靠下 | P1 / S | 桌面场景由 250px 缩至 196px，欢迎区改为紧凑的文字与单一操作，去掉重复的校园网入口 |
| 主面板、卡片和按钮都使用浓黄与粗边框，层次接近 | P1 / S | 木框保留在导航和外层；内页改为浅纸色、细边框与轻阴影，主按钮统一为深绿 |
| 首页网络状态重复，卡片之间缺少不同重点 | P2 / S | 保留完整的外网与校园认证状态，移除重复徽标；伙伴使用独立场景卡片，资源统计和专注入口重新组织 |
| 正文偏小、表单和服务入口显得拥挤 | P1 / S | 正文提升至 15px，辅助文字 13px；统一标题、留白、表单、状态色和入口反馈 |
| 窄屏与暮色样式需同步调整 | P1 / M | 欢迎区、伙伴卡片、专注条明确折行；保持文档滚动、键盘焦点和减少动效偏好 |

验证：编译并启动隔离配置的真实 Go 程序，用实际浏览器检查首页上下区域、桌面服务与学习页、390px 窄屏六个页面，以及暮色庭院；未发现横向溢出或页面控制台错误。现有 UI 13 项、交互 9 项检查及语法检查通过。两份 README 同步更新。修改已随 **beta0.9.1 安装包发布**；本轮视觉检查不替代第 47 节的学校业务验收。

## 49. 宠物交互与 beta0.9.1 发布（2026-09-26）

本轮按用户要求补齐宠物的大小调节、点击菜单和常用功能，完成实际检查后推送、合并 PR 并发布 `beta0.9.1`。第 47 节学校认证修复与第 48 节界面改进一并交付。

| 事项 | 重要性 / 难度 | 已交付实现与验收 |
|---|---|---|
| 点击宠物只能打开主窗，功能入口不够直观 | P1 / S | 左键/右键打开菜单，显示伙伴状态；提供庭院、农田、学习、主窗口、隐藏和退出入口，实际菜单与导航通过 |
| 大小调节藏在设置，无法直接在宠物上操作 | P1 / S | 滚轮每次 10 个百分点调节，保持 40%–200% 范围；菜单增减与默认大小、原设置滑杆和托盘预设共存，边界、设置同步与重启恢复通过 |
| 宠物位置固定，重启后无法保留摆放 | P1 / M | 按住拖动并保存位置，与大小一同重启恢复；真实指针双轴拖动、点击区分、缩放后位置与重启检查通过，物理多显示器仍待设备 |
| 桌面照料与庭院存档需要一致 | P1 / M | 菜单摸头、喂食、陪玩和睡眠沿用庭院动作与本机存档，保留资源/冷却/精力规则与失败提示；真实隐藏主窗照料、库存和持久化通过，11 项命令回归纳入 CI |
| 新功能与下载说明同步交付 | P1 / M | 更新中英文 README、CHANGELOG 与便携包快速开始，明确 Electron 安装版与便携版区别；PR 与标签全绿，10 个附件公开，下载与校验记录见下 |

发布检查保留 Go、界面、Electron 策略、macOS 凭据和 Windows 安装闭环；`smoke_installer.py` 仅在一次性 GitHub Windows runner 执行，不在开发机伪装 CI 运行。实际检查、PR、标签与附件证据统一在本节，不另建任务文档。

本地验证：Go 测试与静态检查通过；宠物命令 11 项、交互 9 项、庭院 13 项、存档界面 7 项、Electron 策略及 Windows 引擎冒烟通过，发布说明抽取 12 项通过。隔离配置中的真实 Electron + Go 已验证菜单、隐藏主窗后的睡眠/喂食、库存消耗、农田/学习导航、40%–200% 缩放、双轴拖动及位置恢复；另外实际点击检查了原生菜单和大小子菜单。拖动修正后的最终打包程序已重复启动并恢复配置，手动滚轮由 170% 调为 180%，实际拖动按预期向左 110px、向上 125px，位置持久化。证据留在忽略目录 `.scratch_probe/pet091-check` 和 `.scratch_probe/pet091-visual`，未将任何用户账号存档纳入发布。

复查一并修复：桌宠照料保留主窗未提交的原表单节点；无关页面不重绘，不复制密码草稿。菜单使用同一份庭院规则计算离线状态；菜单/滚轮缩放同步更新设置页。拖动使用指针事件发生时的屏幕坐标，避免进程通信延迟导致位置偏差。

发布证据：

- [PR #15](https://github.com/SzuDesktopTeam/szudesktop/pull/15) 已合并，提交 `262f864dceb33346c34b3d8043a92f5c04baa826`；`beta0.9.1` 标签指向该提交。[PR 检查 36233028893](https://github.com/SzuDesktopTeam/szudesktop/actions/runs/36233028893) 与 [标签检查 36233240617](https://github.com/SzuDesktopTeam/szudesktop/actions/runs/36233240617) 全部通过。
- Windows CI 真实执行安装、启动、退出后重开、宠物/托盘与缩放恢复、复用便携引擎、同版本重装保留存档和卸载；没有在开发机安装或覆盖用户配置。
- [beta0.9.1 Release](https://github.com/SzuDesktopTeam/szudesktop/releases/tag/beta0.9.1) 已公开为测试版，10 个附件齐全：Electron 安装包及 SHA256、桌面便携 ZIP 及 SHA256、独立桌面 EXE、5 个平台的命令行程序。
- 已从该 Release 实际下载安装包与便携 ZIP，分别与随附 SHA256 和 GitHub 附件摘要核对一致，保存在本机 `dist/published-beta0.9.1/`。安装包 `szuDesktop-Setup-0.9.1.exe` 为 115,521,981 字节，SHA256 `c45de44c63b74b1f9a17f2f1b698ee04f1786211c2239b762e823dded20484a3`；便携 ZIP 为 5,049,499 字节，SHA256 `385f4fd4488a59760909bb2c92af038ca9c11f49cf8ca620719f3917b05d32be`。
- 中英文 README 下载入口同步为 beta0.9.1，说明点击菜单、40%–200% 缩放、拖动、存档照料与剩余学校业务边界。

学校验收边界沿用第 47 节：研究生备用登录与当前课表读取已有真实证据；本科权限、非空课表、真实成绩、安装版会话接回、完整预约和琴房权限仍待完成。此次宠物与界面发布不关闭这些待办，未提交任何学校预约。

## 50. 四位伙伴与 1.0 收口计划（2026-09-26，开发中未发布）

本节记录本轮本地开发和后续范围；第 32 节仍是当前待办入口。公开下载保持 beta0.9.1，不将本地修改写成已交付，也不另建计划文档。继续保留像素校园、木框与纸张风格，中英文 README 随本轮同步。

### 50.1 伙伴选择与旧存档

| 事项 | 本地开发内容 | 验收状态 |
|---|---|---|
| 基础伙伴名册 | 四位伙伴：荔宝 `libao`、栗栗 `chestnut`、小白白鹭 `egret`、阿青小龟 `turtle`；新存档默认荔宝 | 本地规则与真实窗口检查通过；公开 beta0.9.1 仍为原有两位伙伴 |
| 庭院可视选择 | 伙伴小屋展示形象、名字、简介和当前选择，点击卡片切换；名字、成长、心情分别保存 | 实际选择栗栗/荔宝后桌面立即同步；卡片截图及 420px 窄窗无横向溢出检查通过 |
| 桌面同步 | 桌宠原生菜单增加「切换伙伴」；与庭院共用当前伙伴，选择同步到桌面立绘和照料对象 | 实际菜单切换小白/阿青/荔宝、隐藏主窗后切换及存档当前选择写入通过；已检查白鹭/小龟桌面实图 |
| 旧存档补齐 | 追加缺失基础伙伴，不重排或覆盖已有伙伴，保留当前选择、名字和成长；已有名册达到数量上限时不挤掉旧伙伴 | 旧存档、重复读取和满名册规则回归通过；两次实际启动后的大小/位置恢复通过 |

本轮本地验证：庭院规则 16 项、宠物命令 14 项、交互 9 项，以及最终 Electron `npm run check`（包含宠物视图和策略）通过。宠物命令回归包含 409 存档冲突后换到另一伙伴时清除旧改名草稿的修复，避免把上一位伙伴的名字草稿带到新伙伴。

隔离配置中真实 Electron + Go 已启动两次，完成上述菜单与庭院双向同步、隐藏主窗切换、存档写入及大小/位置恢复；桌面白鹭、小龟和伙伴卡片均检查了实际截图，420px 窄窗没有横向溢出。报告与截图留在忽略目录 `.scratch_probe/pet-roster-check/`；无真实用户数据的伙伴卡片截图已同步到 `docs/screenshot-companions.png`，用于中英文 README 的开发版预览。这些是本轮本地验收，不代表新安装包已发布，也不替代跨版本安装升级或第 47 节学校业务验收；公开 beta0.9.1 不包含本轮四伙伴改进。

### 50.2 1.0 剩余任务（暂不部署后端）

按用户本轮决定，先不部署校内后端，也不搭建 Docker 或服务器发布流程。以下按现有 Windows 桌面版和用户此前要求的本科、研究生、应用内空间预约及琴房范围整理；不擅自把未完成的核心业务改成仅提供外链。此清单列出剩余工作，不把历史已通过检查重新记为未完成。

| ID | 发布前任务 | 当前缺口与完成标准 | 重要性 / 难度 |
|---|---|---|---|
| R01 | 校园网真实认证闭环 | 已有状态识别和已在线出口证据，仍需教学区与宿舍各验证认证、失败提示、注销及重连；外网在线或教务登录不能代替。只记录脱敏结果 | P0 / 中，需现场网络 |
| R02 | 安装版学校登录与会话接回 | 本轮修复学校窗口退出登录后重新读取却保留旧账号会话的问题，回归已通过；原页真实登录、接回个人业务及过期重登仍待验收。研究生备用表单成功的已有证据保留，不替代原生窗口路径 | P1 / 大 |
| R03 | 本科真实课表 | 当前账号进入原页返回 403；需要有本科教务权限的账号，核对真实课程、周次、节次及地点，再核对应用显示 | P1 / 大，需对应权限 |
| R04 | 研究生非空课表 | 当前学期、空排课和待安排课程已有真实验收；只补非空排课样本的教师、教室、周次、节次映射及切换学期 | P1 / 中，需样本 |
| R05 | 本科/研究生真实成绩及分页 | 本轮已实现按学校总数逐页读取，分页失败整体报错，总数缺失明确提示未确认完整；真实学校课程、学分、绩点字段及实际分页仍待验收，不自行编造换算规则。自动并入本机绩点统计可后续做 | P1 / 大 |
| R06 | 应用内空间预约完整流程 | 排查安装版详情空白和业务会话，稳定显示场地与时段，进入学校确认表单；在用户确有预约需求时验证最终结果及记录。继续采用学校原页交互，不为测试占用场地 | P1 / 大，依赖学校服务 |
| R07 | 学院琴房权限与只读查询 | 校内服务恢复后，用该系统授权账号验收登录、房间分页和本人预约记录；它与社区共享琴房是不同业务，不能互相代替 | P1 / 中，需权限和可达网络 |
| R08 | 四伙伴改动交付 | 已完成：PR #17 合并，四伙伴进入公开 beta0.9.2；发布安装包的选择、同步、升级与备份恢复检查通过 | 已完成 / 小 |
| R09 | 跨版本升级与数据恢复 | beta0.9.1 → beta0.9.2 已在干净 Windows CI 通过真实安装升级、旧程序资源清理、庭院/学习/加密账号/桌宠设置保留及备份恢复；1.0 候选包需对应最终升级证据 | 本轮完成；正式版待验收 / 中 |
| R10 | 正式版最终交付 | 对最终候选包完成必要安装/启动/退出检查，修复发现的阻断问题；版本、代码、截图、中英文 README、支持范围、更新说明、附件和校验值对应。保留无变化功能的有效既有证据 | P1 / 中 |

发布前建议补齐的体验项（不与上述核心功能混为一类）：安装版开机自启、宠物显隐偏好记忆、应用内新版本提示、发布者签名。其中版本提示不依赖校内后端；当前安装包未签名，若暂时维持此状态，发布说明需如实标明。

明确延期：校内后端/Docker/服务器部署、云同步与好友庭院、内容/素材热更新、自动下载安装升级、整机 VPN、额外学院栏目、图书馆选座、余额/流量和新桌面平台。方案继续保留在原章节，当前不执行部署；已有 Windows/macOS/Linux 命令行支持继续保留。

执行顺序：四伙伴与本次升级已交付；接下来在学校网络可达、账号权限和样本具备时推进 R01–R07，再完成正式版候选包对应的 R09/R10。如学校条件未具备，只能标为待验收，不能通过改文案宣称功能完成。

### 50.3 校内后端与校外访问边界

用户已决定暂不部署，本节仅保留后续方案。首期沿用第 9 节：HTTPS、一个只读公共 API 服务、SQLite/轻量数据库与定时采集，提供服务目录、公开公告、经核实的校历和版本信息；接口仍为 `GET /v1/catalog`、`GET /v1/notices`、`GET /v1/calendar`、`GET /v1/releases/latest`、`GET /healthz`。数据保留来源和更新时间，客户端缓存可用结果。校内公共后端**尚未部署**。

- 当前公开安装版通过学校官方 WebVPN 窗口办理相关网页业务，便携版使用系统浏览器；实验 VPN 隧道不随公开包构建。学校客户端接入方式以[官方 SecureLink 指引](https://www1.szu.edu.cn/nc/view.asp?id=654)为准，本项目没有交付整机 VPN 功能。
- 本机 Go 的场地/空位查询仍直连学校服务，没有借用学校窗口的 WebVPN 通道。因此 WebVPN 页面能打开，不代表本机只读查询已经可达。
- 校内后端只接入经过确认的特定业务，不等于给客户端提供整机 VPN，也不提供任意校内地址转发。若后续接入个人业务，必须逐项落实每位用户的业务权限、会话隔离和可撤销授权，不能共享一个账号代所有人访问。
- 校内托管不自动获得教务权限，也不自动形成校外入口。部署前须确认允许的校外访问方式、域名与 HTTPS、运行维护和数据来源。校园网登录/注销与用户网卡和出口相关，继续由本机执行；首期公共后端不收集校园密码或个人 Cookie。

### 50.4 内容刷新与程序更新

| 更新内容 | 后续方案 | 当前状态 |
|---|---|---|
| 目录、公告、校历与活动文案 | 获取版本化 JSON，保留来源、缓存和更新时间；无需重装客户端 | 公共后端未部署；已有客户端查询不等于统一内容服务上线 |
| 宠物和场景图片 | 后续可用固定来源的素材清单、版本与校验，验证成功后替换；失败保留现有素材 | 未实现远程素材更新，本轮形象随程序资源交付 |
| 页面逻辑、Go 引擎与 Electron 外壳 | 整包更新并重启，保持界面与本机引擎版本一致 | 自动更新未实现；当前页面资源编译在 Go 程序中 |

当前 Electron 依赖未接入 `electron-updater`，主进程没有自动更新流程；构建使用 `--publish never`，发行工作流仅发布安装包与 SHA256 等现有附件，没有发布 `latest.yml` 等更新清单。对本地已下载的 beta0.9.1 安装包读取 Windows 签名结果为 `NotSigned`；SHA256 用于核对下载完整性，不能替代发布者签名。尚无自动回滚实现。

建议先实现版本提示与更新说明，再沿用现有 NSIS 安装格式接入 `electron-updater`：将安装包和更新清单发布到客户端可访问的 GitHub Releases 或 HTTPS 静态服务，提供「发现版本 → 确认下载 → 重启安装」流程；后续再考虑灰度发布。此路线依据 [electron-builder v26 自动更新文档](https://www.electron.build/v26/docs/features/auto-update/)。保留旧安装包和升级前存档备份，并完成实际跨版本验证；不要把发布新安装包、同版本重装或静态校验写成已经实现自动更新/自动回退。

## 51. 向 1.0 推进：beta0.9.2（2026-09-26）

本轮不部署后端。交付第 50 节的四伙伴改动，同时处理 R02 的旧账号残留、R05 的成绩分页和 R09 的升级/备份证据；没有使用真实账号登录、注销校园网或提交学校预约。

| 项目 | 本轮结果 | 验收范围 |
|---|---|---|
| 四伙伴 | 已纳入公开 beta0.9.2；荔宝/栗栗/小白/阿青卡片、桌面切换和存档迁移交付 | 本地及最终安装包检查通过 |
| 成绩分页 | 根据学校 `totalSize` 顺序读取；缺总数明确未确认完整；中途失败、总数变化/消失、空页、重复/重叠分页及上限均不返回假完整成绩；加整体期限与请求取消 | 本科/研究生分页回归已通过；实际学校字段和分页仍待真实账号验收 |
| 退出后旧会话 | 学校窗口无 Cookie 时也请求替换所选业务会话；Go 先清除对应旧会话再校验，失败不会留下上一个账号 | 旧实现 8 项回归失败，修复后通过；四种业务的 Electron 同步入口通过。另一套学术会话保持，手工保存的独立会话不受影响 |
| 学校错误提示 | 外壳按 Go 的 `message` 字段显示错误，兼容既有 `error` 字段；不再把权限不足和连接失败统称为尚未登录 | 使用真实响应结构的 Node 检查通过，包含 403 权限提示 |
| 跨版本安装 | 固定下载并核对 beta0.9.1 SHA256，安装旧版写合成数据，再升级 beta0.9.2；核对数据、旧程序文件清理及卸载保留数据 | 干净 GitHub Windows runner 已通过，未改动开发机用户安装 |
| 备份与恢复 | 真实 Electron 窗口点击导出，读取下载文件；改变资料后通过文件选择事件分别取消和确认恢复；核对学习记录与伙伴名字/成长，并恢复升级检查原数据 | 本地真实 Electron + Go 已通过；安装版也执行同一检查。数据均为合成资料 |

预约详情空白仍没有可证实的外壳代码根因；现有记录含原生窗口与对照浏览器连接失败。继续保留学校原页、安全策略和 HTTPS 域名限制，等待网络稳定后验证详情、时段、确认表单与预约结果。没有把代码回归写成学校业务已验收。

首轮 PR #17 CI（36241522361）：通用测试、macOS、便携版和全部 CLI 通过；Windows 升级已验证旧资源清理、数据文件逐字节保留及新引擎解密旧账号。随后备份脚本在服务端已写入、前端尚未收到保存响应时触发导入，被页面忙碌状态拦住。脚本改为等待文件输入框可用后，PR 重跑（36241874533）及最终发布运行均通过完整安装检查。

发布状态：PR #17 已合并；[beta0.9.2 发布运行](https://github.com/SzuDesktopTeam/szudesktop/actions/runs/36242237913) 全部检查通过，10 个公开附件齐全。安装包和便携 ZIP 均实际下载，SHA256 与校验文件及 GitHub 附件 digest 一致。正式版 1.0 尚未发布，学校业务未因本次回归而标为通过。

本轮交付校验：

- `szuDesktop-Setup-0.9.2.exe`：`454e92d6ef8b888fac3fb15363bce62845088494300d69959fd6da89c67b64e4`
- `szudesktop-beta0.9.2-windows-amd64.zip`：`bd51e71f43b75a394737fce16880d2e3b1339dab3e22cd3944f6cd50c9f399ed`

## 52. 宣传前的第一印象与首次体验（2026-09-26，源码未发布）

用户目标是让项目有足够吸引力，可以向同学宣传并获得真实反馈。本轮在 `codex/campus-first-impression` 保留像素深大、木框、纸张与原创四伙伴，没有替换成通用管理后台，也没有扩展学校业务或部署后端。

审查发现：首页把连接状态、空待办、校园链接和庭院缩略图等权排列，主角不清楚；校园景观被纸面遮住，伙伴太小；首次引导先讲本地文件路径和登录；独特的伙伴互动与专注奖励藏在二级页面；README 主截图仍是旧版，下载前有较长协议说明，反馈入口不明显。

| 优先级 / 难度 | 项目 | 本轮结果 |
|---|---|---|
| P1 / 中 | 首页视觉层级 | 缩短品牌区，文山湖场景与可互动伙伴成为主视觉；待办和专注成为第二层，校园工具和网络状态收拢到下方；删去废弃首页样式 |
| P1 / 中 | 第一次就能体验 | 首页四伙伴切换、点击摸头、5 分钟专注、成熟农田直达；使用真实本机存档，无演示课表或虚构数据 |
| P1 / 小 | 欢迎引导 | 四伙伴迎接，优先进入伙伴小屋或学习工具；保留本机数据、非官方性质、学校业务边界和退出说明，移入可展开说明；可跳过，页脚可重开 |
| P1 / 小 | 宣传与反馈 | 中英 README 前置 Windows 下载、三个真实使用场景和问题反馈；技术协议后移保留；首页页脚可直接打开现有 GitHub Issues |
| P1 / 中 | 首屏操作细节 | 照料、切换、开始专注保留待办草稿；处理冲突后伙伴变化时不串用改名草稿；指南 Esc 不沿用上次跳转目标；收获提示按时更新 |

本轮证据：Go 嵌入页面构建成功；庭院 16、工作区 7、交互 9、网络显示 5、桌宠命令 14 项既有相关检查通过，共 51 项。在独立测试存档上使用真实 Go 服务，通过浏览器验证首页切换伙伴、摸头反馈、未提交待办保留、5 分钟专注启动、待办保存、首页直达农田、指南进入伙伴小屋和重复打开后 Esc 留在原页。1280 与 420 像素宽度检查无横向溢出；420 宽度伙伴小屋也无横向溢出。未访问真实学校账号，未改动用户安装或个人存档。

预览图为 `docs/screenshot-home-preview.png`，使用隔离测试存档中的一条手动输入待办；中英文 README 明确写明源码预览未进入当前 beta0.9.2 安装包。本轮没有发布新版本，未把前端浏览器检查表述为新安装包验收。

后续宣传定位：以像素校园、桌面伙伴、专注与庭院吸引首批试用；学校个人查询和预约按实际验收范围介绍。下一步先收集同学对首次打开、每天会使用什么、何处卡住的反馈，再决定进一步的庭院美术、服务页密度和留存功能。R01–R07 的学校业务验收仍按第 50.2 节推进，不因本轮视觉改版而关闭。
## 53. 庭院场景与每日回访体验（2026-09-26，源码未发布）

接续第 52 节的首页打磨，保留像素校园、纸张和木框，集中改善真实农田与伙伴小屋。当前公开安装包仍为 beta0.9.2，本节改动尚未提交、推送或发布新安装包。

| 优先级 / 难度 | 项目 | 完成结果 |
|---|---|---|
| P1 / 中 | 湖畔农田场景 | 新增原创庭院插画，六块可操作农田放进场景；成熟、成长、浇水、锁定状态可区分，收获篮显示实际库存；窄窗口自动改为两列 |
| P1 / 中 | 伙伴小屋 | 扩大伙伴生活场景，加入湖景窗和书架，照料按钮与三项状态紧邻小屋；成长进度、下级解锁和每日目标同页可见，四伙伴选择与日记层级重排 |
| P1 / 小 | 每日目标与收藏 | 目标可直接进入对应功能，达到要求后领取；纪念章和作物图鉴按真实完成/发现状态显示，沿用既有奖励规则，无连续签到要求 |
| P1 / 小 | 奖励反馈 | 保存成功后根据前后存档差量显示收成、荔枝币和伙伴成长；真实跨级才提示升级，失败/冲突不显示到账，同一目标/收获不能重复领奖 |
| P2 / 小 | 操作细节 | 照料保留改名草稿及焦点；修复点心喂食后说明面板自动折叠；庭院规则和历史日志可展开，减少常驻说明文字 |

验证：Go 嵌入资源构建成功；庭院 16、工作区 7、交互 9、桌宠命令 15、奖励反馈 8 项检查通过，共 55 项。奖励检查覆盖重复领取、待办反复切换、真实跨级、换伙伴不误判升级以及下一天补给。在隔离存档和真实 Go 服务上，浏览器完成补给、摸头、保留改名草稿、种植、浇水、成熟时钟、收获、每日目标领取、纪念章收藏、图鉴点亮和作物喂食；实际验证点心面板保持展开。1280 与 420 像素宽度均无横向溢出，农田与小屋已检查实际截图，最终页面没有浏览器控制台错误。没有重复学校业务或安装包验收；R01–R07 仍未关闭。

交付：中英文 README 同步说明未发布边界，截图为 `docs/screenshot-farm-preview.png` 与 `docs/screenshot-garden-preview.png`，均来自独立测试存档。原 18770 本机预览换为最终构建，保留此前选择的小白与待办记录；单独测试使用另一份存档，不改动用户安装和学校账号。

图片资产：使用内置图片生成工具的原生模式（未使用 CLI），保存到 `desktop/assets/garden/courtyard.png`，1536 × 1024。生成的是原创湖畔花园场景；画面不预先绘制农田或交互按钮，真实状态继续由应用渲染。

<details>
<summary>庭院底图的最终生成提示词</summary>

```text
Use case: stylized-concept. Asset type: original pixel-art background for an interactive desktop campus gardening game. Create a beautiful highly polished 3:2 landscape illustration, 1536x1024 if possible. A small cozy subtropical garden beside Shenzhen University's Wenshan Lake, viewed from a slightly elevated straight-on 2D RPG angle (not isometric). Crisp carefully clustered pixels, approximately a 512x342 native-resolution artwork enlarged by nearest neighbor, 16-bit farming game feeling, original objects and architecture. Quiet mint sage and olive grass, deep teal foliage shadows, warm cream sun highlights, terracotta wood, small pink bougainvillea flowers. COMPOSITION CRITICAL: central 68 percent width and central 72 percent height is a continuous flat open low-detail softly mottled green lawn, no planting beds, no crops, no central objects, no strong cast shadows; this space will hold six REAL interactive soil beds rendered separately. Detailed framing only at the edges: small rustic tool shed at upper-left corner, a couple of lychee trees and subtropical palms at far left and right, glimpse of calm blue lake and distant light campus buildings along the very top edge, short wooden fence along top, flowers and small stones along bottom outer corners, small watering can by shed. Leave center clear all the way across. Calm inviting afternoon, handcrafted cohesive pixel clusters, rich but not noisy. No text, no letters, no icons, no UI, no labels, no people, no animals, no existing franchise characters, no crop plots painted into image. Entire composition is usable game environment art, not a screenshot or mockup.
```

</details>


## 54. 面向真实用户的完整产品审查（2026-09-27，分析与提案）

> **历史审查基线**：第 54.1–54.8 节保留实施前观察、数值和提案，不删改当时发现的问题。当前处理状态以第 54.9 节与第 55 节为准；其中“当前”“尚未实现”均指审查当时，不代表后续源码仍未处理。

### 54.1 结论、基线与证据边界

结论：当前已有可辨认的像素深大风格、能工作的本机庭院和桌面伙伴，适合准备一轮有明确范围的小规模试用；不宜按“学校业务全部打通的正式 1.0”大范围宣传。最大的缺口依次是使用定位不够集中、常驻陪伴断点、成长与专注的奖励方向冲突、中期内容不足、学校业务验收、宣传与安装包交付一致性。继续换背景和加图标，不能单独补齐这些问题。

本轮基线为 codex/campus-first-impression 工作树，含第 52、53 节的未提交改动。GitHub 只读检查确认最新三个发行记录均是预发行版，最新为 beta0.9.2；新版首页、农田与小屋尚未进入公开包。浏览器检查使用同一真实 Go 预览，未登录学校账号、未提交预约、未修改个人资料或重新安装软件。安装版特有行为以 Electron 源码和已有验收记录判断，不能把便携页面的测量当作安装版截图。

本次审查中的“事实”来自当时源码、现场页面或明确已有证据；“判断”是产品/设计推断；“提案”在审查时尚未实施，后续实现见第 55 节。没有真实用户留存、转化或性能统计，不把代码推演当作实际用户流失证据。原有升级、备份恢复、四伙伴同步和 55 项相关检查保留为各自版本的证据，不重新列为缺陷，也不代替新候选包验收。

### 54.2 用户为什么需要它：定位仍有三个方向竞争

现在同时存在校园工具、桌宠养成、专注待办三条线，各自有入口，却没有充分形成相互帮助的日常体验。普通同学首先需要知道“什么时候值得打开这个软件”，而不是理解所有模块。

| 定位选择 | 当前适配程度 | 代价与建议 |
|---|---|---|
| 深大校园全能工具 | 目标有价值，但关键个人业务尚未完整验收；还要与既有官方入口竞争 | 保留为 1.0 学校能力目标，宣传前逐项证明便利和可靠，不用功能数量代替完成 |
| 通用桌宠或农场游戏 | 已有基础，但动画、内容量和个性化明显有限 | 不与成熟桌宠拼角色数量、联机或庞大内容库 |
| 面向深大学生的桌面学习伙伴 | 像素校园、离线庭院、桌宠和专注已有共同基础 | 推荐作为首批试用定位：学习时安静陪伴，完成小事留下成长，需要时能少绕几步办校园事务 |

推荐首先招募“常用 Windows 电脑学习、愿意接受像素伙伴”的深大学生。本科与研究生业务都保留，但按身份呈现入口，不让两套表单同时压给所有人。手机优先用户是否需要额外桌面软件，必须访谈验证，不能从“全校都有手机”推导桌面需求。暂不因此开发移动端。

### 54.3 同类产品值得学的是使用理由

仅比较本轮核对到的官方说明，不引用下载量、评分或所谓行业留存基准。

| 参照 | 官方明确的体验组织方式 | 对本项目的启发 |
|---|---|---|
| [Spirit City: Lofi Sessions](https://mooncubegames.com/home/spirit-city-lofi-sessions-press-kit/) | 将专注工具、声音氛围、房间装饰和发现伙伴放在同一个环境中 | 学习过程中就能享受环境，成长带来可见变化；场景本身应有用途 |
| [Finch](https://finchcare.com/about-finch) | 完成个人小目标让伙伴获得冒险能量，返回后分享故事与发现 | 用户照顾自己时，伙伴关系也有推进；回访应有新内容，而非只领币 |
| [VPet](https://github.com/LorisYounger/VPet/blob/main/README.md) | 多种交互与动画，支持用户自定义内容 | 角色的动作、反应和个性决定陪伴感；先把荔宝做好，比继续加单帧角色更有效 |
| [深大企业微信官方指引](https://www1.szu.edu.cn/nc/view.asp?id=624) | 已列出课表、成绩证明、体育场馆及共享钢琴房预约等入口 | 同学已有办事途径。szuDesktop 需要用更少步骤、桌面提醒或集中信息证明额外安装的价值 |

这些启发不要求照搬它们的全部功能。音乐、图片和角色如要引入，应使用已有许可明确的素材或原创资源；本轮不新增商业游戏素材或联网功能。

### 54.4 外观仍有的毛病

以下是设计判断，结合第 52、53 节真实截图及当前页面，并非宣称布局已经不可用。

1. **景物与操作还没有融为一体。** 新农田用了好背景，但六块地仍是整齐摆在图片上的矩形板，每块附同样的标题、说明和按钮。远看更像六张功能卡片，缺少真正“在湖边照顾菜地”的空间感。建议保持六块真实地，统一土壤纹理、透视与大小；点地选择后集中展示动作，或用清楚的悬浮工具条。要保留键盘和明确文字操作，不能只剩猜图标。
2. **美术的像素尺度不一致。** 校园插画细节密，伙伴和作物更粗，CSS 木框又是另一种精度。建议定义场景、角色、物件和界面四层的像素密度、描边和阴影方向，优先重做荔宝与作物状态，不再随意混入不同游戏的素材。
3. **木框和纸张仍占太多视觉权重。** 顶栏、主导航、页标题、内卡片、按钮各自有边框和阴影，弱化了真正值得看的伙伴。保留主导航与场景框，普通说明用留白和字级区分。
4. **新旧页面完成度不均衡。** 首页/庭院有主视觉，学习、服务与设置仍主要靠卡片向下堆。用户从宣传页进去会感到几个页面属于不同完成阶段。
5. **关键入口太深。** 在 1280×900 的便携页面中，学习页约 3196 像素高，课表标题约在 y=1409、成绩约在 y=1892；服务页约 3106 像素高，常用学校入口约在 y=2301。这是这次未登录状态的测量，不是全部状态的固定高度。应按“专注/课表/成绩”和“空间/公告/常用”分区直达，学院琴房按需展开。安装版额外账号卡还要一起整理。
6. **细字和低对比提示仍多。** 新农田的时间说明约 10px、浇水标签约 9px，部分每日目标文字为 11–12px；桌面缩放后可读性需要提高。标题保留像素字，业务说明、数值与表格用清楚的正文；颜色表达状态，同时保留文字和形状。
7. **首页对回访用户仍偏展示。** 固定大标题、四伙伴选择每次都出现，实际“刚才做什么、接下来做什么”不够突出。首次欢迎可以突出角色；回访应优先显示进行中的专注、今天一件待办和可收获状态，不编造课表或自动填校园卡号。
8. **校园身份还停留在背景与名字。** 文山湖、白鹭、荔枝是好的起点，但成长奖励与台词大多通用。可做少量具体的荔园生活片段、季节变化和可收藏的校园角落，让画面有记忆点；丽湖用户需求应先确认，避免所有人的校园都被默认成一个场景。
9. **动效存在，但角色表现仍单薄。** 目前有 CSS 待机和动作反馈，不能说完全没有动画；然而除栗栗外其余三位主要是单帧立绘，动作多靠整体位移/缩放。重点补开心、睡觉、困倦、专注与收获反应，而不是给所有按钮增加跳动。
10. **“暮色”不等于完整夜间体验。** 当前主要是背景混色和纸面色值变化，新卡片有大量固定浅色；既然名称是庭院光线，不应宣称完整暗色模式。后续统一场景与小屋光线；真正夜间阅读模式是否需要，由使用反馈决定。

### 54.5 庭院好不好玩：实际数值暴露的矛盾

证据主要位于审查时 desktop/assets/garden/engine.mjs 的 CROPS、DECOR、PETS、QUESTS、level 与 act。以下均为**实施前规则计算**，保留为调整依据；新数值见第 55.2 节。实际时长会受用户操作、离线和选择影响。

| 当前规则 | 可复核结果 | 问题与改进方向 |
|---|---|---|
| 专注 5、25、45 分钟均给 10 成长，币按分钟给 | 九次 5 分钟为 90 成长、45 币；一次 45 分钟为 10 成长、45 币 | 奖励鼓励切碎专注。主要成长按完成分钟计算，开始一小段可以保留有限鼓励；不要引入监控学习行为 |
| 萝卜 1 分钟、草莓 5、蓝莓 20、荔枝 60；收获都给 10 成长 | 扣种子成本，每块田每分钟理论净币约 8、2.4、1.1、1.23；萝卜成长效率为荔枝的 60 倍 | 高级作物的等待没有相应价值。让不同作物适合一次休息、一次专注、一个下午，长周期有独特收藏或较好单次回访收益 |
| 摸头间隔 10 秒，+2 成长、+3 亲密度；初始亲密度 10 | 从第一次立即摸头算，30 次最短约 4 分 50 秒满亲密度；50 次约 8 分 10 秒升到 3 级、解锁全部作物 | 机械点击太快耗尽成长目标。保留随时互动的情绪反馈，成长主要关联真实完成的专注、有限日常互动与陪伴里程碑 |
| 买种子和种植用当前伙伴的等级 | 从高等级荔宝换到 1 级栗栗，已能种的荔枝再次被等级拦住 | 自由换伙伴带来功能倒退。庭院已经解锁的能力永久保留，伙伴成长继续独立 |
| 四作物、三装饰、六块田、六纪念章；三装饰共 185 币，后三块田共 270 币 | 按每日一次 25 分钟专注、一次种植收获出售、一次喂食、三次摸头并领每日奖励的引擎推演，可第 1 天购齐装饰、第 3 天开满农田；第 7 天余额 559 币 | 只是说明固定消费目标容量偏小，不代表用户实际第三天流失。需要可见的中期外观/故事目标，不宜仅涨价和延长倒计时 |

伙伴差异也主要集中在外观、简介和初次招呼，成长和照料规则相同。建议以不同台词、动作、偏好和共同经历建立个性，不做“某只加收益所以必须选它”的强弱差异。已有四位足够第一轮，把荔宝做成让人记得住的主角优先。

可执行的七次回访内容提案：首次选择/命名 → 首次收获 → 第一次完成自己的小事 → 解锁一处窗景或小角落 → 伙伴讲一段校园片段 → 留下一张专注纪念物 → 看见本周真实完成记录。它应按个人节奏推进，允许跳过和隔天回来，不是必须连续七天签到；具体内容是待验证提案，不是已完成任务。

### 54.6 日常工具与常驻体验的完整性

- **真实跨日缺陷**：旧浏览器页在 9 月 27 日仍显示 9 月 26 日和昨日每日目标，新开同服务页面显示 27 日。app.mjs 的日期仅在 render 更新，clocks 不更新每日板；昨日已领补给可能保持禁用，昨日可领目标也可能点击后才报未完成。需按日期变化和窗口恢复可见刷新相关区域，保留草稿，不无条件重建全页。
- **专注完成提示留在主窗口**：当前改标题、活动条和应用内提示；Electron 关主窗是隐藏，桌宠未接入完成事件。应给一次可关闭的伙伴/系统提示，点击进入休息或查看成果，尊重勿扰。
- **常驻偏好缺口**：安装版没有开机自启，宠物隐藏状态不保存，重开会再次显示且置顶。需主动选择的自启、显示/隐藏记忆、临时不打扰及明确恢复入口，不能默认擅自开机启动。
- **待办功能不完整**：目前只有新增、完成和删除，数据没有日期/完成时间；首页“今天的小事”显示全部历史条目。应补编辑、已完成折叠或归档、日期记录与简单周回顾；先不做复杂项目管理。
- **专注仅有 5/25/45 档**：缺自定义时长、休息衔接和记录回看，待办与计时也未绑定。先把后台提醒、取消后记录含义和一轮完成体验做通，再决定暂停、声音和更多模式。
- **公告没有形成“我的学校信息”**：notices.mjs 的来源每次初始化为本科教务部；切换只存在当前实例，没有保存学院订阅，也没有最近阅读状态。应记住用户主动选的学院/层次，显示更新时间和上次内容，保留官方原页；设置中的自由文本“学院/一句话”不是订阅配置。
- **声音和即时反馈要节制**：可以补可关的轻收获声或原创环境音，但需要先把专注不被打断做好。音效不是首发硬门槛。
- **性能待测而非已差**：本轮没有长时间 CPU、内存、耗电或物理多显示器数据。正式候选包应测主窗打开/隐藏、桌宠常驻、睡眠恢复和常用缩放各一次，按结果修实际问题；不因 Electron 名称就断言资源占用过高或要求换框架。

### 54.7 校园工具需要把一个办事过程完成

沿用第 50.2 节 R01–R07，不新建另一套重复的学校验收清单。

| 业务 | 当前边界 | 用户感知上的完成标准 |
|---|---|---|
| 校园网 | 有协议实现与诊断，现场认证仍有剩余范围 | 登录/在线/注销的含义明确；失败知道下一步，已验证范围与环境对应 |
| 学校账号 | 安装版原页登录后还需选择业务并接回会话；研究生备用路径有部分真实证据 | 本科/研究生入口清楚，认证回来能继续原任务；过期、权限不足和连接失败有不同处理 |
| 课表 | 本科权限样本不足，研究生非空排课字段仍待验收 | 用户能准确看到课程时间地点和周次，知道最后更新时间；空课表不等于读取失败 |
| 成绩 | 解析/分页已有测试，真实数据仍缺验收 | 课程、学分、成绩/绩点与官方对应，完整性明确；手动导入与在线结果关系清楚，不擅自换算 |
| 社区预约 | 有原页入口与空位/规则查询，安装版完整路径仍待验收 | 从找场地、看可用时间、认证、确认到查看结果/取消有连续路径；最终写操作由本人确认，不为了测试占座 |
| 学院琴房 | 与社区琴房不同，仍依赖特定账号权限和网络 | 只向需要的用户展示清楚入口；不给普通用户首先摆一张不知道是否能用的密码表单 |
| 学院公告/校历 | 已有可用公共查询与部分缓存 | 记住选择、准确标来源和时间，失败可看上次有效结果或进入官方页；不把所有学院均可直读作为承诺 |

学校原页放在应用内并不天然不完整；如果登录、上下文返回和结果确认连贯，它可以是合理成品方案。真正需要补的是完整路径和可靠性，而不是为了“看起来原生”强行重写学校所有表单。

统一登录入口也不等于统一凭据：校园网、教务、WebVPN 和学院琴房的权限/会话必须保持真实区别，但让产品根据用户目标引导，不要求用户理解 Cookie、接口和多套认证术语。校内后端不能自动解决个人账号授权、二次验证和校外访问问题。

### 54.8 下载、信任、推广和维护

1. **画面与下载需同版。** 当前 README 已诚实标注源码预览，但用于公开推广前仍须交付包含这些画面的安装包、对应截图与更新说明。新 check-rewards.mjs 尚未接入现有 CI 检查清单，交付时补齐。
2. **更新可先小步做。** 当前为手动下载且未签名，设置只有 Releases 链接。先做版本提示、更新说明和唯一推荐下载，不需要先部署校内后端。签名与自动更新是独立工程决策，不伪装成一两行的小修，也不让用户关闭系统保护。
3. **安装版许可材料有待补齐。** Electron files/extraResources 未列根 LICENSE；对本机现有 win-unpacked 和 app.asar 的只读检查未见项目许可文件，便携打包则明确加入。需随附项目、第三方和字体许可并核对最终候选包。本轮未重新拆开公开安装包，不将本机证据称为公开包实测或给出法律定性。
4. **隐私文案已有冲突。** SECURITY.md 把业务 Cookie 统一写成系统加密落盘，而学校原页会话实际为内存保留。需按登录方式讲清楚何时保存、退出后保留什么、怎样删除，确保 README、设置和安全说明一致。没有证据表明本轮存在凭据泄漏。
5. **反馈入口有，但门槛偏高。** 当前仅 GitHub Issues，且版本信息靠用户填。保留已有表单，补一键复制不含凭据的版本/环境信息，再使用维护者选定的便捷反馈渠道。不能替用户创建群、公布私人联系方式或自动上传诊断资料。
6. **缺短而可信的展示内容。** 需要一段 15–30 秒实机演示：桌宠出现 → 开始专注 → 完成提示 → 收获/小屋变化；配三张同版本图片和明确下载按钮。静态长截图无法证明桌宠有多好用。当前没有这条完整演示，不使用概念动画冒充成品。
7. **缺自然分享理由。** 可以做本地导出的庭院明信片或本周专注卡，让伙伴和场景有个性。默认不含学号、成绩、课程等个人学校信息；先不依赖社交后台、排行榜、好友系统。
8. **维护需要有主次。** STATUS 已超过 2000 行，历史阶段中的“当前”容易与最新发布混淆。继续同一文档维护，以第 1 节当前摘要、50.2 学校验收与本节产品问题为入口，历史明确标注快照；不要再复制一套“1.0 总清单”。应用渲染、学校业务、奖励规则已有模块，应按下一次实际改动逐步整理，当前不为审美升级重写框架。

### 54.9 本次新增产品任务：重要性、难度与验收

以下为审查后的**当前执行状态**。源码实现不等于已发布；最终本地候选包及浏览器证据见第 55.3 节，实际安装与学校业务验收分别保留边界。P1 为首批试用或已承诺 1.0 的关键项，P2 为提高持续使用和表现力的后续项；S/M/L 沿用第 2 节，难度不是工期承诺。学校验收继续只按 R01–R07 结算。

| 编号 | 优先级 / 难度 | 任务 | 当前状态与剩余验收 |
|---|---|---|---|
| UX01 | P1 / S | 明确首批用户与一句话价值 | **文案已调整，试用待做**。聚焦深大学生的桌面学习伙伴；需真实同学确认能理解使用场景，不宣传未验收业务 |
| UX02 | P1 / M | 源码、安装包、图片同版交付；奖励检查入 CI | **本地交付已核对，公开发布待做**。新增检查已列入 CI；beta0.9.3 候选安装包与便携包已构建，源码、构建资源及同版截图已核对，公开下载仍为 beta0.9.2 |
| UX03 | P1 / S | 修跨午夜日期与每日状态 | **已修并通过时钟回归**。日期变化及恢复可见时刷新每日状态并保留草稿；未进行真实跨午夜现场验收 |
| UX04 | P1 / S | 已解锁庭院能力不随伙伴切换退回 | **规则已实现并回归**。庭院永久保留解锁，旧存档从伙伴与已有作物恢复；伙伴成长各自保留 |
| UX05 | P1 / M | 隐藏主窗时专注完成提醒 | **源码已实现，候选包待验收**。完成后一次提醒，可关闭、勿扰并返回学习页；完全退出后不通知 |
| UX06 | P1 / M | 安装版自启与宠物显隐偏好 | **源码已实现，安装版实测待补**。Windows 自启由用户主动开启，显隐和置顶选择保留；不默认登记自启 |
| UX07 | P1 / M | 校园服务与学习页按目标分区 | **已实现并检查实际页面**。学习分专注/课表/成绩，服务按空间/公告/常用/琴房进入；真实学校业务另行验收 |
| UX08 | P1 / S→L | 学校入口承诺与 R01–R07 对齐 | **部分完成，R01–R07 未关闭**。培养层次可记忆，场地查询区分访问失败与网络要求。用户确认在校园网内、暂无本科权限账号；研究生页可达但登录过期，等待本人登录；本机查询访问失败，不能记为通过 |
| UX09 | P1 / M | 重新平衡专注、摸头与种植成长 | **规则已实现并回归，长期平衡待试用**。按完成分钟给成长、日常摸头有限成长、长周期作物收益提高；不靠涨装饰价格拖延 |
| UX10 | P1 / M | 待办编辑、完成记录与归档 | **已实现并通过真实交互**。编辑文字/日期、完成分类、归档后记录保留；旧记录未知日期保留未知 |
| UX11 | P1 / S | 更低门槛的反馈与环境信息 | **已实现并实测复制**。实际剪贴板仅含当前版本、Windows 与便携运行方式；信息可预览，不含账号、Cookie 或成绩，不自动上传 |
| UX12 | P1 / S | 安装版许可材料与凭据说明一致 | **已补并核对构建内容**。便携 ZIP 与安装版构建目录均含项目/第三方/字体许可；凭据说明按原页内存会话与手动导入分别说明。未将构建目录检查视为实际安装验收 |
| UX13 | P1 / M | 第一轮定向试用及同版演示 | **待完成**。尚未开展真实同学试用或拍摄 15–30 秒完整实机演示；不能以内部回归代替 |
| UX14 | P2 / M | 学院/层次选择与公告来源记忆 | **已实现，重开偏好实测通过**。保存主动选择的公告来源和本科/研究生层次；未新增全量订阅或阅读历史系统 |
| UX15 | P2 / M | 回访首页突出下一件事 | **已实现并检查实际页面**。回访收紧欢迎区，突出当前专注、待办和收获；同版首页截图已更新，不编造学校数据 |
| UX16 | P2 / M | 统一像素密度、描边、字级与信息色 | **部分完成**。提高部分字号、状态色与层级；场景、角色和全部控件尚未完成统一美术重制 |
| UX17 | P2 / M | 农田交互融入场景 | **已实现并通过真实交互**。六块田可选择，种子与种植/浇水/收获/开田集中在工具面板；已实测收获、换田、种草莓和浇水。1280px 与 420px 宽度无横向溢出，窄窗可滚到工具面板 |
| UX18 | P2 / M→L | 主伙伴表情与四伙伴个性 | **已实现并核对状态图**。四位伙伴均有普通/开心/低落/睡眠四状态，主界面与桌宠一致；共 56 条差异化照料台词 |
| UX19 | P2 / M | 七次回访的可见内容与中期目标 | **部分完成**。七次非连续来访的小故事、纪念物与回忆册已实现；长期目标容量和平衡仍待试用，未扩大装饰库 |
| UX20 | P2 / M | 专注与待办关联、周回顾 | **已实现并通过真实交互**。实际完成关联待办的 1 分钟专注，切页后领取并写入周回顾；支持 1–120 分钟和最多 365 条历史，不自动勾完任务，暂停/休息模式未做 |
| UX21 | P2 / S→M | 应用内版本提示与更新说明 | **已实现并实测查询**。手动查询读到公开 beta0.9.2，并正确区分本地候选 beta0.9.3；提供渠道、版本说明与下载入口，没有自动下载或安装 |
| UX22 | P2 / M | 安静陪伴与适度氛围 | **部分完成**。通知、勿扰、置顶与减少动画可调；没有新增声音或全屏应用自动识别，实际通知行为待候选包验证 |
| UX23 | P2 / M | 本地导出庭院分享图 | **已实现并通过最终构建实测**。带像素校园背景、当前伙伴和实际庭院/累计专注数据的 1200×800 PNG 可先预览再保存；已保存并查看真实图片，不含学号、课表或成绩 |
| UX24 | P2 / S→M | 常驻资源、缩放、多屏与恢复抽查 | **待候选包实测**。需记录打开/隐藏、恢复和常用缩放；物理多显示器仍未验收 |
| UX25 | P2 / S | 单一状态文档的当前/历史区分 | **本轮已整理**。第 1 节为当前摘要，第 54.1–54.8 节保留审查基线，第 55 节收实施与证据，不另建清单 |
| UX26 | P2 / S | 空数据与业务说明减负 | **已实现，空成绩实测通过**。未录入成绩显示空状态而非真实 0.00 绩点，说明就近展开；业务失败与无数据仍分开呈现 |

接下来的顺序：运行本候选版的安装升级、系统通知与自启等原生验收，并完成远端 CI 与公开交付；网络与账号权限具备时继续原 R01–R07。随后按 UX13 招募定向试用，检验首次操作、回访价值与成长节奏。不要为了等待所有 P2 完成而无限推迟真实反馈，也不要把尚未做的试用或安装包验收提前记为完成。

明确不作为首批试用前置：校内后端、Docker、好友庭院、排行榜、商城支付、账号云同步、AI 聊天、整机 VPN、多平台 GUI、全面自动更新、新增大量伙伴。用户既定的校园业务目标保留，延期部署的决定继续有效。后端将来可服务公共缓存、内容分发与自愿同步，但不能替代产品体验和学校授权。

### 54.10 怎么证明产品更完整，而不是只觉得更漂亮

以下是建议试验，不是已做或有统计意义的行业指标：

1. 找 5 位符合首批画像的同学，给同一版本安装包和一句话介绍；让其自行完成“装好 → 选伙伴 → 写小事/开专注 → 关闭主窗 → 找回应用”。观察卡点，尽量不口头代操作。
2. 修阻断后扩到约 10–20 人，试用 7 天。分别问第一天为何下载、第三天是否主动打开、第七天保留或停用的原因；宠物党与校园工具党分开记录。
3. 记录有分母的安装成功人数、首个核心动作完成人数、主动回访人数、常用功能和失败原因。用访谈或用户自愿提供的本机统计即可，不暗中加埋点，不收集成绩、课表或密码。
4. 将“看得懂但不想装”“装好后没事做”“常驻太打扰”“校园查询失败”视为不同问题。先修阻碍真实使用的原因，再增加被明确需要的内容。
5. 通过标准应与这轮承诺对应：下载得到的画面一致；首次核心流程不需要开发者指导；隐藏/退出/提醒可控；一周记录可回看；至少有一个同学愿意反复使用的具体场景。不能用所有测试全绿、Star 或漂亮截图替代这些判断。

在保留原定 1.0 校园业务范围的情况下，R01–R07 与最终候选包交付仍需完成。上述定向 beta 试用可以更早进行，但必须清楚标明它测试的是哪些能力。

## 55. 产品体验实施与候选版准备（2026-09-27，源码未发布）

本节接续第 54 节审查，记录当前工作树中的实现与证据。**公开版本仍是 beta0.9.2；beta0.9.3 本地安装包和便携包已构建，尚未公开发布。** 第 52、53 节是此前源码阶段的记录，已有证据保留，但不代替本轮新增行为或最终安装包验证。UX01–UX26 状态统一维护在第 54.9 节，不另建任务文档。

### 55.1 源码已经落地的体验

| 范围 | 已实现 | 保留的边界 |
|---|---|---|
| 学习与校园入口 | 学习按专注、课表、成绩分区；校园服务按空间、公告、常用和琴房进入。保存公告来源与本科/研究生选择；空成绩使用真实空状态 | 没有补造课表或成绩；身份选择不授予学校权限，真实业务仍按 R01–R07 验收 |
| 待办与记录 | 编辑文字、计划日期、完成时间、归档/恢复和分类；近七日查看真实完成的小事与专注分钟 | 旧待办没有的日期不推测补齐；删除记录后不承诺仍能回顾该记录 |
| 专注 | 保留 5/25/45 分钟快捷项，新增 1–120 分钟自定时长与可选待办；开始时记录任务文字，完成领取后写入历史 | 不监控学习行为，不自动勾完关联待办；取消不给完成奖励。历史最多保留最近 365 条，旧累计值不伪造为历史记录 |
| 常驻与跨日 | 日期变化、窗口恢复可见时刷新每日内容、待办默认日期和周轴；保留未提交草稿。安装版新增专注完成通知、勿扰、宠物显隐/置顶记忆、主动开启的 Windows 自启 | 完全退出后不通知；自启默认不登记。真实候选包的通知、重启恢复和自启仍待核验，不宣称可识别全屏应用 |
| 回访与伙伴 | 回访首页收紧欢迎展示，突出下一件事；七次非连续来访可阅读校园小故事、领取纪念物，并在回忆册与小屋展示。荔宝、小白、阿青补齐普通/开心/低落/睡眠状态，栗栗沿用四状态；四伙伴共 56 条差异化照料台词 | 错过一天不清零，没有连续签到要求；四位伙伴状态形象保持一致。故事与纪念物不等于无限中期内容 |
| 庭院与反馈 | 持久解锁、调整专注/摸头/作物成长；六块田可选择，种子和操作集中到工具面板，保留明确文字与键盘操作。根据实际到账提示奖励；带校园背景的分享图可先预览，再主动保存 PNG | 全套美术统一仍未完成。分享图展示当前伙伴、真实本机累计专注与庭院数量，不含学校个人数据；由用户决定是否分享 |
| 设置与交付准备 | 手动检查正式/测试发布渠道并打开说明/下载；复制可预览的无凭据环境信息；补齐便携包与安装版构建目录内许可材料及不同登录方式的隐私说明；将新增回归加入 CI 清单 | 未自动下载/安装更新，不自动上传诊断或开反馈群；本候选版实际安装升级与原生行为仍待验收 |

本轮同时修复确认框 Esc 沿用旧值、待办编辑字段冲突、专注提交后草稿清理、待办筛选丢失表单草稿、公告选择保存时的写入冲突，以及慢公告查询占住写入锁的问题；补回常用服务的联系电话入口，修复跨日待办日期与周回顾坐标更新、研究生错误状态串用和版本信息等待慢校园查询等问题。场地查询不再把所有访问失败都误报成“需要校园网”，连接错误与真实业务权限分别处理。

新增存档字段继续按白名单规范化，兼容旧存档和备份往返。旧伙伴、作物、已领取奖励及累计专注保留；已有作物和伙伴等级可恢复庭院解锁。为新待办/专注历史扩大存档容量，并修复通用请求限制与工作区限制不一致的问题；实际路由大请求回归已通过，见第 55.3 节。

### 55.2 新手体验与成长数值

新存档仍有 **40 荔枝币、3 份点心、4 颗萝卜种子、2 颗草莓种子**，并预种一块约 1 分钟成熟的萝卜。立即浇水后剩余时间缩短为原来的 75%，约 45 秒可收获；不需要学校账号或先买道具。每日补给仍为 20 币、1 份点心和 2 颗萝卜种子，作物不会枯萎。

| 作物 | 生长时间 | 一颗种子成本 | 一次收获 | 每件售价 | 收获成长 | 单块田理论净币/分钟 |
|---|---:|---:|---:|---:|---:|---:|
| 小萝卜 | 1 分钟 | 4 | 2 个 | 3 | 1 | 2.00 |
| 草莓 | 5 分钟 | 12 | 2 个 | 12 | 5 | 2.40 |
| 蓝莓 | 20 分钟 | 24 | 2 个 | 38 | 20 | 2.60 |
| 荔枝 | 60 分钟 | 40 | 3 个 | 70 | 60 | 2.83 |

上表净币是按未浇水、收获后全部出售且不间断种植计算的规则值，不是实际用户收益。长周期作物单次收获更有价值，保留一分钟萝卜作为新手即时体验；装饰和开田价格没有为延缓进度直接上涨。

- 专注每完成并领取 **1 分钟 = 1 币 + 1 成长**；一次 45 分钟与九次 5 分钟的直接专注奖励均为 45 币、45 成长，不再鼓励拆短计时。
- 每只伙伴每日前 **3 次有效摸头**各给 2 成长、3 亲密度；后续仍可互动并获得情绪反馈，保留原有 10 秒间隔。
- 每日最多 **5 件首次完成的小事**各给 2 成长；编辑、取消后重勾不会重复发奖，删除重建也受当日上限约束。
- 浇水缩短剩余时间，不再额外刷成长。伙伴仍每 50 成长升一级、上限 20 级；庭院解锁独立保存，切换低等级伙伴不会重新锁住已用过的作物。
- 专注纪念章改按累计 75 分钟判断；旧版已领取的纪念章保留。补给、每日目标、成就、专注和回访纪念物都不能重复领取。

这些规则解决了审查中可计算的奖励倒置与重复操作问题，但七天后的新鲜感、消费目标容量及真实回访意愿仍需第 54.10 节的试用验证。

### 55.3 已有验证与待补证据

| 验证范围 | 当前证据 | 尚缺内容 |
|---|---|---|
| 前端、规则与 Electron 模块 | 25/25 个 Node 检查脚本通过，涵盖庭院、旧存档、成长、奖励、页面交互、学校状态、桌宠及桌面偏好等；最终分享预览/保存调整后补跑交互 15、学习记录 6 项及语法检查通过 | 规则检查不替代真实安装、系统通知和实际用户试用；GitHub CI 结果尚未取得 |
| Go 与存档容量 | `go test ./...` 与 `go vet ./...` 通过；实际 HTTP 路由 >1 MiB 存档可保存，>2 MiB 返回拒绝并保留原档；Workspace / Booking / Release 回归通过 | 学校真实数据与权限仍按 R01–R07 验收 |
| 真实学习与设置路径 | 关联待办的 1 分钟专注 → 切页 → 领取 → 周记录增加 1 分钟；待办编辑文字/日期、完成、归档后记录保留；研究生选择与公告来源重开保留；空成绩、版本查询和三行无凭据反馈复制均已实测 | 跨日采用时钟回归，未实际守候跨午夜；没有以便携页面验证安装版原生能力 |
| 真实庭院与分享路径 | 首次回访章节领取并展示纪念物；成熟萝卜收获、切换田地、种草莓、浇水及后续自然成熟；最终候选构建生成分享预览，主动保存并查看 1200×800 像素校园 PNG | 长期节奏、内容容量与回访意愿仍需真实同学试用 |
| 最终页面与同版图片 | 1280×900 首页、农田、伙伴小屋、学习页均检查实际截图；最终农田在 420×850 窄窗无横向溢出，可滚到集中工具面板。检查路径无浏览器控制台错误；四伙伴四状态图已核对 | 不等同于 Windows 原生窗口、物理多屏或完整宣传录屏验收 |
| 候选构建与一致性 | 本地安装包与便携 ZIP 构建成功，校验值见下表；安装版构建目录 `app.asar` 版本为 0.9.3，16 个 mjs/cjs/html 文件与源码一致，sidecar 与 `dist` 程序一致，庭院引擎与源码一致；ZIP CRC 无错且内含程序一致；ZIP 与 `win-unpacked` 许可检查通过 | 未运行本候选包实际安装/升级/卸载、原生 GUI、系统通知或自启；未拆解 NSIS 安装程序内部。构建目录核对不能代替这些验收 |
| 对外试用与宣传 | 同版静态截图与分享图已就绪；本轮尚未开展真实同学试用，尚无完整宣传录屏 | 5 人首轮流程观察、约 10–20 人七日试用、15–30 秒实机演示；常驻资源、睡眠恢复和物理多屏待现场验证 |

**最终本地候选文件**（beta0.9.3，未公开发布）：

| 文件 | 字节数 | SHA-256 |
|---|---:|---|
| `desktop/electron/release/szuDesktop-Setup-0.9.3.exe` | 118,283,590 | `7547c7fd4f7bcb56caa8a1ea4dcf51341feb264b38c7682386de9ffa0c344fae` |
| `dist/szudesktop-windows-amd64.exe` | 13,108,224 | `b565865ad1c4d59ffe379ff9062b39e98f378a666fee731a85638227bd871b7a` |
| `dist/szudesktop-beta0.9.3-windows-amd64.zip` | 7,850,720 | `32594df8bfd35175e5fac352ed04fd153e372226ade1fe2abaf3d2997dac8e8d` |

安装包 SHA-256 与同名校验文件一致。最终四张界面截图均来自上述便携程序与独立合成测试存档：[首页](screenshot-home-preview.png)、[农田](screenshot-farm-preview.png)、[伙伴小屋](screenshot-garden-preview.png)、[学习与周回顾](screenshot-study-preview.png)。[分享图](screenshot-share-preview.png)为同一程序实际导出文件，展示合成测试存档中的实际累计值，不含学校个人数据。

发布说明抽取 12 项检查与许可检查通过。升级验收脚本同步处理了新待办/偏好字段、四伙伴状态图标与学校登录卡片分区；保留旧格式升级样本，严格核对原有内容及明确的新字段默认值，丢失内容或伪造日期仍会使检查失败。纯 Node、Python 断言与语法检查通过。

代码已推送至 [PR #19](https://github.com/SzuDesktopTeam/szudesktop/pull/19)。首轮 [CI 36258992671](https://github.com/SzuDesktopTeam/szudesktop/actions/runs/36258992671) 的功能回归、macOS 凭据、五平台 CLI 构建及 Windows 便携包运行检查通过。Windows 安装流程已实际完成旧版安装、升级、旧文件清理和升级前存档/凭据/宠物配置字节保留，后续界面检查因脚本仍引用已改名的 `.companion-picker` 中止。滚动和截图选择器已改为当前的 `.pet-roster-card`，并核对其他选择器；不跳过断言，完整安装闭环以修正后的 CI 结果为准。

早一轮便携候选曾通过运行冒烟；本节不将该结果算作最终安装包验收，也不沿用 beta0.9.2 的安装升级结果。后续原生验收、远端 CI 和发布结果直接更新本节，不另建清单。

### 55.4 学校业务与发布边界

用户确认当前在校园网内，暂无本科权限测试账号。学校官方研究生页面可达，但登录已经过期，已请用户在学校页面重新登录；本轮还没有新的真实课表、成绩或预约验收成功证据。只读网络诊断观察到学校域名经过本机代理的 Fake-IP 路由且 HTTP 返回为空，这只是排查线索，不能据此断言唯一原因或认定用户在校外。公开场地查询的访问错误提示已分类修正，不再把连接失败一律解释为需要校园网。

**R01–R07 仍按第 50.2 节逐项推进，不能因页面可达、入口重排或自动化回归通过而关闭。** 本科权限、真实数据核对、完整预约与学院琴房权限仍待各自证据；不为了测试提交占座预约。

本轮已生成 beta0.9.3 本地候选安装包和便携包，尚未公开发布。下一步完成第 55.3 节保留的原生验收与远端 CI，再更新公开版本、README 下载链接、附件和发布说明。校内后端、Docker、云同步、自动安装更新继续延期；试用和宣传仅介绍已验证范围。
